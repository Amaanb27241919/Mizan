# MĪZAN — Backlog

> **Generated 2026-07-07** by sweeping the last 7 days of session notes, memory files, and audit/roadmap docs, then **verifying every item against the actual code/git** — not just what the docs claimed. That verification reclassified 5 "pending" items as already shipped and 2 as obsolete.

## How to use this file

Persistent backlog. Future sessions read this instead of re-deriving from scattered notes. Each item has a stable **pick-ID** (F1, N3, …), an **effort** (S/M/L), a **user-value** rating, what **remains**, and a one-line **evidence** proof from the code.

**Operating mode (owner decision, 2026-07-07): MAINTENANCE.** Do not build net-new features (`N`/`M`/`P` buckets) unless the owner explicitly asks, it is driven by **user feedback**, or it fixes **something broken**. The `F` (fixes/correctness) bucket is the only one green-lit by default.

| Bucket | Meaning | Default action |
|---|---|---|
| **F** Fixes / correctness | Something wrong or imprecise | ✅ Fair game in maintenance mode |
| **N** Net-new features (autonomous) | New surface / capability | ⏸ Parked — explicit ask only |
| **M** Needs migration | New feature + DB schema change | ⏸ Parked — needs owner approval (CLAUDE.md §8) |
| **P** Parked / not-autonomous | Feature blocked on keys or external setup | ⏸ Parked |
| **O** Blocked on owner | Re-auth, live trade, API key, decision | 👤 Owner action |
| **S** Already shipped | Docs over-claimed as pending | 📝 Doc-correction only |
| **X** Obsolete | Superseded | 🗑 Drop |
| **U** Unverified | Verification hit a session limit | 🔎 Re-verify before acting |

---

## F — Fixes / correctness (autonomous)

The "something is wrong" bucket. Aligned with maintenance mode.

### F1 — Zakat calculation correctness verification

- **Status:** partial · **Effort:** M · **User value:** high · **Autonomous:** yes
- **Remaining:** 1) Hawl tracking (the actual open work): pure Hijri-calendar conversion (hand-rolled tabular/Umm-al-Qura algorithm — do NOT add an npm package, CLAUDE.md §8), per-asset hawl start-date capture on mizan_manual_assets entries (they already store an `added` date + persist to user_state JSON), a hawl-completion countdown in the ZakatSadaqah hero, and a "Zakat due" reminder reusing the existing push-notification cron. 2) Optional but recommended for a task literally titled "correctness verification": add unit tests for the metals-endpoint conversion (oz→g, 87.48/612.36 nisab) and the zakat calc (invFactor, debt deduction, 2.5%, nisab gate) — currently verified only by inspection.
- **Blockers:** No hard blocker. The verification half is done (logic confirmed correct, no bug). The hawl half is a Medium feature build — autonomous IF built with a hand-rolled Hijri conversion (no new npm dependency per CLAUDE.md §8) and hawl dates carried in the existing user_state / mizan_manual_assets JSON (no dedicated table, so no migration). Owner approval would only be needed if the team wants a dedicated hawl table or a third-party date library instead.
- **Evidence:** Nisab/Stooq logic VERIFIED CORRECT: lib/handlers.mjs:1777-1831 (/api/metals/spot) — TROY_OZ_TO_G=31.1034768, nisab weights 87.48g gold / 612.36g silver (lines 1820-1821), parses Stooq xauusd/xagusd CSV Close col, validates finite/>0, 12h cache, graceful {ok:false,source:"fallback"}. MizanApp.jsx:3190-3242 nisabValueFor/useLiveNisab prefer live over static (NISAB_GOLD_USD=8310/SILVER=670 fallback, d.ok guard = no NaN). MizanApp.jsx:3599-3610 zakat calc: invFactor(full 1.0/longterm_30 0.30), zakatable manual filtered, short-term debt+negativeBank deducted (AAOIFI), zakatDue=zakatable*0.025, abo…

### F2 — Fix the lock-less /api/cron/bot-signals handler: duplicate-execution races (#3–#6)

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** The lock-less cron fix (#3-#6) is not started — only the unrelated approve-path race #1 (eafae77) shipped. Implement a per-strategy claim/CAS at the top of the strategy loop (5748) to guarantee one cron fire owns a strategy per tick — reuse the existing bot_strategies.updated_at column as a lease (atomic .update({updated_at:now}).eq('id',id).lt('updated_at',threshold).select() like eafae77); a single claim closes #3 (duplicate BUY), #4 (double-SELL), and #5 (racy DCA guard) since all three run inside one loop iteration. For #6, on an ambiguous SnapTrade failure resolve the signal to a terminal/needs-review state instead of leaving it pending/reusable. Add tests for concurrent-fire behavior. The alternative robust fix (unique partial index enforcing one executed signal per strategy/day/side) would require an owner-approved migration per CLAUDE.md §8 — the CAS path avoids that.
- **Evidence:** Races #3-#6 all confirmed OPEN in lib/handlers.mjs cron handler (5689-6106): no claim/lock/CAS in the `for (const strat of strategies)` loop. #3: strategies loaded plain at 5725-5728, stale trades_today read at 5751, non-atomic increment at 6074. #4: pos from botPositionFromSignals at 5867, netQty gate 5874, SELL insert 5884 marked executed only at 5897. #5: DCA count 5779-5781 then insert 5831, and migration 020 (supabase/migrations/020_trading_bot.sql:48-49) shows pending_signals has ONLY plain indexes, no unique constraint. #6: !exec.ok leaves signal pending (6085), catch at 6099. Only rac…

### F3 — purification_ratios table needs an ongoing maintenance path

- **Status:** partial · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** No global admin maintenance UI exists and the shared table is still only 8 tickers. To finish autonomously: add an owner-only CRUD route (e.g. POST/PUT/DELETE /api/admin/purification-ratios) reusing requireAdmin() + sbAdmin (service role bypasses the RLS write-block, so no schema change needed), plus a small "Purification ratios" section in the existing AdminPanel (MizanApp.jsx:8127) to add/update ticker→impurity_pct→source. Alternatively, the already-coded Zoya provider feed (handlers.mjs:5091-5098) activates automatically once ZOYA_API_KEY is provisioned (that route is blocked-external). Also correct MIZAN-STATE-AUDIT.md line 468: uncovered tickers fall back to DEFAULT_PCT=1.5%, not 0%.
- **Evidence:** Table + seed: supabase/migrations/019_purification.sql (8 tickers, RLS service-role-write-only USING(false)). Coded-but-dormant data-provider feed: lib/handlers.mjs:5091-5098 (screenBatch→nonPermPct, gated `if (activeShariaProvider()==="zoya")` — Zoya unprovisioned per CLAUDE.md §11). Resolution order override→ratioMap→screenNonPerm→default at handlers.mjs:5104-5113. Per-user manual override UI SHIPPED: MizanApp.jsx:3492 (click-to-edit %), persist MizanApp.jsx:3304. Unknown tickers fall back to DEFAULT_PCT=1.5 (handlers.mjs:5073,5102) — NOT 0%, so state-audit line 468 "fall back to 0% (no pur…

### F4 — Day-weighted-average-capital % return (ROAI denominator) — Phase 2 correctness remainder

- **Status:** pending · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Implement a whole-period Modified-Dietz/ROAI % return: (1) add a pure function to src/lib/performance.js computing gain ÷ day-weighted average invested capital (start value + Σ flow×(days-remaining/period-days) as denominator), reusing the existing DEPOSIT/WITHDRAWAL flow parsing in buildCashflows/flowsByDate; (2) Vitest cases in src/test/performance.test.js (fixture-validated like the XIRR Excel fixture); (3) surface it as a stat in PerformancePanel.jsx next to MONEY-WEIGHTED (XIRR) with FM font, fmtPct, and mask(). Optional later refinement: feed it the dense daily balance curve once Phase 4's materialized balance table exists.
- **Evidence:** Grep of src/ + lib/ for `dietz\|roai\|avgCapital\|averageInvested\|investedCapital\|capitalDays\|dayWeighted` = 0 hits. src/lib/performance.js (230 lines, read fully) exports only xirr/buildCashflows/moneyWeightedReturn (lines 45-105), unrealizedFromHoldings/realizedFromActivities (110-140), and risk metrics (146-230) — no Modified-Dietz/ROAI function. src/components/PerformancePanel.jsx:62-77 shows XIRR (`mwr`) + `simplePct` (P&L/cost), not a day-weighted denominator. Phase 2 commits 87ece9b + b7fca08 shipped the panel; no ROAI commit since. MIZAN-BENCHMARK-ROADMAP.md:37 and :63 explicitly mark th…

### F5 — Remaining npm audit findings: esbuild/vite (dev-only, needs breaking vite@8)

- **Status:** pending · **Effort:** M · **User value:** low · **Autonomous:** yes
- **Remaining:** Clear the 2 dev-only findings by upgrading the build tool: bump vite from ^5.4.1 to ^8.x and @vitejs/plugin-react (^4.3.1) to a vite-8-compatible major, run `npm audit fix --force` or a manual bump, then verify `npm run build` and `npm test` (vitest 4) still pass. This is a two-major-version jump (Node 20+ requirement, plugin/vitest compat, vite.config changes) with genuine regression risk on the 11k-line SPA build — the reason it was intentionally deferred. Nothing here reaches production runtime.
- **Evidence:** npm audit (current) = 2 vulns: esbuild (moderate, <=0.24.2) + vite (high, depends on vulnerable esbuild); fix message "Will install vite@8.1.3, which is a breaking change". Installed vite 5.4.21 / esbuild 0.21.5 (package.json:41 "vite": "^5.4.1"). Prod-dep patch landed in commit 1c34aed (2026-07-05): package-lock.json +1572 lines, form-data now @4.0.6, undici @7.28.0, opentelemetry @7.29.7 (all patched). MIZAN-STATE-AUDIT.md:8 finding #7 states the exact same framing (prod deps patched; esbuild/vite left, dev-only, needs breaking vite@8).

### F6 — Fully deprecate the legacy .snaptrade-users.json plaintext flat file

- **Status:** partial · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** The production/security substance is DONE — handlers.mjs no longer reads or writes the plaintext file (the state-audit's claim that "the file-read code path remains active" is false). To "fully deprecate": (1) update MIZAN-STATE-AUDIT.md line 270 (Section 4 secrets scan) and line 466 (Section 8 #2), which still list the flat file as an active plaintext risk — stale since 2026-06-16; (2) retire or neutralize the flat-file dependence in the owner-only scripts, chiefly scripts/verify-refresh.mjs:32 which still reads mizan_primary from it (snaptrade-wipe-credentials.mjs legitimately keeps a delete-if-present cleanup step). No production code change required.
- **Evidence:** Production path removed: lib/handlers.mjs:856-858 comment "The legacy .snaptrade-users.json flat-file path has been removed"; getSnapForRequest (lib/handlers.mjs:859-934) is Supabase-only (reads encrypted user_snaptrade row / registers fresh user, no fs IO). Removal shipped in commit f600d9d (2026-06-16, "security: AES-256-GCM encryption for secrets"). grep readFileSync\|writeFileSync\|snaptrade-users over lib/ and api/ = zero file-IO hits (only benign comment handlers.mjs:202 + log label :480). Residual refs are owner-only scripts: scripts/verify-refresh.mjs:32 reads the file, scripts/snaptrad…

### F7 — Sharia screener reliability — Finnhub rate-limit handling

- **Status:** partial · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Route the four Finnhub proxy endpoints (news, earnings, profile2, metric) through fetchWithRetry with 429/Retry-After-aware exponential backoff instead of raw httpsGetJson, and add a short shared server-side cache for news/earnings (in-memory per-warm-instance, mirroring lib/sharia.mjs's _cache, to keep it migration-free) so users don't each re-spend the Finnhub budget. Then update CLAUDE.md sec10 #7 / state-audit to mark the accordion rate-limit path hardened. Optional stretch: a client-side expand throttle/queue on rapid distinct-ticker expansion.
- **Evidence:** Mitigations present: accordion client caches — news 5-min TTL per ticker (src/components/MizanApp.jsx:4278,4281-4296) + ONE shared earnings-calendar call 30-min TTL (4279,4298-4310), single-open (openTk 4423) + cached-reopen early-return (4442) [commit 84cadee]; app-side marketdata 150/hr bucket on /api/finnhub/* explicitly protecting the 60/min upstream (lib/rateLimit.mjs:43-47,90-92) [1c34aed]; quote fallback Finnhub->Alpaca->Yahoo (lib/handlers.mjs:635-669) [453e002]; screener verdict per-day in-memory cache (lib/sharia.mjs:183-234). NOT done: httpsGetJson has zero retry (lib/handlers.mjs:…

### F8 — Invite email: rebrand to match Mizan + fix spam deliverability (user-reported) — ✅ DONE

- **Status:** ✅ DONE (2026-07-12) · **Effort:** M · **User value:** HIGH · one optional owner step left (push auth templates)
- **Reported 2026-07-12:** owner sent 4 invites blind (no preview), landing in Spam + off-brand. **Follow-up:** re-invite those 4 via the new panel now that branding + DMARC are fixed (their originals went out unbranded + pre-DMARC).
- **SHIPPED (branding + previewability):** owner-only `POST /api/admin/invite` (`lib/handlers.mjs`, near run-cron) — `sbAdmin.auth.admin.generateLink({type:"invite"})` mints the action link WITHOUT Supabase's default email, then sends a MĪZAN-branded invite via `sendUserEmail`/`renderBrandedEmail` from `ALERT_FROM` (alerts@mizan.exchange). New **"Invite a user"** form in Admin → Users. Also fixed the email footer (was "you have a Mizan account" — wrong for invites → configurable `footNote`), added a "Learn more" button → www.mizan.exchange, and added `<meta charset=utf-8>` (the `→`/`Ī` were garbling).
- **DELIVERABILITY FIXED (DMARC added):** `mizan.exchange` DNS is on **Vercel** (`ns2.vercel-dns.com`). Verified via `vercel dns ls` there was NO DMARC (only DKIM `resend._domainkey` + `send.` SPF). Added TXT `_dmarc` = `v=DMARC1; p=none; rua=mailto:dmarc@mizan.exchange; fo=1` via `vercel dns add` (rec_ffe0b0304563df7a441821bd). DKIM already aligns for the domain, so DMARC passes on DKIM. **Later:** tighten `p=none` → `p=quarantine` after a week of clean reports; optionally add root SPF `v=spf1 include:amazonses.com ~all`.
- **Also shipped (owner mid-turn ask):** all **6 branded Supabase Auth email templates** generated from `renderBrandedEmail` into `supabase/email-templates/` (confirmation, invite, magic_link, recovery, email_change, reauthentication) + `subjects.json` + README. Regenerate with `scripts/gen-auth-email-templates.mjs`; apply via `scripts/push-auth-email-templates.mjs` (needs the owner's `SUPABASE_ACCESS_TOKEN`) or dashboard paste. **OPTIONAL OWNER STEP:** run the push (or paste) so Supabase-native auth emails match too — the app-side invite already works without it.
- **Root cause:** invites are **Supabase-Auth-native**, NOT app code — `grep -rni invite lib src api` = **zero hits**. So they use Supabase's default "Invite user" email template, sent via the custom SMTP → Resend (sender `no-reply@mizan.exchange`, see memory `email-sender-domain`). They do NOT go through the branded `renderBrandedEmail` shell in `lib/alerts.mjs` (that's app emails only: digest/re-auth/bug-report). So both the look AND the deliverability are configured in Supabase + DNS, not in this repo.
- **Remaining — two parts:**
  1. **Branding:** in Supabase Dashboard → Authentication → Email Templates, replace the "Invite user" template (and ideally Confirm signup / Magic Link / Reset) with a Mizan-branded HTML matching `renderBrandedEmail` (logo header, paper/navy palette, Fraunces/IBM Plex, the `{{ .ConfirmationURL }}` CTA button). I can draft the template HTML here; owner pastes it into the dashboard.
  2. **Spam / deliverability:** diagnose why it hits spam — verify **SPF, DKIM, and DMARC alignment** for `mizan.exchange` (Resend domain must be verified with DKIM signing for the auth-SMTP sender; DMARC record present; the SMTP `from` domain must align with the DKIM `d=`). Add a plaintext alternative part, avoid spammy phrasing, consider a `List-Unsubscribe` header, and warm the domain. Validate with mail-tester.com / Google Postmaster Tools before re-inviting. Note the auth SMTP path is separate from the app's Resend `ALERT_FROM` path — check the Supabase Auth SMTP settings specifically.
  3. **Preview before send:** so the owner never sends blind again — either test-invite themselves first, or (nice-to-have) build an app-side branded invite via `lib/alerts.mjs` + `inviteUserByEmail`'s redirect so invites use the same shell as other Mizan emails and are previewable/renderable to a file.
- **Evidence:** no invite code in repo (`grep -rni invite lib src api` = 0); `lib/alerts.mjs` branded shell (`renderBrandedEmail`, ALERT_FROM `alerts@mizan.exchange`) covers app emails only; Supabase Auth emails send via custom SMTP → Resend as `no-reply@mizan.exchange` (memory `email-sender-domain`). So the fix surface is Supabase Auth Email Templates + SMTP + `mizan.exchange` DNS (SPF/DKIM/DMARC), plus an optional app-side branded-invite path.

---

## N — Net-new features (autonomous, PARKED)

Building these is the treadmill the owner is stepping off. Build only on an explicit ask or a user-feedback trigger.

### N1 — Hijri calendar / Hawl tracking for Zakat

- **Status:** pending · **Effort:** M · **User value:** high · **Autonomous:** yes
- **Remaining:** Entire feature; nothing started. Build: (1) Hijri date conversion via built-in Intl.DateTimeFormat('en-US-u-ca-islamic') — no new npm package; (2) per-asset hawl start-date capture + storage in the existing user_state key/value store (avoids a new migration); (3) hawl-elapsed / lunar-year countdown + due-date logic feeding the ZakatSadaqah panel (MizanApp.jsx:3551); (4) giving-mode / Ramadan-detection UI surface; (5) daily zakat-due reminder push cron (new /api/cron path + vercel.json entry — daily is Hobby-plan compatible, reusing existing web-push infra). Store hawl dates in user_state (NOT a new table) to stay migration-free.
- **Evidence:** No commits (git log --since=14d shows debt/perf/trade/ETF work only, none hawl/hijri). Zakat calc is plain 2.5%: MizanApp.jsx:3609 `zakatDue = zakatable*0.025`, MizanApp.jsx:1512 same — no hawl gate/countdown/due-date. Only incidental refs: MizanApp.jsx:3765 disclaimer text "hawl timing"; handlers.mjs:200 AI-prompt "lunar-year context if MIZAN provides it" (never actually provided); MizanApp.jsx:619/644 demo Ramadan Sadaqah data. No Hijri/date lib in package.json (no moment-hijri/luxon/date-fns). Migrations end at 024 — no hawl table/column. No zakat-reminder cron in vercel.json. MIZAN-STATE-…

### N2 — AI portfolio insight card on Overview (AI roadmap pending task #2)

- **Status:** pending · **Effort:** M · **User value:** high · **Autonomous:** yes
- **Remaining:** Build the entire feature: a new Overview BentoTile (or CollapsibleTile) "AI insight" card that (1) assembles grounded facts already in client state — top-position concentration % from `merged`/mapPosition, halal diversification and shared-holdings redundancy (can reuse the ETF Overlap Analyzer's overlap engine / etfHoldings.mjs data), purification-due total from the purification log, and nisab-proximity from Zakat state; (2) POSTs those facts to the existing auth-gated /api/advisor with an explain-don't-judge / never-invent-a-number prompt (mirror the explainVerdict pattern at MizanApp.jsx:2347); (3) caches the generated narrative per-day in localStorage (follow the existing mizan_intraday / purification localStorage patterns — no DB needed); (4) handles loading/empty/error states in both light and dark themes, uses T.* tokens, FU/FP/FM fonts, and mask() on any numbers. Never captures in demo mode.
- **Evidence:** No implementation exists. grep -rn -i "insight" src/ lib/ → zero hits; grep for concentration/diversification/nisab-proximity/aiCard/InsightCard/proactive/dailyInsight/ai_insight/portfolio_insight in src+lib+supabase → zero relevant hits. Only AI-on-Overview-adjacent code is a canned suggested chat prompt "What's my biggest concentration risk?" in the AI Advisor tab (src/components/MizanApp.jsx:6713), NOT a proactive Overview card. The one shipped grounded-AI feature is task #1 (screening explanations, commit 60e86fd): explainVerdict at MizanApp.jsx:2347 + button at :2627 — that is the refere…

### N3 — AI Zakat & purification guidance (AI roadmap pending task #1)

- **Status:** pending · **Effort:** M · **User value:** high · **Autonomous:** yes
- **Remaining:** Build the grounded AI Zakat & purification guidance in the Goals/Zakat surface (ZakatSadaqah component ~MizanApp.jsx:3551, and/or PurificationPanel ~3268). Clone the shipped explainVerdict pattern (MizanApp.jsx:2342-2635): (1) assemble a grounded context payload from the user's REAL zakat calc already computed in-component — net zakatable wealth, nisab standard + threshold, 2.5% due, gold/silver, short-term-debt deduction, investment method — plus purification data (per-ticker impurity %, pending purification total from purification_ratios). (2) Add a system prompt in the explain-don't-judge / never-invent-a-number / 'confirm with a qualified scholar' style. (3) Surface an 'Explain my Zakat' button and/or a small grounded Q&A input covering fiqh (hawl, madhhab, what's zakatable, how to purify). (4) Handle loading/empty/error states, both light+dark themes, FM/FP fonts, T.* tokens, mask() on figures. Reuse POST /api/advisor (+ optional /api/advisor/count pre-flight) — no backend route, no env var, no migration.
- **Evidence:** Not started. Only /api/advisor client calls in src/components/MizanApp.jsx are the chat advisor (lines 6669, 6687) and the shipped SCREENING explainer (explainVerdict at line 2342-2356, system prompt line 2354 "Sharia-screening explainer"). The Zakat component ZakatSadaqah (MizanApp.jsx:3551) and PurificationPanel (MizanApp.jsx:3268) have ZERO advisor/AI wiring. git log f482cd0..HEAD (roadmap commit 2026-07-05) shows only debt/perf/trade commits, none for AI zakat guidance. ai-roadmap.md lists it as PENDING item #1. Infra ready: /api/advisor auth-gated at lib/handlers.mjs:1863-1870.

### N4 — Phase 3 remainder: transaction rules engine + Islamic budget categories

- **Status:** pending · **Effort:** M · **User value:** high · **Autonomous:** yes
- **Remaining:** The whole item: (a) pure rules engine lib (e.g. src/lib/budgetRules.js) — rule = triggers (payee/category/amount matchers) + actions including set_islamic_category, flag_for_purification, mark_zakatable, modeled on Firefly SearchRuleEngine / Actual server/rules; (b) Vitest suite for it (src/test/, matching the recurring.js/performance.js rhythm, current suite is 80/80 via `npx vitest run`); (c) localStorage-backed `mizan_budget_rules` store added to TRACKED_KEYS in src/lib/userState.js:16-33 so it syncs via user_state (no new table); (d) Islamic category presets (Sadaqah, Masjid donations, Zakat, Islamic school fees, Halal food) surfaced in Budgeting.jsx category options + spend matching (currently keyed only off Plaid personal_finance_category.primary) and a rules-management UI in the Finances tab; (e) wire flag_for_purification / mark_zakatable outputs so tagged txns are visible (at minimum as txn tags; ideally feeding the existing Zakat/purification surfaces); (f) respect the owner boundary — stay in Budgeting.jsx/Finances, don't move debts from Goals; (g) dogfood in demo mode, npm run build, commit/push, verify Vercel deploy READY.
- **Evidence:** (1) Zero code exists: `grep -rniE "budget_rules\|applyRules\|set_islamic_category\|flag_for_purification\|mark_zakatable\|islamic.?categor\|transaction.?rule"` across /Users/amaankhan/Documents/mizan-app/src and /lib returns nothing. (2) No rules lib or tests: src/lib/ contains only apiFetch.js, auth.jsx, exportCSV.js, formatters.js, performance.js, recurring.js, supabase.js, useKeyboard.js, userState.js; src/test/ has no rules test. (3) Budgeting still uses raw Plaid categories: src/components/Budgeting.jsx:141-151 matches spend via `t.personal_finance_category?.primary`, :98-100 seeds generic FOO…

### N5 — Grounded-AI debt answers via /api/advisor (Track 1 #6)

- **Status:** pending · **Effort:** S · **User value:** high · **Autonomous:** yes
- **Remaining:** Inject a debt summary into the AIAdvisor context so debt questions are grounded: (1) surface a debt summary at the MizanApp level — either lift mizan_debts via the existing userState read path or compute totals with the existing pure helpers readDebts()/debtState(); (2) pass it into AIAdvisor as a new prop; (3) extend the PORTFOLIO SUMMARY string (MizanApp.jsx:6649-6654) with total debt, riba (apr>0) vs interest-free split, and per-debt remaining balances so 'what's my total debt' / 'pay riba-debt vs invest' answer from real data. Frontend-only; advisor endpoint, auth gate, and rate limiting already exist. Add empty-state text for users with no tracked debts.
- **Evidence:** Advisor context omits all debt: src/components/MizanApp.jsx:6640-6655 builds PORTFOLIO SUMMARY (NW, accounts, positions, contributions, dividends, activity count) with no debt/riba/mizan_debts. AIAdvisor signature src/components/MizanApp.jsx:6628 = {accounts,activities,metrics,hasKey}; render src/components/MizanApp.jsx:11366 passes no debt prop. Debt state siloed in Goals: src/components/Goals.jsx:1908. Roadmap marks it Pending: MIZAN-BENCHMARK-ROADMAP.md:25. No commit wires debt into advisor (git log 14d). Data is reachable/persisted: src/lib/userState.js:19 (mizan_debts TRACKED_KEY) + pure…

### N6 — Per-token crypto Sharia screening verdicts (AI roadmap pending task #4)

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** Build the entire feature: (1) add a curated per-token classifier in lib/sharia.mjs that differentiates crypto (e.g. major L1 like BTC/ETH vs interest-bearing/asset-backed stablecoins vs memecoins) and returns per-token nuance with "review" as the safe default, never auto-bless; refine/replace the blanket CRYPTO_RE -> "halal" branch at sharia.mjs:217-219. (2) Optionally add an AI-assisted verdict layer for uncurated tokens reusing the existing auth-gated /api/advisor (grounded, explain-don't-judge). (3) Route held crypto through the engine instead of the client's hardcoded inline status:"review" override at MizanApp.jsx:10366-10372 so verdicts flow from the single source of truth (the "not hardcoded per-surface" requirement). (4) Reconcile the server/client inconsistency (server blanket-halal vs client blanket-review). (5) Add unit tests for the crypto classifier.
- **Evidence:** Blanket-only handling; no per-token logic exists. Client: src/components/MizanApp.jsx:10366-10372 forces ALL crypto holdings to a single hardcoded status:"review" (reason "Cryptocurrency — Sharia status is token-specific and scholar-dependent; Mizan does not auto-classify crypto"), and MizanApp.jsx:10373 filters crypto OUT of the /api/screen call so it never reaches the engine. Server: lib/sharia.mjs:105 CRYPTO_RE fixed regex (BTC\|ETH\|SOL\|DOGE\|ADA\|DOT\|LINK\|AVAX\|MATIC\|XRP\|LTC\|BCH) then lib/sharia.mjs:217-219 returns blanket status:"halal" (stale, unreached for held crypto). grep for memecoin\|a…

### N7 — Sharia X-Ray rules panel

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** Entire feature: a rules-evaluation layer (Ghostfolio apps/api/src/models/rules/ style — pure rule classes returning pass/warn/fail + message) plus a panel UI (BentoTile/CollapsibleTile, likely Portfolio or Overview tab) implementing five checks: (a) haram-concentration — % of portfolio value in h.sh_==="haram"/"review" positions from shariaScreen state via mapPosition(); (b) purification-due — unpurified dividend total from the existing purification log in Goals; (c) compliance-drift — change in compliant % over time (needs a snapshot of compliant-share, could piggyback on nightly net_worth_history or localStorage history); (d) fee-ratio — weighted avg ETF expense ratio vs threshold (needs a small curated expense-ratio map for the 7 halal ETFs or Finnhub profile data — no new API key required); (e) emergency-fund — bankBalance vs Emergency Fund goal target / monthly-spend multiple. Pure client-side math on existing state; add Vitest coverage like src/lib/performance.js.
- **Evidence:** Not started. (1) grep -riE "x-?ray\|rules panel\|haram.?concentration\|compliance.?drift\|fee.?ratio\|purification.?due" across src/, lib/, api/ returns zero hits. (2) git log --since="14 days ago" (HEAD a18453a) contains no related commit — shipped work was debt payoff (0dcb37e), performance analytics (87ece9b), recurring auto-link (0a83a1f), trade-ticket quotes (02db1c0/a18453a). (3) MIZAN-BENCHMARK-ROADMAP.md:38 lists T2 #4 with NO shipped marker while siblings #3/#5/#6/#8 carry explicit "✅ shipped" flags; line 65 slots it in Phase 4 behind the unstarted materialized balance table (T2 #1). (4)…

### N8 — Backtester Phase A: walk-forward / out-of-sample testing + honest metrics + disclaimers

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** All three sub-parts are unbuilt for the backtester tool: (1) add walk-forward / out-of-sample validation to computeSmaBacktest — split the Polygon bar series into an in-sample training window and a held-out out-of-sample validation window (or rolling walk-forward windows), report metrics separately for each so users see whether the SMA edge holds out-of-sample; (2) add honest risk metrics — Sharpe ratio and annualized volatility (reuse the pure, dependency-free helpers already in src/lib/performance.js: sharpeRatio, maxDrawdown, annualizedVolatility) alongside the existing hit rate; (3) add a persistent 'hypothetical past performance does not predict future results / not financial advice' disclaimer to the HistoricalBacktest component UI (MizanApp.jsx ~5340-5424), matching the disclaimer style already used in StrategyReality. All doable in src/components/MizanApp.jsx + an import from src/lib/performance.js.
- **Evidence:** computeSmaBacktest (src/components/MizanApp.jsx:5138-5168) runs a SINGLE in-sample SMA-50/200 backtest over the full bar series — no walk-forward/OOS split; stats = winRate, maxDrawdown, totalRet, buyHold, dist, avgTradeRet (NO Sharpe/volatility). HistoricalBacktest tool (MizanApp.jsx:5340-5424) has a 'Strategy:' note but NO 'hypothetical past performance' disclaimer. Disclaimers exist only in StrategyReality (MizanApp.jsx:5262-5263), the trading-bot NL reality-check shipped in commit bb02dc2 (Jun 24, BEFORE this backlog was queued Jun 27) — a different surface. src/lib/performance.js:178-226…

### N9 — AI-written weekly digest (AI roadmap pending task #3)

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** Upgrade /api/cron/weekly-digest to generate a Claude narrative: (1) assemble per-user week facts server-side - net-worth move (already computed), dividends received + purification owed, newly non-compliant holdings flags, zakat/nisab status (these are computed client-side today, so server-side reconstruction from user_state/holdings is needed); (2) build a grounded prompt (real numbers only, explain-don't-judge, never invent a figure, scholar disclaimer) and make a server-side Anthropic call reusing the existing api.anthropic.com pattern from the bot NL parser; (3) wrap in try/catch so ANY AI failure falls back to the existing template email at lines 4978-4996; (4) pass the AI narrative into sendUserEmail. No frontend change (email only).
- **Evidence:** lib/handlers.mjs:4930-5015 is the /api/cron/weekly-digest route, purely template-based: hardcoded text array at lib/handlers.mjs:4978-4989 built from a 7-day net-worth delta, no Anthropic call. Grep for anthropic\|ANTHROPIC\|advisor\|Claude\|narrative scoped to lines 4926-5020 returns empty. ai-roadmap.md task #3 lists it pending; MIZAN-STATE-AUDIT.md:8 NEXT section names 'AI-written weekly digest' as pending. Reusable plumbing exists: ANTHROPIC_KEY at lib/handlers.mjs:173, server-side Anthropic call pattern at lib/handlers.mjs:4316-4323 (bot NL parser), /api/advisor proxy at line 1863. email_dig…

### N10 — AI tool-calling with per-user enum'd schemas on /api/advisor

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** Everything: (1) define custom Anthropic tools on the /api/advisor payload (e.g. get_holding_detail / get_sharia_verdict) with input_schema whose symbol property is an enum of the user's actual tickers; (2) source the enum + data — client already builds portfolio context and shariaScreen state, or server can call lib/sharia.mjs screenSymbol — and validate the tool input server-side; (3) implement the single-hop loop: on stop_reason==="tool_use", execute the tool against real data and send one continuation request with the tool_result block (either loop in handlers.mjs or in the MizanApp.jsx callAdvisor wrapper), then return the final text; (4) widen/replace the SAFE_TOOLS filter (lib/handlers.mjs:1883) so validated custom tools aren't stripped while still blocking arbitrary tool abuse; (5) preserve prompt caching (ADVISOR_SYSTEM_PREFIX cache_control), the per-user 60/hr anthropic rate bucket, and audit logging on the extra hop; (6) update /api/advisor/count if tool definitions materially change token math.
- **Evidence:** lib/handlers.mjs:1863-1962 — /api/advisor is a single-pass proxy to api.anthropic.com/v1/messages with no tool_use/tool_result continuation loop; lib/handlers.mjs:1883-1886 — SAFE_TOOLS = Set(["web_search_20250305"]) strips any custom tool a client sends, so per-user enum'd tools cannot even pass through today; grep -rn "tool_use\|tool_result\|input_schema\|tool_choice" over lib/, api/, src/ → zero hits; src/components/MizanApp.jsx:736 — frontend's only tool usage is Anthropic's built-in web_search for the price/news fallback; git log since 2026-06-22 has no tool-calling commit (nearest AI commi…

### N11 — Envelope budget rollover + 'left to assign' pot (Track 2 #9)

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** yes
- **Remaining:** Entire feature: (1) month-keyed budget model — compute per-month spent from the Plaid txns already passed to Budgeting, persist month-keyed budgeted amounts + rollover flags in user_state via src/lib/userState.js (no-migration path; an Actual-style schema change is the cleaner alternative but needs owner approval); (2) rollover math per Actual envelope.ts:52-66 — leftover_m = budgeted_m + spent_m + max(prevLeftover, 0); (3) 'left to assign' pot — month income (Plaid inflows) minus total assigned, surfaced as a header tile (Firefly AvailableBudget analog); (4) UI in src/components/Budgeting.jsx — leftover column, per-category rollover toggle, left-to-assign tile, month navigation; (5) demo-mode fixture + unit tests for the pure rollover math.
- **Evidence:** supabase/migrations/013_budgets.sql:5-12 — budgets table is (user_id, category, monthly_limit, currency, updated_at), PK (user_id, category): no month dimension, no rollover/leftover columns. src/components/Budgeting.jsx:142-156 — spentByCat filters to current-month-only (t.date < monthStart); lines 170-178 totals = {spent, budgeted, count} only; zero rollover/envelope/assign logic. lib/handlers.mjs:3266-3339 — /api/budgets GET/PUT handles only {category, monthly_limit}. Codebase grep for rollover\|envelope\|left.to.assign\|carryover: only hits are Goals.jsx:1209,1243 (debt snowball comments, un…

### N12 — Optional min_payment field on the debt form (Track 1 #7)

- **Status:** partial · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Add the optional input to DebtForm in src/components/Goals.jsx: (1) state `const [minPayment, setMinPayment] = useState(initial?.min_payment != null ? String(initial.min_payment) : "")`; (2) a "MIN PAYMENT ($/MO) · OPT" input (FM label + inputMode="decimal", matching the APR/PAYOFF BY grid row style ~lines 1665-1682); (3) include `min_payment: minPayment === "" ? null : (Number(minPayment) \|\| 0)` in the onSave payload (lines 1591-1604). Optional polish: hide or de-emphasize the field when mode === "recurring" since payment_amount takes priority in monthlyMinFor (lines 1176-1178). The planner already consumes it — no other code changes. Then npm run build + update roadmap row 26.
- **Evidence:** READ PATH SHIPPED: src/components/Goals.jsx:1179 (`if (Number(debt.min_payment) > 0) return Number(debt.min_payment);` inside monthlyMinFor, priority 2 after recurring payment_amount, before the 2%-of-balance estimate) and Goals.jsx:2060 (planner input mapping forwards `min_payment: d.min_payment` into computePayoffPlan) — both introduced by commit 0dcb37e (feat(goals): debt payoff planner). FORM FIELD NOT BUILT: DebtForm (Goals.jsx:1547-1742) has no min_payment state/input, and the onSave payload (Goals.jsx:1591-1604) omits it, so users cannot set it — the line-1179 branch is unreachable for…

### N13 — Goal funding schedules

- **Status:** pending · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Implement the whole feature: a pure function computing requiredMonthly = (target_amount − current) / max(1, monthsRemaining to goal.target_date) (handle past/absent target_date, goal-reached, and clamping — model on Actual's goal-template.ts), surface it on GoalCard in Goals.jsx alongside the existing trend-ETA (formatted via fmtUSD + mask, FM font, tabular-nums, both themes), and add Vitest unit tests for the math. All inputs (target_amount, target_date, current per track_mode) already exist client-side; no API or schema work needed.
- **Evidence:** MIZAN-BENCHMARK-ROADMAP.md:44 lists item #10 with no shipped marker (siblings #3/#5/#6/#8 carry explicit ✅ markers); src/components/Goals.jsx:441-529 GoalCard renders only progress % + trend-ETA via projectionLabel (Goals.jsx:188-198, slope from linearRegression at 149-166) — no required-monthly figure; the only (remaining/months-to-target_date) math is for DEBTS in the payoff planner (monthlyMinFor, Goals.jsx:1174-1187, esp. 1183-1184, shipped in debt Phase 1 commit 0dcb37e); git log --all --grep="funding\|schedule" shows no goal-funding commit (26c5313 "funding accounts" = debt payment sourc…

### N14 — Debt `direction` bit: qard hasan you LENT vs owe (Track 1 #5)

- **Status:** pending · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Whole feature, frontend-only in src/components/Goals.jsx. 1) Add a direction field (default "owe", alternative "lent") to the DebtForm save object at ~line 1592 and a UI toggle in the form. 2) Adjust display semantics so a "lent" qard hasan is framed as money owed TO the user (counts down as they get repaid) and is visually distinct from money owed — copy at Goals.jsx:843, :2081, :2086. 3) Separate the total-debt strip/totals so lent-out amounts don't inflate "what you owe". Persists via the existing mizan_debts JSON blob in user_state — no schema change.
- **Evidence:** MIZAN-BENCHMARK-ROADMAP.md:24 marks Track 1 #5 "Pending". No commit references it (git log --all --since=20d matched only unrelated "grounded-AI direction"). Debt object save schema at src/components/Goals.jsx:1592-1604 has name/creditor/icon/original/apr/target_date/mode/linked_account_id/payment_amount/cadence/autopay/funding_account_id — no direction/lent field. Codebase grep for liability_direction\|lent\|is_lent\|owedToYou found nothing debt-related. Tracker is hardcoded as money-you-owe (Goals.jsx:843, :2081, :2086). The qard template (Goals.jsx:1054) only models a debt owed, not one lent…

### N15 — Maintain the ethical/BDS curated exclusion list against an authoritative source

- **Status:** pending · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Do a reconciliation pass: expand/curate ETHICAL_EXCLUSIONS in lib/sharia.mjs (currently 9 tickers) against a current authoritative divestment list (e.g. UN OHCHR settlement-business database ~112 companies, AFSC 'Investigate', Who Profits), correcting stale/adding missing publicly-traded names with per-ticker reasons. Then document the source URL + last-reconciled date + intended refresh cadence in the code comment (sharia.mjs:189-193) since there is no machine-readable feed to cron against. Optionally add a lightweight scripts/ helper that diffs the current list against a manually-pasted source export. No automated periodic 'process' is feasible (sources are PDFs/websites, no API).
- **Blockers:** No hard blocker to the code work (a JS object edit — no re-auth, API key, migration, or live trade). Soft caveat: this is a politically/ideologically charged editorial curation and the 'authoritative source' is not machine-readable, so an agent can do a one-time curated reconciliation but a truly automated recurring process is not buildable; the owner may reasonably want to review/approve which companies get the 'BDS/divestment-target' label before it ships.
- **Evidence:** Feature shipped (commit 6cdecde, 2026-07-02): ETHICAL_EXCLUSIONS + ethicalScreen() at lib/sharia.mjs:194-208; verdict carries ethical:{excluded,reason,list} at sharia.mjs:216,219,233; frontend overlay wired at src/components/MizanApp.jsx:2368,2425,2454,2515-2527 and mapPosition h.bds_ at MizanApp.jsx:10340-10343. BUT the MAINTENANCE chore is untouched: `git log 6cdecde..HEAD -- lib/sharia.mjs` = 0 commits; list is the identical 9-ticker starter (CAT,HPQ,HPE,MSI,BKNG,ABNB,EXPE,CVX,PLTR); no script/cron/doc reconciles it; authoritative sources exist only as a comment at sharia.mjs:189-191. Stat…

### N16 — Benchmark overlay comparing the portfolio vs SPUS/HLAL

- **Status:** pending · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** Entire feature: (a) fetch SPUS and/or HLAL daily closes over the selected range via the existing /api/polygon/bars route (respect Polygon free-tier 5 req/min; polygon_cache already dedupes); (b) normalize both series as value/base−1 (Ghostfolio benchmark-comparator model) — portfolio side reuses the hero chart's existing series builder (mizan_networth_history snapshots + deposit-bucket data in MizanApp.jsx); (c) render the benchmark as a second line on the Overview hero chart (or a PerformancePanel sub-chart) with a benchmark-select toggle (SPUS/HLAL/off), FM-labeled, T.* colors, mask()-aware, both themes, loading/empty states; (d) note in-UI that long ranges use monthly-bucketed portfolio history (Known Limitation #1) so the overlay is approximate until Track 2 #1 (materialized daily balance table) lands. Optional same-line-item sibling: goal funding schedules ((target−saved)/monthsRemaining) is the other half of Track 2 #10 but is a separate deliverable.
- **Evidence:** Not started, confirmed in code: (1) grep -ri "benchmark" across src/ and lib/ hits only roadmap-reference comments — src/lib/performance.js:3 and :208 ("no interest-bearing benchmark" note about the 0% Sharpe rate), src/components/PerformancePanel.jsx:2, src/lib/recurring.js:3 — zero implementation. (2) No SPUS/HLAL comparison/normalization code in MizanApp.jsx (only unrelated hits: ETF Overlap Analyzer comment at MizanApp.jsx:2221, BDS overlay at :2365-2369). (3) git log --all --grep="benchmark"/"overlay" shows only the roadmap doc commit 833696e and the unrelated BDS overlay 6cdecde. (4) MI…

### N17 — Broaden test coverage beyond the pure src/lib modules

- **Status:** partial · **Effort:** M · **User value:** low · **Autonomous:** yes
- **Remaining:** Add unit tests for still-untested backend libs: lib/sharia.mjs (ethicalScreen sector rules; screenSymbol/screenBatch with mocked fetch), lib/crypto.mjs (AES-256-GCM encrypt->decrypt roundtrip), lib/anomaly.mjs detectors, plus more handlers.mjs pure helpers. Add React render tests via the already-installed @testing-library/react + jsdom (start with extracted components Goals.jsx, PerformancePanel.jsx, Budgeting.jsx). Stand up Playwright (add playwright.config, an e2e/ dir, and smoke specs - @playwright/test is already a dependency). Add Zakat + Sharia domain-logic tests. Deepest slice - testing MizanApp.jsx Zakat calc / Sharia screening / view state - requires first extracting pure logic out of the 9,201-line monolith, which CLAUDE.md section 8 gates behind explicit owner instruction. Also fix MIZAN-STATE-AUDIT.md:455-456 which under-claims "zero tests".
- **Blockers:** No hard blocker for the next increment: a coding agent can add backend-lib unit tests (sharia/crypto/anomaly), RTL render tests for the already-extracted components, and Playwright E2E with zero owner action, no new API keys, and no migrations. The only gated portion is comprehensive MizanApp.jsx coverage, which needs owner-approved component extraction per CLAUDE.md section 8 (do-not-split-the-monolith).
- **Evidence:** npm test => "Test Files 6 passed (6) / Tests 80 passed (80)". Beyond-src/lib coverage already exists: src/test/csvExport.test.js:6 tests lib/handlers.mjs (csvEscapeCell @ handlers.mjs:325, toCsv @ handlers.mjs:345 - the 6.1k-line API monolith); src/test/rateLimit.test.js:2 tests lib/rateLimit.mjs (matchAction, added commit a18453a 2026-07-06); src/test/BillsCalendar.test.js:2-7 tests src/components/BillsCalendar.jsx pure exports (median/labelCadence/gapStats/buildBills, commit 27775fe 2026-05-15). Still absent: no RTL render tests (grep NO_RENDER_TESTS though @testing-library/react + jsdom in…

### N18 — Phase 3 remainder: apply the recurring-detection engine to Finances subscriptions/BillsCalendar

- **Status:** pending · **Effort:** M · **User value:** low · **Autonomous:** yes
- **Remaining:** 1) Replace the inline Finances recurring memo (src/components/MizanApp.jsx:8890) with detectRecurringOutflows() from src/lib/recurring.js, preserving the EXCLUDE_CATS/PAYMENT_RE category filters (pre-filter transactions) and adapting the engine's output shape to what the subscriptions table consumes (merchant, cadence, avgPerCharge, estMonthly, lastDate, active). 2) Route the Plaid-native path (MizanApp.jsx:9497-9538) through normalizePlaidStreams() instead of the inline stream mapping. 3) Decide the BillsCalendar half: the component is unmounted dead code since e59c7ad (2026-06-16) — either drop it from scope (and correct MIZAN-STATE-AUDIT.md:163) or re-wire it into the Finances tab consuming the engine output. 4) Optional DRY win: have the bill-reminders cron (lib/handlers.mjs:5247-5330) import the pure engine instead of its own duplicate. Keep everything finance-categorized per the owner boundary (no move into Goals). Update roadmap/state-audit docs after.
- **Blockers:** No hard blockers. One scoping caveat: the item's "BillsCalendar" target is a component removed from the UI on 2026-06-16 (e59c7ad) — an agent must choose between dropping that half or re-mounting the component; functional user delta is small since the inline heuristic already does median-gap cadence and Plaid's native /api/plaid/recurring is preferred when available (this is mostly a DRY/consistency refactor plus better merchant normalization in the fallback path).
- **Evidence:** grep -rn "lib/recurring" src → only src/components/Goals.jsx:14 and src/test/recurring.test.js:10 import the engine (shipped 0a83a1f, 2026-07-06). Finances subscriptions still use the inline heuristic: src/components/MizanApp.jsx:8890 (const recurring=useMemo — 2+ months + median-gap cadence, authored 2026-06-16 in e59c7ad) and the Plaid-native mapping at MizanApp.jsx:9497-9538 is inline, not normalizePlaidStreams. BillsCalendar.jsx is DEAD CODE: e59c7ad removed its import + render from MizanApp.jsx ("Remove BillsCalendar (Upcoming Bills tile) ... entirely"); only src/test/BillsCalendar.test.…

### N19 — Richer per-strategy-type screening in the bot cron (ma_crossover / breakout)

- **Status:** pending · **Effort:** M · **User value:** low · **Autonomous:** yes
- **Remaining:** Implement per-strategy-type screening in the live bot cron entry engine (lib/handlers.mjs, entry-engine block ~5964-6020). Today all momentum-family types score identically by day momentum at line 6016. To finish: (1) for ma_crossover, fetch multi-day daily bars per candidate (reuse the Polygon bar pipeline already used by the client SMA-50/200 backtest; POLYGON_KEY is already provisioned) and require the fast SMA to cross above the slow SMA; (2) for breakout, require price to close above the prior-day high / N-day high rather than just +1.5% day momentum; (3) branch on strat.strategy_type before line 6016 to select the scoring rule, keeping the existing momentum path as default. Keep the affordability filter, entry_threshold, and engine-verified halal gate intact. Note: Polygon free tier is 5 req/min, so bar fetches must stay sequential/cached (polygon_cache table exists, migration 005).
- **Evidence:** lib/handlers.mjs:6016 `const score = (q.current - q.prevClose) / q.prevClose;` is the sole entry-engine scoring formula — identical for strategy_type momentum/ma_crossover/breakout (no per-type branch in the entry engine, lines ~5960-6100). The ONLY strategy_type branch in the entire cron is lib/handlers.mjs:5771 `if (strat.strategy_type === "dca")` (a separate accumulation engine, unrelated). No SMA/crossover or prior-day-high breakout logic exists server-side. strategy_type IS carried through the NL builder (lib/handlers.mjs:4284, defaulted :4394) and the client reality-check backtest (src/…

### N20 — AI anomaly/security-event explainer (AI roadmap pending task #5, lower priority)

- **Status:** pending · **Effort:** M · **User value:** low · **Autonomous:** yes
- **Remaining:** Everything, two integration points. (A) Owner-alert path: add a server-side Claude call (in lib/alerts.mjs or a helper) that turns a security_events / anomaly.mjs event into a plain-language explanation injected into the branded owner email. The client /api/advisor requires a user auth token, so this server-to-server email path cannot reuse it directly; it must call Anthropic directly using the already-provisioned ANTHROPIC_KEY (or refactor a shared server-side explain helper out of the advisor handler). (B) User-facing surface: no per-user security-event feed exists today; security_events is mostly global/owner telemetry (IP blocks, brute force, 5xx spikes, cron staleness) plus per-user new-device events, so an agent must either build a small per-user recent-events list in Settings and hang a client explainer on it (modeled on explainVerdict at MizanApp.jsx:2347), or scope the feature to the owner-alert path only. The explainer logic itself is small (~15 lines like explainVerdict), but there is no existing surface to attach it to.
- **Evidence:** NOT STARTED, no implementation exists. (1) MIZAN-STATE-AUDIT.md:8 lists security-event explainer in the NEXT/pending queue. (2) memory ai-roadmap.md lists it as task #5, flagged lower priority. (3) Grep of src/ and lib/ finds no anomaly/security explainer, only Privacy.jsx:162 (policy line) and the backend detectors in lib/anomaly.mjs (trackAuthFailure/trackSnapTradeError/checkCronStaleness/checkNewDevice). (4) Owner alerts in lib/alerts.mjs:97 formatBody() are a pure text template (Alert/When/Severity + key:value lines), zero AI; no anthropic/advisor/explain reference anywhere in alerts.mjs.…

### N21 — Quarterly refresh of curated Amana mutual-fund holdings

- **Status:** pending · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Refresh is a scheduled future task, NOT started and NOT yet due. Around Oct 2026 (90d after the 2026-07-01 asOf): re-transcribe the top-25 constituents for AMANX, AMAGX, AMDWX, AMAPX from the latest Saturna fund fact sheets / stockanalysis.com holdings pages (MFs file holdings quarterly via SEC N-PORT), update the four holdings arrays in CURATED_HOLDINGS (lib/etfHoldings.mjs:80-100), and bump each asOf. Current data is ~5 days old and accurate, so no action is warranted now.
- **Blockers:** Not due until ~Oct 2026 (curated data seeded 2026-07-01, within its 90-day TTL). Executing requires web-fetching current Amana holdings from public Saturna/stockanalysis (N-PORT) pages, then editing the CURATED_HOLDINGS literal — no owner action, API key, or migration needed, but it is a recurring maintenance edit that should only run when actually due.
- **Evidence:** lib/etfHoldings.mjs:80-100 — Amana MF rows (AMANX/AMAGX/AMDWX/AMAPX) all stamped asOf:"2026-07-01", hand-transcribed from stockanalysis.com (comment line 57). lib/handlers.mjs:34 CURATED_TTL_MS=90d; handlers.mjs:503-508 TTL keyed off cache row's fetched_at by provenance (mutual_fund→90d), NOT the source asOf; handlers.mjs:511-541 MFs skip Alpha Vantage and always re-read the hardcoded CURATED_HOLDINGS literal, so TTL expiry only re-stamps fetched_at — the actual numbers change only when a human edits the source. Only commits touching the file: 8e786bf + 13b5b43 (both 2026-07-05); nothing sinc…

### N22 — Optional: log the SnapTrade 402 response body in the symbol-search failure path

- **Status:** pending · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Add the response body to the warn payload at lib/handlers.mjs:1167, e.g. warn("snaptrade.symbol_search.failed", { status: r.status, ticker: t, body: typeof r.body === "string" ? r.body.slice(0, 500) : r.body }). Truncate/guard to avoid logging huge payloads; no secrets are in this body so no redaction needed.
- **Evidence:** lib/handlers.mjs:1167 — warn("snaptrade.symbol_search.failed", { status: r.status, ticker: t }) logs status+ticker only, not r.body. r.body (parsed SnapTrade error payload with broker code) is available in scope from snapReq's resolve({ status, body }) at lib/handlers.mjs:610-611. warn accepts structured data (lib/logger.mjs:64). Line last modified 2026-06-24 (commit 3299910); no later commit added body logging. Only symbol-search failure path in the file. Source: session file 2026-07-01-mkt-hours-gate-session.tmp:43 lists it under "What Has NOT Been Tried Yet" as optional.

### N23 — USPTO 'Mizan' trademark check: confirm joinmizan filing status + clearance search before any TM spend

- **Status:** pending · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Run two USPTO searches at tmsearch.uspto.gov (public, free): (1) owner/entity search for "Mizan Markets LLC" / applicants Yusuf Samad & Eesa Samad to confirm whether joinmizan filed any 'Mizan' mark and its status (live/pending/dead, class); (2) a clearance search for the 'Mizan' wordmark in the relevant fintech classes (IC 036 financial services / IC 042 SaaS) to gauge how crowded/registrable it is. Write the outcome into joinmizan-competitor.md and MIZAN-STATE-AUDIT.md. The actual decision to file/spend on a trademark stays with the owner (out of scope for this check).
- **Evidence:** Owner requested it 2026-07-05 (session-data/2026-07-05-mizan-landing-session.tmp:16 "yes, check whether they've filed a trademark...") but chose SEO #1&#2 and said "let's stop here"; both sub-actions still marked undone: memory/joinmizan-competitor.md:20 "Recommended (not yet done): a cheap USPTO clearance search at tmsearch.uspto.gov before any trademark spend"; MIZAN-STATE-AUDIT.md:8 "a cheap USPTO clearance search advised before any TM spend". No USPTO lookup output exists anywhere; the "wordmark likely unregistrable" note is a general web observation, not a TESS search. git log over 14 da…

### N24 — Wire the ethical/BDS overlay into the trading-bot buy universe

- **Status:** pending · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Import ethicalScreen from lib/sharia.mjs into lib/handlers.mjs (screenSymbol already imported at :20). In the bot-signals candidate loop (lib/handlers.mjs:6013-6019) skip candidates where ethicalScreen(cand).excluded is true, or add an ethical branch to the halal gate at :6042. Gate on the owner opt-in by reading the existing mizan_ethical_overlay preference from the user_state KV table server-side (sbAdmin already in scope); default off preserves current behavior. Add a bot.signal.bds_blocked warn/audit log mirroring the sharia_blocked pattern at :6044-6045. No schema or env changes. Bogleheads basket path unaffected (diversified ETFs, no individual BDS names).
- **Evidence:** lib/handlers.mjs has 0 ethical/BDS references (grep -ci = 0). Bot entry engine lib/handlers.mjs:5972-6047 picks candidates by momentum (:6013-6019) and gates ONLY on the Sharia 'haram' verdict via screenSymbol (:6032-6047, block at :6042); it never calls ethicalScreen or reads verdict.ethical. Overlay engine is shipped display-only: lib/sharia.mjs:194 ETHICAL_EXCLUSIONS, :205 export ethicalScreen, :216/:233 ethical flag on every verdict; frontend src/components/MizanApp.jsx:2368 Screener toggle (pref mizan_ethical_overlay), :10338-10343 bds_ in mapPosition. git log --all -S 'ethicalScreen' --…

### N25 — Order Ticket: add a stop-price field for Stop / Stop-Limit order types

- **Status:** pending · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** Frontend (MizanApp.jsx TradeBot): add a stopPx state + STOP PRICE input rendered for stop/stoplimit types; add Stop-Loss and Stop-Limit to OTYPE_BY_NAME so the cards become selectable; send stopPrice in both request bodies (Alpaca `/api/alpaca/order` and SnapTrade `/api/snaptrade/trade/impact` — the latter already accepts it); adjust LIMIT PRICE visibility (plain stop = stop price only, stop-limit = both) and the Estimated Total basis per type. Backend (handlers.mjs): thread stopPrice through the /api/alpaca/order route destructure (line 5481) into placeAlpacaOrder and set orderBody.stop_price with validation (required for stop/stop_limit). Side finding worth fixing in docs: MIZAN-STATE-AUDIT.md lines 214/285/464 still claim the Order Ticket is `{false && ...}` double-gated — stale; it now renders for isAdmin users (MizanApp.jsx:6487-6494).
- **Evidence:** src/components/MizanApp.jsx:6461-6464 — `OTYPE_BY_NAME={Market:"market",Limit:"limit"}` with comment "Stop variants would need a stop-price field, so they stay display-only for now"; MizanApp.jsx:6327 — only `lpx` (limit price) state, no stop-price state or input exists (LIMIT PRICE input at 6555-6559 is the only price field); MizanApp.jsx:6400-6401 (Alpaca body sends limitPrice only) and 6419-6429 (SnapTrade body sends `price` only, omits stopPrice) — stop-type mappings exist but are unreachable and would send no stop price. Backend: lib/handlers.mjs:2055+2084 — SnapTrade impact route ALREAD…

### N26 — Compliance-tier gate for every new grounded-AI feature (owner-directed 2026-07-15)

- **Status:** pending (standing gate) · **Effort:** S (per-feature review overhead) · **User value:** high · **Autonomous:** yes
- **Remaining:** REQUIRED review step, not optional, before shipping ANY new grounded-AI feature — the pending set is N3 (Zakat/purification guidance), N2 (proactive portfolio insight card), N9 (AI weekly digest), N6 (per-token crypto screening), and N20 (security-event explainer). For each: (1) classify the feature's output into the three compliance tiers — IMPERSONAL / ACCOUNT_SERVICING / PROHIBITED (`lib/compliance/policy.mjs`); (2) route generation through the hardened advisor prompt (`lib/compliance/advisor-prompt.mjs`) plus the post-generation `lib/compliance/advisor-filter.mjs`; (3) verify it never emits a personalized buy/sell/hold recommendation or suitability judgment (the read-only line is drawn at personalization of advice). Do not ship an AI surface that skips this classification + filter pass.
- **Evidence:** In-code compliance boundary shipped + deployed 2026-07-15: `lib/compliance/policy.mjs` (three data tiers IMPERSONAL / ACCOUNT_SERVICING / PROHIBITED), `lib/compliance/advisor-prompt.mjs` (hardened advisor prompt), `lib/compliance/advisor-filter.mjs` (post-generation filter). Read-only line drawn at personalization of advice. Refs: `docs/COMPLIANCE.md`, memory `compliance-boundary-and-price-chart`.

---

## M — Needs a DB migration (PARKED, owner approval)

Each introduces a Supabase migration → explicit owner approval required (CLAUDE.md §8).

### M1 — Shareable snapshot links

- **Status:** pending · **Effort:** M · **User value:** medium · **Autonomous:** no · **Needs migration:** yes
- **Remaining:** Everything — feature not started. Concretely: (1) authenticated POST route in lib/handlers.mjs to mint a share token and capture an anonymized snapshot (allocation %, compliance breakdown, optionally masked absolute values); (2) storage: either a new shared_snapshots table (token PK, snapshot JSONB, expiry, revoked flag — requires a new migration and owner approval per CLAUDE.md §8) or a migration-free stateless encrypted-token design via existing lib/crypto.mjs (no revocation, long URLs — owner trade-off decision); (3) unauthenticated rate-limited GET route serving the snapshot by token; (4) read-only public view component, routed by extending the publicLegalRoute() pattern in src/App.jsx:99; (5) share UI inside the app (generate/copy/revoke link) using T.* tokens, mask(), BentoTile per design system.
- **Blockers:** Canonical design (revocable/expiring links) needs a new shared_snapshots Supabase table — schema changes require explicit owner approval per CLAUDE.md §8. A migration-free stateless-encrypted-token variant is possible with existing lib/crypto.mjs, but choosing it (no revocation, snapshot frozen in the URL) is a product decision the owner should make. Also an anonymization-policy decision: whether shared views show absolute dollar values or percentages only.
- **Evidence:** git log --all --oneline grep for share/snapshot-link/public/read-only: no matching commits (only unrelated hits, e.g. e27ce91 docs). Zero code hits: no share_token/shareToken/public-view route in lib/handlers.mjs (grep across 6,100 lines; all "share" hits are buying-power/stock-share comments, e.g. handlers.mjs:4026,4285); MizanApp.jsx hits are unrelated (line 11385 iOS "Tap Share" PWA hint, line 4151 clipboard trade copy, line 7686 demo-mode "useful for screenshots, sharing"). No migration among supabase/migrations/001-024 creates a share/snapshot-token table. Only public client route is pub…

### M2 — Multi-aggregator fallback for liabilities (MX/Finicity behind Plaid) (Track 1 #8)

- **Status:** pending · **Effort:** L · **User value:** low · **Autonomous:** no · **Needs migration:** yes
- **Remaining:** Everything: (1) owner signs up for MX and/or Finicity (partner-agreement onboarding, not self-serve) and provisions API credentials as new env vars; (2) new Supabase migration for encrypted provider tokens + accounts (mirror 003_plaid.sql + 016/017 AES-256-GCM pattern); (3) server routes in lib/handlers.mjs for link/token-exchange/accounts/liabilities sync per provider, following requireAuth → checkRateLimit → audit-log pattern; (4) a provider seam (like lib/sharia.mjs) that dispatches liabilities fetch to Plaid first, MX/Finicity fallback, plus user-selectable provider in the connect flow; (5) frontend wiring so Goals.jsx liabilityAccounts and Finances accounts consume the unified feed; (6) webhook/cron sync for the new providers.
- **Blockers:** Owner must contract with and provision MX/Finicity API credentials (both require partner onboarding, not instant self-serve keys); new env vars in Vercel; new Supabase migration requires explicit owner approval per CLAUDE.md section 8.
- **Evidence:** MIZAN-BENCHMARK-ROADMAP.md:27 marks Track 1 #8 as Pending. Repo-wide grep for finicity\|mx\|mastercard\|akoya\|yodlee across src/, lib/, api/, supabase/, package.json: zero real hits (only noise: min/max vars at src/components/MizanApp.jsx:836-837, ticker NVSN.MX at lib/etfHoldings.mjs:113). git log --all -i --grep for finicity/aggregator: no matching commits. supabase/migrations/ 001-024 contain only Plaid tables (003_plaid.sql, 010_plaid_transactions.sql). src/components/Goals.jsx:1975 derives liabilityAccounts solely from plaidAccounts — no provider seam or fallback dispatch. No FINICITY_*/MX_…

### M3 — Anonymous peer comparison

- **Status:** pending · **Effort:** L · **User value:** low · **Autonomous:** no · **Needs migration:** yes
- **Remaining:** Entire feature is unstarted. Needs: (1) opt-in consent flag for cross-user data sharing (new column in profiles/user_state); (2) an anonymized aggregation layer — service-role aggregate query views or an RPC computing cohort stats (e.g. net-worth/savings-rate percentiles) over consenting users' user_state/balances, with a minimum-cohort-size guard to prevent de-anonymization; (3) a backend route (handlers.mjs: requireAuth → checkRateLimit → aggregate query → response) returning the caller's percentile vs cohort; (4) a frontend surface (BentoTile in Overview or a new panel) rendering the comparison with T.* tokens, FU/FM fonts, formatters + mask(), loading/empty/error states, both themes.
- **Blockers:** Requires a new Supabase migration for anonymized aggregate/cohort views plus an opt-in consent column, and CLAUDE.md §8 mandates explicit owner approval before adding any migration. It is also a privacy-sensitive feature reading across other users' financial data, which warrants owner sign-off on the anonymization/consent design before implementation.
- **Evidence:** MIZAN-STATE-AUDIT.md:380-387 marks "Anonymous Peer Comparison" ❌ Not present (effort L); CLAUDE.md §16 lists it last in the feature-gaps table (effort L). Grep for peer\|cohort\|percentile\|anonymized\|leaderboard\|compare.*users across src/, lib/, api/, supabase/ = zero feature hits (only unrelated Terms/Privacy legal copy, crypto.mjs type checks, etfHoldings.mjs sukuk grouping, 018_security_events.sql counters, handlers.mjs bot-P&L comments). All 24 migrations reviewed (supabase/migrations/001-024) — none create aggregate views, cohort tables, or a data-sharing consent column. git log --since=20…

### M4 — Phase 4: materialized daily balance table + reverse calculator to backfill net-worth history

- **Status:** pending · **Effort:** L · **User value:** high · **Autonomous:** no · **Needs migration:** yes
- **Remaining:** Everything — item is not started. Required: (1) new Supabase migration for a per-user daily balance/net-worth table (owner approval required per CLAUDE.md §8); (2) a reverse calculator (Maybe's app/models/balance/reverse_calculator.rb pattern) that walks snapActivities backward from current balances to backfill a dense per-day curve, likely as a pure module (e.g. src/lib/balance.js) + server materialization in lib/handlers.mjs (extend nightly-snapshot cron or new route); (3) rewire the Overview hero chart's 1M/3M/YTD/1Y/All ranges from monthly deposit buckets to the daily curve and retire the mizan_intraday localStorage buffer + the 365-entry networth_history KV array; (4) feed PerformancePanel's riskMetrics the dense curve so max drawdown/volatility/Sharpe no longer wait on >=20 slowly-accruing nightly snapshots; (5) update CLAUDE.md Known Limitation #1 and MIZAN-STATE-AUDIT.md.
- **Blockers:** Schema change: a materialized daily balance table needs a new Supabase migration, and CLAUDE.md §8 forbids adding migrations without an explicit owner ask. No external/API blockers otherwise — snapActivities data and the nightly cron pipeline already exist.
- **Evidence:** No implementing code: grep for reverse-calc/daily_balance/materialized across src+lib+supabase returns only unrelated backfill hits (CSV import MizanApp.jsx:7057, Plaid cursor handlers.mjs:412). Migrations end at supabase/migrations/024_etf_holdings_cache.sql — no balance table. The structures this item replaces are still live: mizan_intraday localStorage buffer (src/components/MizanApp.jsx:1450, 1649), mizan_networth_history localStorage+user_state KV array (MizanApp.jsx:10264-10271), nightly cron appending to user_state key networth_history capped at 365 entries (lib/handlers.mjs:4852-4905)…

---

## P — Parked / not-autonomous

Feature-flavored but an agent cannot finish them solo (blocked on API keys or external setup).

### P1 — Wire the free Alpaca Market Data API (extended-hours prices) + swap MARKET_HOLIDAYS for /v2/clock

- **Status:** partial · **Effort:** M · **User value:** medium · **Autonomous:** no
- **Remaining:** 1) Owner must provision ALPACA_KEY_ID/ALPACA_SECRET in Vercel prod — without them the existing Alpaca quote path returns [] and nothing activates. 2) (optional, small) reorder /api/finnhub/quote to try Alpaca before Finnhub if 'preferring Alpaca' is still desired. 3) Add explicit extended-hours (4am-8pm ET) handling + surface pre/post-market price+change in the UI (currently only a static feature label exists). 4) Swap the hardcoded MARKET_HOLIDAYS set (lib/handlers.mjs:1186-1190) for a call to Alpaca /v2/clock for auto holiday + half-day early-close handling (the 'clean fix' — half-day closes currently unmodeled). Steps 2-4 are agent-codeable but cannot be validated or activated until the keys land.
- **Blockers:** ALPACA_KEY_ID / ALPACA_SECRET are unset in Vercel production (confirmed by memory alpaca-data-pending.md and MIZAN-STATE-AUDIT.md line 130). Owner must provision the free Alpaca data keys; until then fetchAlpacaQuotes returns [] and any /v2/clock work is untestable. This is an owner env-var action a coding agent cannot perform.
- **Evidence:** SHIPPED: fetchAlpacaQuotes() lib/handlers.mjs:641-669 calls the real Alpaca Market Data API (data.alpaca.markets/v2/stocks/snapshots?feed=iex), wired into /api/finnhub/quote at lib/handlers.mjs:1697-1718 (commit 453e002, 2026-06-26 "feat(prices): Alpaca + Yahoo fallback"). Keys read at lib/handlers.mjs:24-25; returns [] when unset (:642). NOT DONE: (1) /v2/clock swap — MARKET_HOLIDAYS still hardcoded lib/handlers.mjs:1186-1190; comment :1181-1185 says replace with /v2/clock once keyed; grep confirms zero /v2/clock calls anywhere. (2) Ordering is Finnhub-primary→Alpaca-fallback, NOT the specif…

### P2 — Browser extension (Sharia checker)

- **Status:** pending · **Effort:** L · **User value:** low · **Autonomous:** no
- **Remaining:** Everything — nothing started. Build: (1) MV3 extension scaffold (manifest.json, background service worker, content script for ticker detection on brokerage/finance pages, popup UI); (2) auth story for the extension to call the auth-gated /api/screen (Supabase session reuse from the extension, or a deliberately public rate-limited screening endpoint — product decision); (3) CORS/CSP allowances for the extension origin on the API; (4) build/packaging pipeline separate from the Vite SPA; (5) owner: Chrome Web Store developer account + review submission for distribution. Minor doc fix: audit says reusable screener logic is 'in MizanApp.jsx' — it now lives in lib/sharia.mjs via /api/screen.
- **Blockers:** Not blocked today (simply unstarted), but end-to-end completion needs owner actions: Chrome Web Store developer account + store review for distribution, and a product decision on whether screening verdicts are exposed unauthenticated to extension users vs. requiring Mizan login (current /api/screen is auth-gated per CLAUDE.md handler pattern).
- **Evidence:** git ls-files \| grep -iE 'extension\|manifest' → only public/manifest.webmanifest (PWA, not an extension manifest); repo-wide grep for chrome.runtime/content_scripts/browser_action/manifest_version → 0 hits; git log --all --grep=extension → only 2452d87 (Plaid Permissions-Policy, unrelated); no extension files on scheduler-pending or any worktree branch. Audit claim at MIZAN-STATE-AUDIT.md:358-366 ('❌ Not present — no extension manifest, no content script, nothing') matches code. Reusable seam confirmed: lib/sharia.mjs (screening service) + /api/screen route at lib/handlers.mjs:1650-1655 (GET ?…

---

## O — Blocked on owner (checklist)

Re-auths, live trades, API-key provisioning, decisions. **O18 (rotate CRON_SECRET) is live-secret hygiene worth doing soon.**

### O1 — Landing SEO item #3: halal-investing content pages — plan/discuss with owner before build

- **Status:** blocked-on-owner · **Effort:** M · **User value:** medium · **Autonomous:** no
- **Remaining:** Everything: (1) recover/define the #3 scope (which halal-investing queries to target, page list, e.g. what-is-halal-investing / zakat-calculator-guide / halal-ETF-comparisons, URL structure, internal linking, how pages fit the single-page static landing site), (2) draft the plan and hold the discussion with the owner — he explicitly required this before any build, (3) after approval: write the pages, add them to sitemap.xml, cross-link from index.html, deploy via git push (auto-deploys), then owner submits updated sitemap in Search Console. An agent may autonomously DRAFT the plan/proposal, but cannot proceed to build without the owner conversation.
- **Blockers:** Owner explicitly gated build on a plan/discuss session ("We need to plan and discuss #3 before build") and that discussion has never happened — no scope doc exists anywhere. Secondary: the work lives in the separate mizan-landing repo, which has moved from ~/Documents/mizan-landing to "/Users/amaankhan/Desktop/AI Projects/mizan-landing" since the session notes were written.
- **Evidence:** Owner deferral verbatim: /Users/amaankhan/.claude/session-data/2026-07-05-mizan-landing-session.tmp:19 ("yes, do 1 & 2. We need to plan and discuss #3 before build") + :21 ("let's stop here"). Audit claim matches code: /Users/amaankhan/Documents/mizan-app/MIZAN-STATE-AUDIT.md:8 ("#3 (halal-investing content pages for ranking) was scoped but parked — plan/discuss before build"). Items 1&2 DID ship in the mizan-landing repo (commits cae8b2c, bd01886, a6742e5 — SEO head edits, canonical/og host fix, robots.txt+sitemap.xml). Item #3 has zero code: landing repo (NOW at "/Users/amaankhan/Desktop/AI…

### O2 — khanstyle02 + mmfarooki: bot consent disclosures not yet accepted

- **Status:** blocked-external · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Only ONE of the two users is still outstanding. khanstyle02 (owner's own secondary account) already accepted consent on 2026-06-29 17:40 UTC — that half is done, the docs under-claimed it. The remaining piece is mmfarooki@outlook.com (external beta user fe9f7326-9c4d-4161-b985-82eec50dda7e): he must log into the running app and click the "I understand — enable Trade for my account" consent button (POST /api/bot/consent). This is a human action by an external person; a coding agent cannot and must not set the timestamp via SQL because the disclosure is a compliance/legal consent that has to be genuinely accepted by the user. No code change is possible or appropriate here.
- **Blockers:** Requires the external beta user mmfarooki@outlook.com to personally log in and accept the first-use consent disclosure in-app. The consent must be user-accepted (Mizan-is-not-an-RIA compliance disclosure), so it cannot be completed by code or by the owner on his behalf. Migration 022 already applied; no schema/env work needed. Note: khanstyle02's half is already complete (accepted 2026-06-29), so the item as written is half-obsolete.
- **Evidence:** Consent feature fully shipped: migration supabase/migrations/022_trading_bot_beta.sql:19 (trading_bot_consent_at col, applied); POST /api/bot/consent at lib/handlers.mjs:3850-3859; botConsentOk() gate at lib/handlers.mjs:1060-1065; enforced at order-place lib/handlers.mjs:2209, strategy-create :3995, signal-approve :4131; frontend consent card + accept button at src/components/MizanApp.jsx:5442-5449 and :5791-5803; commit a3d3b69. LIVE DB (Supabase project kcghivcvczxaguezurii, profiles): khanstyle02@gmail.com trading_bot_consent_at=2026-06-29 17:40:01+00 (ALREADY ACCEPTED — item premise stal…

### O3 — Decide fate of the tiny momentum sleeve (strategy 43c1b866)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner must decide, on/after July 9 (today is 2026-07-06, before the gate), whether to (a) enable 43c1b866 in mode:semi as fun/learning capital, (b) leave it dormant (enabled:false, current state), or (c) DELETE the row for pure DCA/hold. Whichever, the follow-up is a one-line DB write on the existing bot_strategies table (flip enabled, no-op, or delete row) — no code, no schema change. The universe/deploy_pct/mode engineering is already complete.
- **Blockers:** Requires an owner live-trading decision on a funded E*Trade account (302ebba3) — arm, dormant, or delete a real autonomous-order strategy. Explicitly optional and date-gated to July 9, which has not arrived (current date 2026-07-06). An agent cannot make a live-money risk-appetite call.
- **Evidence:** Strategy 43c1b866 is a Supabase bot_strategies row (migration supabase/migrations/020_trading_bot.sql), not code — grep for "43c1b866" across *.jsx/*.mjs/*.js returns 0 matches. The halal rebuild + demotion is already done per MIZAN-STATE-AUDIT.md:8 item(5) ("REBUILT halal + demoted to deploy_pct:25, enabled=false") and memory/trading-july9-plan.md:15. The only remaining piece is the owner's decision, per trading-july9-plan.md:21: "(Optional) enable the tiny momentum sleeve 43c1b866 in mode:semi only if owner still wants it; otherwise leave dormant / delete." No commit executes it (git log --…

### O4 — Owner: provision Alpaca Market Data API keys

- **Status:** blocked-on-owner · **Effort:** S · **User value:** medium · **Autonomous:** no
- **Remaining:** Owner must provision a free Alpaca Market Data account and set ALPACA_KEY_ID + ALPACA_SECRET in Vercel Production (`vercel env add`). That single owner action IS the entire item — the code that activates on those keys is already deployed and no-ops harmlessly until then. Separately gated follow-on coding work (not part of this item): surface pre/post-market price+change in the UI, and swap the hardcoded MARKET_HOLIDAYS set for Alpaca /v2/clock — both only become possible once the keys exist.
- **Blockers:** Requires the owner to mint Alpaca API keys and set two Vercel secrets. A coding agent cannot create external API keys or write env vars. Note: the linked memory (alpaca-data-pending.md) under-claims the code side as "not started" — the quote-data wiring is in fact already shipped; only the owner's key provisioning remains.
- **Evidence:** Keys confirmed UNSET: `vercel env ls` (ran, exit 0) lists all 22 project env vars — no ALPACA_KEY_ID / ALPACA_SECRET present. Code that consumes them is already shipped: lib/handlers.mjs:24-25 (env read), :641-669 (fetchAlpacaQuotes → data.alpaca.markets/v2/stocks/snapshots?feed=iex, extended-hours capable), :1697-1718 (wired into /api/finnhub/quote as Finnhub→Alpaca→Yahoo fallback, commit 453e002 + cd126eb). Graceful no-op when unset: :642, :1105. /v2/clock swap NOT done — still hardcoded MARKET_HOLIDAYS at :1186-1190 with an inline TODO to swap once keys exist.

### O5 — Verify Order Ticket live-quote chip under real market-hours load

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** 1) Owner opens Trade → Order Ticket in prod during 09:30–16:00 ET while background portfolio/watchlist polling is active, types a symbol, and confirms the chip shows price + LIVE (not 'No live price'). 2) ONLY IF it fails: implement the contingent fix — a short (~30-60s) per-symbol server-side quote cache in the /api/finnhub/quote handler (lib/handlers.mjs:1679-1718) or a dedicated quote source for intent=order. 3) Optional de-risk (owner): set ALPACA_KEY_ID/ALPACA_SECRET in Vercel so the dormant Alpaca fallback tier actually serves when Finnhub throttles. 4) Housekeeping: MIZAN-STATE-AUDIT.md:214/285/464 + CLAUDE.md still describe the retired {false&&}+ComingSoon double-gate; the real gate is now isAdmin (MizanApp.jsx:6474).
- **Blockers:** Requires the owner's authed prod session (Order Ticket renders only for isAdmin/trading-beta accounts; forcing isAdmin locally is explicitly forbidden to commit) AND a live-market window (09:30–16:00 ET; market closed at time of audit). The contingent code fix is gated on the owner actually observing 'No live price' in production. Alpaca fallback tier additionally needs owner to provision ALPACA_KEY_ID/SECRET env vars in Vercel.
- **Evidence:** Shipped enablers: commit a18453a (lib/rateLimit.mjs:48 'marketdata.ticket':240; :85-86 matchAction routes /api/finnhub/quote + intent=order to it; 5 tests src/test/rateLimit.test.js:5-31) and commit 02db1c0 (src/components/MizanApp.jsx:6361-6379 debounced quote effect, :6370 appends &intent=order, chip at :6537-6546 with 'No live price' fallback at :6546). Both on origin/main. Live prod curl 2026-07-06 20:42 ET: /api/finnhub/quote?symbols=AAPL&intent=order returned AAPL $312.66 src=Finnhub — deploy is live and honors the marker. The verification itself has NOT occurred: no market-hours observ…

### O6 — Submit sitemap in Google Search Console + re-scrape social debuggers (landing SEO)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** medium · **Autonomous:** no
- **Remaining:** Only two manual owner actions remain, both in third-party web dashboards (no code artifact): (1) In Google Search Console, add/verify the mizan.exchange property and submit https://www.mizan.exchange/sitemap.xml. (2) Re-scrape the social-card debuggers (Facebook Sharing Debugger, X/Twitter Card Validator, LinkedIn Post Inspector) against www.mizan.exchange to bust cached OG data now that og-image.png returns 200 with no redirect.
- **Blockers:** Requires the owner's own Google account and domain-ownership verification to submit in Search Console, and manual use of Facebook/X/LinkedIn debugger dashboards to re-scrape. Neither is automatable by a coding agent nor produces a committable artifact. The code/deploy prerequisites are already live.
- **Evidence:** MIZAN-STATE-AUDIT.md:8 states "All verified live. PENDING (owner clicks): submit sitemap in Search Console + re-scrape social debuggers." Code groundwork fully shipped in the separate mizan-landing repo (/Users/amaankhan/Desktop/AI Projects/mizan-landing): commits cae8b2c (title/meta+JSON-LD), bd01886 (canonical/og:url to www), a6742e5 (robots.txt+sitemap.xml, og:image to www); working tree clean. index.html:6-34 has rewritten title, canonical, og:url, og:image, and SoftwareApplication JSON-LD with alternateName "Mizan". Live checks: sitemap.xml=HTTP 200, robots.txt=HTTP 200, og-image.png=HTT…

### O7 — Verify the E*Trade money transfer landed in khanstyle02's account (ecfea3bb)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner-side: run a live SnapTrade read on account ecfea3bb (needs the owner's SnapTrade client creds + APP_ENCRYPTION_KEY to decrypt khanstyle02's userSecret) to confirm cash + buying_power cleared, and confirm it's Individual (not custodial — buying_power==cash, no UTMA title). Only then, as an explicit live-trading decision (parked to ~2026-07-08 per the 2026-07-01 resumption checklist): repoint bot_strategies 0fd266ae.account_id → ecfea3bb, set account_full_auto true, re-enable, and run an in-hours workflow_dispatch fill test to verify status=executed. No code change produces the answer.
- **Blockers:** External E*Trade settlement window (1-3 biz days from Fri 6/26) has already elapsed, so funds almost certainly landed but were never verified — verification requires the owner's live SnapTrade credentials/env (read-only balance check, no committable artifact). Using ecfea3bb as the DCA target for 0fd266ae is a live-trading decision the owner explicitly parked to ~2026-07-08 pending a separate custodian→bank→Individual fund move and confirmation the account is non-custodial.
- **Evidence:** Operational check, not code — no related commit in `git log --since="12 days ago"`. Last recorded state: `ecfea3bb` "khanstyle02's Individual, was settling 6/30 ($100 cash, $0 BP)" (memory bot-snaptrade-impact-401.md; 2026-07-01-mkt-hours-gate-session.tmp:42,100). Owner's ask 2026-06-29-mizan-app-session.tmp:17 "can you see if E*Trade Money hit Khanstyle02". The verify scripts etrade-check.mjs/etrade-detail.mjs in /private/tmp scratchpad are deleted (dir empty). No session (7/2,7/3,7/5,7/6) or memory re-confirms the landing; grep of all session-data for "ecfea3bb"/"money hit"/"landed" shows n…

### O8 — Repoint + re-enable DCA strategy 0fd266ae once funds land (~2026-07-08)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Pure operational/owner steps, no code: (1) owner confirms funds landed in an Individual (non-custodial) E*Trade account and identifies its account_id; (2) UPDATE bot_strategies SET account_id='<individual-id>', enabled=true WHERE id='0fd266ae-...' (live prod data write); (3) UPDATE/INSERT account_full_auto enabled=true for user b1403200 + that account; (4) clear the day's pending_signals guard and gh workflow run cron-bot-signals.yml during 09:30-16:00 ET — this fires a REAL-MONEY order; (5) verify pending_signals status=executed vs error_msg. Blocked until funds land (~2026-07-08; today 2026-07-06) and requires the owner to confirm the account title and authorize a live trade. Superseded/extended by the July-9 plan (sell SPTE, retire e528f2d7, create DCA core).
- **Blockers:** Funds still moving custodian→bank→Individual E*Trade (~2026-07-08, not yet landed as of 2026-07-06). Owner must (a) confirm the target account is Individual not custodial/UTMA (E*TRADE bars API trading on custodial — deeper code-1140 wall reconnect can't fix), (b) pick/top-up an account (302ebba3 ~$2 free, ecfea3bb settling), (c) authorize a live real-money verification trade. All remaining steps are live-prod DB writes + a live trade — not agent-safe.
- **Evidence:** DCA/Bogleheads machinery fully shipped: lib/handlers.mjs:5771-5862 (dca cron branch, params.basket :5796, params.dca_amount :5784, live/semi exec :5841), lib/handlers.mjs:110-137 + 3955+ (buildBogleheadsPreset + /api/bot/bogleheads, commit 88c559f 2026-07-03); migrations 023_bot_strategy_type_dca.sql + 021_full_auto_per_account.sql present/applied. Strategy 0fd266ae + accounts 366265e9/302ebba3/ecfea3bb/b1403200 are LIVE DB rows — grep across *.mjs/*.jsx/*.sql finds them ONLY in MIZAN-STATE-AUDIT.md, never in code. No commit since 88c559f repoints it; HEAD=a18453a. State-audit 2026-07-01/07-0…

### O9 — Revisit the full-auto allowlist boundary before any compensated/commercial arrangement

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Nothing actionable now — the deferral IS the completed decision, and the guardrail (SQL-only allowlist + COMPLIANCE comments at all three write paths) is already in place. The 'revisit' is a future-triggered owner compliance/legal call: if/when Mizan enters any compensated or commercial arrangement, the owner must make an RIA-registration determination and either keep the allowlist restricted to accounts the owner is personally authorized to trade, or restructure. That trigger has not occurred; Mizan is not an RIA and has no compensated relationship. A coding agent cannot make this legal determination.
- **Blockers:** Requires an owner-only compliance/legal decision (RIA registration boundary), explicitly gated on a future commercial/compensated arrangement that has not occurred. Not a code task; no autonomous work possible.
- **Evidence:** Decision made + documented in-code, untouched since commit 05e6de0 (2026-07-01): lib/handlers.mjs:1067-1080 (canUseFullAuto gates on owner-controlled full_auto_enabled, SQL-only; rewritten COMPLIANCE comment says 'revisit before ANY compensated / commercial arrangement. Owner-reviewed 2026-07-01'). Enforced at lib/handlers.mjs:4010-4016, 4057-4061, 4221-4222; cron read at 5741-5744. `git log -S revisit -- lib/handlers.mjs` returns only 05e6de0 (no later change). Owner-decision record: memory/trading-bot-beta-access.md:19-20. Column: supabase/migrations/020_trading_bot.sql:51.

### O10 — Owner: complete the custodian→bank→Individual E*Trade transfer and pick/fund the DCA target account

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** All remaining steps are owner/real-world actions, none a code change: (1) complete the custodian->bank->Individual E*Trade money transfer (resume was scheduled ~2026-07-08, 2 days out); (2) confirm the destination is a non-custodial Individual account (E*TRADE title in owner's name / buying_power==cash in the Trade panel); (3) top-up 302ebba3 (~$2 free after the breakout spent ~$97.62/$100 on 2 SPTE) OR select another funded Individual account; (4) repoint bot_strategies 0fd266ae.account_id + set enabled=true + account_full_auto.enabled=true (a live-trading DB change arming REAL orders, explicitly owner-gated to July 9); (5) verify a live fill (status=executed) in market hours. The trading-bot code, market-hours gate, and DCA strategy_type are all built and deployed — nothing is left to implement.
- **Blockers:** Requires the owner to (a) move real money via a multi-day custodian->bank->Individual E*Trade transfer, (b) visually confirm the destination account type (Individual vs custodial — owner-only), and (c) make a live-trading decision to arm full-auto real orders (owner said "arm at July 9, not before"; agent instincts forbid firing/arming the bot without explicit go-ahead). A coding agent cannot move money, cannot verify the brokerage account title, and must not arm live trades. No code, env var, or migration is pending.
- **Evidence:** Session file /Users/amaankhan/.claude/session-data/2026-07-01-mkt-hours-gate-session.tmp ("BLOCKER (external, ~1 week): funds not yet in a tradeable Individual account... Resume ~2026-07-08"). Memory trading-july9-plan.md (updated 2026-07-05) still lists the July-9 checklist as pending ("arm this at July 9, not before"). Memory bot-snaptrade-impact-401.md: DCA strategy 0fd266ae DISABLED (enabled=false, 2026-07-01 20:19) and still pointed at custodial 366265e9; account_full_auto reverted to false. Code side already shipped: buildBogleheadsPreset lib/handlers.mjs:114 (strategy_type "dca"), usMa…

### O11 — July 9: sell 2 SPTE at break-even and retire breakout strategy e528f2d7

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner-only, on/after 2026-07-09: (1) confirm ~$100 landed and account 302ebba3 (user b1403200) is trade-enabled (buying_power==cash); (2) confirm live SPTE price near break-even; (3) place a REAL sell order for the 2 SPTE shares held @ ~$48.81 to recover ~$97; (4) set breakout strategy e528f2d7 enabled=false in bot_strategies to retire it; (5) verify read-only via net._http_response / pending_signals — never manually fire /api/cron/bot-signals (places real orders). No code change, migration, or env var involved.
- **Blockers:** Requires the owner to (a) act on/after the explicit 2026-07-09 date gate (today is 2026-07-06), (b) confirm funding landed + the brokerage account is trade-enabled, and (c) place a real live sell order of 2 SPTE shares on a funded E*Trade account. Live money, a live-trading decision, and an owner-only DB flag flip — an agent cannot execute any of it. This is owner personal-account trading ops, not a feature delivering value to real Mizan users.
- **Evidence:** Today is 2026-07-06 (via `date`); action is gated to 2026-07-09. trading-july9-plan.md JULY-9 CHECKLIST: "Sell 2 SPTE at break-even on e528f2d7 (recover ~$97), then enabled=false to retire it. Owner said arm this at July 9, not before; confirm live SPTE price first" (preconditions: ~$100 lands + 302ebba3 confirmed trade-enabled, buying_power==cash). MIZAN-STATE-AUDIT.md:8 explicitly lists "Also pending: July-9 trading activation ([[trading-july9-plan]])". bot-execution-model.md confirms e528f2d7 breakout still LIVE holding 2 SPTE @ ~$48.81 on E*Trade 302ebba3 (first fill 2026-06-30). git log…

### O12 — CSP nonce hardening (deferred from the 2026-07-05 security audit)

- **Status:** blocked-on-owner · **Effort:** M · **User value:** low · **Autonomous:** no
- **Remaining:** Everything: not started. To ship, need (1) owner go-ahead per CLAUDE.md §8 (CSP edits are gated); (2) a per-request nonce mechanism — the app is a static Vite SPA with no server rendering index.html, so this requires new infra (Vercel Edge Middleware, or a serverless route that templates index.html) to generate a fresh nonce, inject nonce= into index.html's inline SW <script> and inline <style>, and emit a dynamic CSP header with 'nonce-XXX' instead of the static vercel.json header; (3) careful handling of third-party inline injection — Plaid Link injects its own inline scripts/styles (the TradingView widget was **removed 2026-07-15** with the market-sector heatmap, so it's no longer a factor — `s3.tradingview.com` was dropped from the CSP `script-src` the same day, owner-directed), and per CSP3 a nonce makes browsers ignore 'unsafe-inline', so Plaid's injection must be verified not to break (the documented "needs care" reason for deferral); (4) same treatment for style-src (THEME_CSS + inline style block + Plaid inline styles).
- **Blockers:** CLAUDE.md §8 forbids changing vercel.json CSP headers without explicit owner instruction; item text requires owner go-ahead. Static-SPA architecture also needs new per-request infra (Edge Middleware) that changes hosting model — an architectural decision the owner should approve. Risk of breaking Plaid Link inline injection when a nonce disables 'unsafe-inline' (TradingView removed 2026-07-15 — no longer a factor).
- **Evidence:** vercel.json CSP `script-src` is now `'self' 'unsafe-inline' https://cdn.plaid.com` (only Plaid remains; still no nonce token). ✅ DONE (2026-07-15): all TradingView origins were removed from the vercel.json CSP (owner-directed) — `https://s3.tradingview.com` from `script-src`, `https://*.tradingview.com` + `wss://*.tradingview.com` from `connect-src`, and `https://*.tradingview.com` from `frame-src` — after the S&P-500 market-sector heatmap (their only consumer) was removed. Plaid Link is now the sole remaining third-party inline-injection concern for the nonce work. `grep -rn nonce` over vercel.json/api/lib/index.html = 0 matches (only unrelated AES `_nonce` DB columns exist). No middleware.* file — static Vite build served from dist + catch-all rewrite to /index.html, so no per-request HTML template. index.html has an inline SW-registration <script> + inline <style> depending on 'unsafe-inline'. Deferred explicitly in commit 1c34aed body; documented pending in MIZAN-STATE-AUDIT.md:266-267 and ai-roadmap.md:21; CLAUDE.md §8 for…

### O13 — APP_ENCRYPTION_KEY missing from local .env.local (blocks local SnapTrade debugging)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner must decide and act: pull the production ENCRYPTION_KEY (32-byte hex, sensitive in Vercel env) and add a line `ENCRYPTION_KEY=<hex>` to /Users/amaankhan/Documents/mizan-app/.env.local — accepting the security tradeoff of a prod secret on the local machine — OR deliberately keep the remote-only (live app / DB / Vercel logs) SnapTrade debugging workflow. No source code change is required; the code already reads ENCRYPTION_KEY correctly. (Separately, docs mis-name it APP_ENCRYPTION_KEY in CLAUDE.md and MIZAN-STATE-AUDIT.md:227,236 with wrong fn/column names — a doc-only cleanup an agent could do.)
- **Blockers:** Requires the owner to provide the production ENCRYPTION_KEY secret value (an agent has no access to it) and to accept the security tradeoff of placing a prod secret on the local machine — or to consciously keep remote-only debugging. This is inherently an owner decision + owner action, not a codeable task.
- **Evidence:** Key genuinely absent from .env.local (modified today Jul 6 12:47; var list has SnapTrade/Supabase/Alpaca/etc. but no ENCRYPTION_KEY). Real var is ENCRYPTION_KEY not APP_ENCRYPTION_KEY: lib/crypto.mjs:20 (process.env.ENCRYPTION_KEY, throws if unset), lib/handlers.mjs:216 (ENC_ENABLED). Decrypt path proving the blocker: lib/handlers.mjs:702 extractUserSecret() -> decryptSecret() -> crypto.mjs masterKey() throws; migration 017 dropped plaintext secret cols so prod rows are ciphertext-only. Original claim: session-data/2026-07-01-mkt-hours-gate-session.tmp:36,99. .env.example:127 has only the pla…

### O14 — Provision ZOYA_API_KEY and verify the Zoya adapter mapping against the live API

- **Status:** blocked-on-owner · **Effort:** S · **User value:** medium · **Autonomous:** no
- **Remaining:** 1) Owner must create the free Zoya account, obtain the API key, and set ZOYA_API_KEY (optionally ZOYA_API_BASE) in Vercel prod via a fresh git build (a Vercel Redeploy reuses the stale env snapshot). 2) After the key lands, an agent must verify screenViaZoya's assumed request path (GET {base}/advisory/v2/report?symbol=) and response field names against the live Zoya API and correct the mapping in screenViaZoya()/mapZoyaStatus() (lib/sharia.mjs:146-185) — the current shape is guessed and silently falls back to Finnhub until confirmed. This verification cannot be done without the live key.
- **Blockers:** Requires the owner to create a Zoya account and provision the free ZOYA_API_KEY (a new env var / API key) in Vercel. Until then lib/sharia.mjs stays on Finnhub. The adapter's assumed REST path and response field mapping can only be verified against the live API once the key exists. Provisioning a new API key is explicitly outside autonomous completion.
- **Evidence:** Seam fully coded in lib/sharia.mjs (commit 51e02c4, 2026-06-24): activeShariaProvider() dispatches zoya-when-keyed vs finnhub; screenViaZoya() at lib/sharia.mjs:146-185 with assumed REST shape (GET {ZOYA_API_BASE=https://api.zoya.finance}/advisory/v2/report?symbol= + Bearer, lib/sharia.mjs:148-151); mapZoyaStatus() plus best-effort field mapping (rep.complianceStatus/status/rating, nonCompliantRevenue/nonPermissibleIncomePct); screenSymbol() falls back to Finnhub on any error. Purification wired at lib/handlers.mjs:5092-5112 (only spends screenBatch non-perm % when provider===zoya). KEY NOT S…

### O15 — Run a $1 fractional impact dry-run on the chosen broker account

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner must (1) pick a broker (Robinhood or Schwab), (2) confirm a FUNDED + trade-enabled SnapTrade connection (Robinhood on khanstyle02 has unconfirmed trade-connection; Schwab 0bf63f25 is unfunded), then (3) POST a $1-sized fractional BUY (decimal units) to /api/snaptrade/trade/impact against that live connection and inspect the response for fractional acceptance vs a whole-units rejection (SnapTrade code 1095). The endpoint already accepts decimal units with no flooring, so no code, migration, or env var is required — the task is purely an operational verification on the owner's real brokerage account.
- **Blockers:** Requires owner action an agent cannot take: a live broker choice (Robinhood vs Schwab), a funded + trade-enabled SnapTrade connection the owner must establish, and executing a live impact API call against the owner's real connected brokerage (uses the owner's encrypted userSecret). This is owner/beta-only trading-bot tooling, not a real-user feature.
- **Evidence:** Impact endpoint already exists and passes fractional units through un-floored: POST /api/snaptrade/trade/impact at lib/handlers.mjs:2038 (tradeBody.units = units, lib/handlers.mjs:2078-2086); executeSnapTradeOrder impact->place at lib/handlers.mjs:1208-1245. NOT performed: source note /Users/amaankhan/.claude/session-data/2026-06-30-dca-and-broker-caps-session.tmp "What Has NOT Been Tried Yet" lists "The $1 fractional impact dry-run test ... (Robinhood/Schwab)"; memory snaptrade-broker-capabilities.md lists Schwab/Robinhood fractional as "confirm ... via a $1 impact dry-run on the connected a…

### O16 — Confirm SnapTrade per-broker trading/fractional capabilities (Schwab, Webull, Chase, Firstrade)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Authoritatively confirm trading + fractional support via SnapTrade for the four named brokers, then reconcile the in-app matrix (add Webull/Chase/Firstrade rows to TradeConnectionsPanel and correct Schwab if wrong) and update snaptrade-broker-capabilities.md + MIZAN-STATE-AUDIT.md. Confirmation requires either (a) a human reading support.snaptrade.com Notion matrix (not machine-fetchable — renders empty), or (b) a ~$1 impact dry-run per broker, which needs the owner to first connect + fund Schwab (currently unfunded)/Webull/Chase/Firstrade with trade-permission and authorize the live impact preview — none of these three are even connected today (demo-only).
- **Blockers:** No autonomous path: the authoritative SnapTrade capability matrix is a JS/Notion page (support.snaptrade.com) that renders empty via WebFetch (re-verified) and WebSearch has no SnapTrade-specific trading/fractional table for these brokers. The only definitive fallback — a $1 impact dry-run — requires the owner to connect + fund Schwab/Webull/Chase/Firstrade with trade permission and authorize a live trade impact preview (Webull/Chase/Firstrade aren't connected at all; Schwab 0bf63f25 is unfunded). Motivation also partly obsolete: the shipped Bogleheads basket DCA (88c559f) buys whole shares, sidestepping the fractional requirement that drove this chore.
- **Evidence:** Item's deliverable (authoritative confirmation for Schwab/Webull/Chase/Firstrade) never done. src/components/MizanApp.jsx:6266-6309 (commit 903fec8, 2026-07-01) ships a best-effort TradeConnectionsPanel covering only Robinhood/E*TRADE/Schwab/Fidelity/Coinbase — Schwab line 6276 labels "whole shares" as fact but it's still unconfirmed; Webull/Chase/Firstrade absent. Memory snaptrade-broker-capabilities.md still lists all four as "confirm / unconfirmed / best-effort." MIZAN-STATE-AUDIT.md line 18 item (13): "Schwab = trade but likely whole-share via API … Chase/Firstrade unconfirmed." Webull/Ch…

### O17 — Named Sharia advisory board on the Methodology page

- **Status:** blocked-on-owner · **Effort:** S · **User value:** medium · **Autonomous:** no
- **Remaining:** The Methodology page and an honest "Sharia Governance" placeholder section (with a recruitment CTA) are already shipped. What remains is the actual NAMED board: real AAOIFI-certified scholar names, credentials, and bios rendered on the page. This is gated on the owner recruiting and naming real scholars -- the code explicitly forbids fabricating a board, so a coding agent cannot honestly produce a "named" board without owner-supplied scholar data. Once real names exist, the app work is a small render change (a board-members list in the governance BentoTile), no schema or backend changes needed.
- **Blockers:** Requires the owner to recruit and name real AAOIFI-certified scholars before the board can be published. The codebase deliberately refuses to fabricate a board (MizanApp.jsx:7334-7335), so the "named board" deliverable cannot be built by a coding agent alone -- only the honest placeholder plus recruitment CTA (already shipped) can exist until real scholars are onboarded.
- **Evidence:** Methodology page shipped: commit cadbd66 (Jun 26 2026), ShariaMethodology() at src/components/MizanApp.jsx:7336, routed at Settings->Methodology (MizanApp.jsx:7520 + :7714). Governance section exists at MizanApp.jsx:7416-7425 but is a DELIBERATE placeholder, not a named board -- line 7420: "A named, AAOIFI-credentialed Sharia advisory board is being established and will be published here." plus a scholar-recruitment CTA at :7422. Code comment MizanApp.jsx:7334-7335: "named scholar certification is in progress (do NOT fabricate a board here)."

### O18 — Rotate the CRON_SECRET (value passed through chat)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner must: (1) generate a new random secret; (2) update the Vercel Production CRON_SECRET env var (Sensitive) with it; (3) re-store the same value in Supabase Vault via vault.create_secret(<new>,'cron_secret',…) so trigger_bot_signals() keeps matching; (4) trigger a FRESH git build (a Redeploy reuses the old env snapshot, so the new value won't bind), then verify end-to-end by firing trigger_bot_signals() and confirming net._http_response status 200. A coding agent cannot do steps 2-4 (new secret value in Vercel env + Vault write are owner-only). Optionally scrub the plaintext token from the session .tmp file.
- **Blockers:** Requires owner to provision a new secret value in the Vercel CRON_SECRET env var AND write it into Supabase Vault (name 'cron_secret'), then a fresh git build to bind it. Both the env-var set and the Vault write are owner-held secrets a coding agent cannot supply. No code change is involved; the leaked value was never committed (chat/session-log exposure only).
- **Evidence:** Leaked token only in session file /Users/amaankhan/.claude/session-data/2026-06-30-dca-and-broker-caps-session.tmp:100 ("⚠️ ...rotate at some point"). `git log -p -S"ffd967bcb...ecc5dc" --all` = no hits (never committed; grep of working tree clean). No commit references rotation; recent CRON_SECRET commits 5fd184a & 4cdd6cb (2026-06-25) only BIND the original value. Live consumers: lib/handlers.mjs:215 reads process.env.CRON_SECRET for cronUnauthorized() gate (fail-closed); Supabase Vault entry 'cron_secret' read by trigger_bot_signals() pg_cron job (MIZAN-STATE-AUDIT.md:109,462). No evidence…

### O19 — July 9: create the DCA CORE strategy (primary wealth engine)

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner-only action on/after 2026-07-09: (1) confirm the ~$100 deposit has landed and E*Trade Individual 302ebba3 is trade-enabled (buying_power==cash); (2) create the 'dca' strategy via the existing Bogleheads picker or POST /api/bot/strategies with SPUS core + a small SPSK sukuk smoother as a weighted params.basket, weekly dca_amount ~$25-50, no stop/target, enabled=true; (3) separately sell 2 SPTE at break-even and retire strategy e528f2d7. Zero code to write — the DCA basket engine, create endpoint, and Trade-tab picker are all deployed. This step arms a bot to place real market orders with real money on a funded brokerage account, which a coding agent cannot and must not perform.
- **Blockers:** Live trading on the owner's funded E*Trade account. Requires a ~$100 deposit to land, the account confirmed trade-enabled, and the owner to activate the strategy on/after July 9 (owner explicitly deferred to that date and wants to confirm live prices first). It is the owner's personal trading bot, not a feature that ships value to real Mizan users.
- **Evidence:** All code is shipped; only a live owner action remains. Migration 023 (supabase/migrations/023_bot_strategy_type_dca.sql) allows strategy_type='dca' (applied in prod per MIZAN-STATE-AUDIT.md line 59). DCA basket cron branch handles weighted params.basket at lib/handlers.mjs:5771-5820 (requires basket.length>=2, so SPUS+SPSK works; dca_amount weekly sizing at :5784). bogleheadsBasket()/buildBogleheadsPreset() at lib/handlers.mjs:81 and :114 build the SPUS-core + SPSK-sukuk preset. Create endpoint POST /api/bot/strategies at lib/handlers.mjs:3993 (insert at :4030); frontend Bogleheads picker shi…

### O20 — mmfarooki full-auto onboarding — blocked on the beta tester's own setup

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** No code work remains — the full-auto beta onboarding path is entirely built and committed. Four purely-human steps block Mohammed's account from trading: (1) he must record consent by clicking Accept (POST /api/bot/consent), (2) reconnect his brokerage via SnapTrade with connectionType:"trade" (read-only connections 402 on symbol resolution and can't place orders — a live re-auth), (3) have a funded NON-custodial Individual account (custodial UGMA/UTMA can't API-trade; real money movement), (4) an owner-created mode='full' strategy pointed at that account. None are achievable by a coding agent.
- **Blockers:** Requires live human actions by the owner/family beta tester (Mohammed): a SnapTrade brokerage re-auth with trade permission, funding a non-custodial Individual account, recording bot consent, and creating a full-auto strategy. A coding agent cannot re-auth a brokerage, move funds, or make the live trading decision. This is owner/beta-tester setup, not development. Value is scoped to a single owner-adjacent family beta tester's auto-trading, not the broad Mizan user base.
- **Evidence:** Code fully shipped/committed: canUseFullAuto() reads profiles.full_auto_enabled allowlist (lib/handlers.mjs:1076-1080, commit 05e6de0); POST /api/bot/consent handler (handlers.mjs:3850-3859) + client consent UI wired (src/components/MizanApp.jsx:5443-5448, TradingBotPanel); migrations already applied — full_auto_enabled (supabase/migrations/020_trading_bot.sql:51), trading_bot_enabled/consent_at (022_trading_bot_beta.sql:18-19), account_full_auto (021); cron enforces mode='full' + full_auto_enabled + per-account opt-in (handlers.mjs:5760-5762). Working tree clean (only unrelated MIZAN-STATE-A…

### O21 — Build the equal-weight fractional $50/week halal basket DCA engine (Robinhood, khanstyle02)

- **Status:** blocked-on-owner · **Effort:** M · **User value:** low · **Autonomous:** no
- **Remaining:** Build a NEW variant distinct from the shipped Bogleheads whole-share basket: (a) equal-weight split logic dividing ~$50/wk across a user-supplied halal symbol list (screen each via screenSymbol first) toward equal DOLLAR weight; (b) fractional order support in executeSnapTradeOrder (decimal units, drop the Math.floor whole-share gate for fractional brokers); (c) route execution to the Robinhood trade+fractional account, not E*Trade/Fidelity; (d) symbol-list input UI in the Trade builder. Reuses strategy_type='dca' + params.basket JSON + numeric(12,4) qty (no migration). BLOCKED on owner: (1) confirm khanstyle02 Robinhood connection is trade-scoped (brokerage re-auth), (2) owner supplies the halal symbol list, (3) full-auto-vs-semi decision. Inherently involves live fractional trades on a real account, only validatable against a live fractional broker.
- **Blockers:** Three owner inputs, per MIZAN-STATE-AUDIT.md:18 and snaptrade-broker-capabilities.md: (1) confirm khanstyle02's Robinhood connection has SnapTrade trade scope (brokerage re-auth — funded != trade-enabled); (2) owner supplies the halal symbol list (each halal-screened); (3) full-auto-vs-semi live-trading decision. Also fundamentally requires placing live fractional trades on a real account, and the fractional execution path can only be validated against a live fractional-capable broker connection (E*Trade rejects fractional, code 1095). Not completable by a coding agent end-to-end.
- **Evidence:** Distinctive ask (equal-weight, fractional, user symbol list, Robinhood) NOT built. handlers.mjs:5828-5829 floors to WHOLE shares (Math.floor(dcaBudget/dq.current); if(dcaQty<1) continue); handlers.mjs:6025-6028 comment: 'WHOLE shares only — SnapTrade brokers reject fractional orders (code 1095)'; executor sends integer units at handlers.mjs:1234 (units: qty). What SHIPPED is a DIFFERENT engine: weighted (not equal-weight) Bogleheads basket HALAL_BOGLEHEADS_SLEEVES handlers.mjs:62-75 (US50/Intl30/Sukuk20), buys ONE most-underweight affordable member per period (handlers.mjs:5814-5821) from a c…

### O22 — Monetization strategy decision

- **Status:** blocked-on-owner · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Owner must make the business call: freemium vs subscription, plus price point(s) and the gating boundary. No decision is recorded and no scaffolding exists in code — a coding agent cannot decide this on the founder's behalf. Downstream implementation (after the decision) would require a billing provider (Stripe) with new env vars/API keys, a paywall/tier-gating UI, and a subscriptions/entitlements table (new Supabase migration, which needs owner approval per CLAUDE.md section 8).
- **Blockers:** Founder pricing/business decision (freemium vs subscription) that only the owner can make; explicitly sequenced after Zakat accuracy and screener reliability per project-next-priorities.md.
- **Evidence:** No monetization code exists: recent git log has no pricing/billing/Stripe/freemium/tier commits; source greps only hit false positives (lib/handlers.mjs:2649 Plaid "double billing" comment; src/components/MizanApp.jsx:597-598 demo "STRIPE PAYOUT" income rows; src/components/MizanApp.jsx:7079 "not a paywall" comment). MIZAN-STATE-AUDIT.md and CLAUDE.md have zero monetization/freemium/pricing-model mentions. No memory file records a decision (memory dir grep returned NO MONETIZATION MEMORY FILES). Sole source is project-next-priorities.md, which lists it as next-phase priority #3 "Monetization…

---

## S — Already shipped (doc corrections only)

Verification found these already in code despite being listed as pending. Only action: correct stale docs.

### S1 — Order Ticket activation: remove both gates together

- **Status:** shipped · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** The activation itself is done (shipped 2026-06-23 in 3e8edae, beta-gated form, actively enhanced at HEAD). Leftovers: (1) sync stale docs — MIZAN-STATE-AUDIT.md lines 214/285/464 and CLAUDE.md §8 (gate rule), §9 Known Limitation #9, §16 gap table still claim double-gating; (2) optionally delete the unreachable ComingSoon block at MizanApp.jsx:6487-6492 (dead code after the 6474 early return); (3) GET /api/alpaca/orders (handlers.mjs:5503) and GET /api/alpaca/positions (5526) still have no frontend caller — an open-orders/positions view for the ticket is a possible follow-up; (4) opening Quick Trade beyond the trading_bot_enabled allowlist (real live trading for all users) is a separate product/compliance decision for the owner, not part of this item.
- **Evidence:** Gate removal: commit 3e8edae (2026-06-23) diff shows `{false&&sub==="order"&&...}` → `{sub==="order"&&isAdmin&&...}` (git show 3e8edae -- src/components/MizanApp.jsx). Current code: MizanApp.jsx:6493-6494 render the full Order Ticket for isAdmin; MizanApp.jsx:6474 `if(!isAdmin)return null;` hides the whole Trade tab otherwise; the leftover ComingSoon at 6487 is unreachable (guarded by !isAdmin after the 6474 early return). isAdmin = trading_bot_enabled allowlist via /api/user/features (MizanApp.jsx:10978, migration 022). End-to-end wiring: frontend calls /api/alpaca/order (6394), /api/snaptra…

### S2 — Write market-hours gate + DCA/custodial findings into MIZAN-STATE-AUDIT.md and commit

- **Status:** shipped · **Effort:** S · **User value:** low · **Autonomous:** yes
- **Remaining:** None. The market-hours gate and DCA/custodial findings were written into MIZAN-STATE-AUDIT.md and committed in e27ce91 on the same day (07-01), contrary to the stale session note that flagged it pending. The caveat in the task ("may have been done later that day") is confirmed true.
- **Evidence:** Commit e27ce91 (2026-07-01 16:16:43 -0500, 34 min after the 15:42 session end) wrote both findings into MIZAN-STATE-AUDIT.md. Current MIZAN-STATE-AUDIT.md:14 contains the "2026-07-01 session changes" block covering (1) the market-hours gate (usMarketStatus() + hardcoded holiday set, gates /api/cron/bot-signals and executeSnapTradeOrder with 425 market_closed, commit bd4c639 cited) and (2) the DCA custodial/read-only blocker (366265e9, 402->422, parked ~1wk). Underlying code verified in lib/handlers.mjs:1186 (MARKET_HOLIDAYS), :1191 (usMarketStatus), :1222 and :5697 (gate call sites). The curr…

### S3 — Purification log/overrides not durably persisted (lost on new device)

- **Status:** shipped · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** No code work — durable cross-device persistence already exists and is verified end-to-end. The only residual is a documentation correction: CLAUDE.md Known Limitation #6 and MIZAN-STATE-AUDIT.md Section 8 #4 still assert "New devices lose the purification history," which is false. They should be updated to reflect that both purification keys are Supabase-synced via user_state (TRACKED_KEYS) exactly like mizan_sadaqah. Optional hardening if ever desired (NOT required by this item): move to a dedicated purification_log table for queryability, but that would need owner-approved migration and delivers no user-facing durability improvement over the current user_state sync.
- **Evidence:** Purification log + overrides are Supabase-backed (user_state), identical to sadaqah — refuting the "lost on new device" claim. src/lib/userState.js:32-33 lists both `mizan_purification_log` and `mizan_purification_overrides` in TRACKED_KEYS (same array as mizan_sadaqah, line 27). MizanApp.jsx:3301-3310 persists both via persistUserState() → Supabase user_state upsert (userState.js:116-136), same pattern as sadaqah (MizanApp.jsx:3329,3655). App.jsx:58-77,93 gates render on hydrateUserState(user.id) which restores all TRACKED_KEYS from Postgres→localStorage on sign-in BEFORE PurificationPanel's…

### S4 — SnapTrade rejected the refresh for all connections — resolution unconfirmed

- **Status:** shipped · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** None. The issue was a misleading false-alarm toast (real-time/PAYG accounts returning 402/benign 4xx were rendered as a hard 502 "rejected the refresh for all connections"), not an actual broken connection — /accounts and /activities were always streaming live, which is why later sessions read account data successfully. Fix broadened benign detection, preserved 401/404 as real failures, reports partial success as OK, and added diagnostic logging to the genuine-failure branch. Fully committed and in prod HEAD.
- **Evidence:** Commit c21229c (2026-06-29 16:19:55, authored within the same 06-29 session that opened with the "rejected the refresh" task) — commit subject: "fix(snaptrade): treat real-time/PAYG refresh responses as benign, log genuine failures". Verified in HEAD a18453a: `git merge-base --is-ancestor c21229c HEAD` = YES. Current code at lib/handlers.mjs:2105-2196 (`/api/snaptrade/refresh` handler): isRealtimeBenign() at :2141 matches 402 / 403+code-1141 / real-time-plan detail; hardFailures at :2153 keeps 401/404 as failures; partial success returns 200 at :2158-2160; genuine-failure branch now logs per-…

### S5 — Dividend Income Planner — verify it already shipped before scheduling

- **Status:** shipped · **Effort:** S · **User value:** medium · **Autonomous:** yes
- **Remaining:** None. Fully implemented, committed to main, and wired into Portfolio → Tools → Dividends with no gating. Drop from backlog.
- **Evidence:** Commit dc37b63 (in main history; HEAD a18453a is later). DividendPlanner component at src/components/MizanApp.jsx:4845-4946. Tab routing: line 4613 TOOLS array includes "dividends"; line 4616 toolTabs ["dividends","Dividends"]; line 4773 render {sub==="dividends"&&<DividendPlanner holdings={merged} portfolioValue={tot}/>} — no ComingSoon/false gate. Gross→net-of-purification projection at 4863-4874 (only net reinvested via DRIP, 4871); starting yield blended from real halal-fund holdings via ETF_LIST at 4848-4853; Recharts gross-vs-net area chart 4922-4943; purification-owed total 4890-4891.…

---

## X — Obsolete

### X1 — Decide full-auto vs SEMI mode for khanstyle02's bot strategies

- **Status:** obsolete · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Nothing to build for this decision. Its premise (full-auto is root-only by design, so choose SEMI or extend the gate) is obsolete: commit 05e6de0 already decoupled full-auto to the full_auto_enabled allowlist, khanstyle02 was already placed on it, and the "sets the model for all beta users" policy call was made by the owner at that decoupling. The equal-weight fractional basket DCA this decision blocked was superseded by the Halal Bogleheads weighted basket and the July-9 recalibration to DCA-core on the owner's own account. Any residual is a per-strategy full/semi toggle IF the owner ever activates a live strategy on khanstyle02's brokerage — an RIA-sensitive live-trading judgment only the owner can make, not a coding task.
- **Blockers:** No coding blocker — the decision is obsolete. The only residual is an owner live-trading/compliance judgment (RIA-sensitive, per the COMPLIANCE comment) about whether to ever run a mode=full strategy on khanstyle02's own account; a coding agent cannot make that call, but nothing is currently waiting on it since the DCA work moved to the owner's account.
- **Evidence:** Commit 05e6de0 (2026-07-01) removed the root-only full-auto gate: lib/handlers.mjs:1076-1079 canUseFullAuto() now gates on the full_auto_enabled allowlist (not is_root); enforced at handlers.mjs:4014 (create), 4059 (edit), 4222 (per-account PATCH). khanstyle02 is on that allowlist per memory trading-bot-beta-access.md + MIZAN-STATE-AUDIT.md line 12 ("decoupling ACTIVATED its full-auto capability; owner is aware"). The blocked deliverable was re-homed: Halal Bogleheads basket DCA shipped (7488b5d + picker 88c559f) and the plan recalibrated to DCA-core on the OWNER's account (memory trading-jul…

### X2 — Owner decisions for the basket DCA: broker choice + symbol list

- **Status:** obsolete · **Effort:** S · **User value:** low · **Autonomous:** no
- **Remaining:** Nothing for this item — the basket DCA engine is fully shipped and the broker+symbol-list decision it gated no longer applies (symbols became a curated in-code selectable menu; the Robinhood-fractional recommendation was recalibrated to whole-share E*Trade DCA-core). The only residual is a DIFFERENT, separately-tracked item: the July-9 live activation (create the real DCA-core strategy, fund the account, decide full-auto-vs-semi) — an owner-only live-trading action, not engine work.
- **Blockers:** No real blocker for this item — it is obsolete. Any residual (July-9 live activation) requires owner action and live trades on a real brokerage, which a coding agent cannot perform. The feature is canUseTradingBot-gated (owner/beta-allowlist only, handlers.mjs:1047), so it is not yet exposed to general Mizan users.
- **Evidence:** Basket DCA engine built WITHOUT the owner inputs the item said were required: commit 7488b5d (backend, 2026-07-02) + 88c559f (frontend picker, 2026-07-03). Symbol-list decision dissolved into a curated halal-screened menu (lib/handlers.mjs:62-75 HALAL_BOGLEHEADS_SLEEVES: SPUS\|HLAL / SPWO\|UMMA / SPSK / +SPTE/SPRE), user-selectable per sleeve with HARAM_TICKERS guard (:89, :5807), assembled via buildBogleheadsPreset (:114-141), served by /api/bot/bogleheads (:3959-3971), picker UI in MizanApp.jsx:5463-5960. Robinhood-fractional broker rec superseded: cron branch buys WHOLE shares of the most-un…

---

## U — Unverified (re-verify before acting)

Verification agent hit a session limit; status unconfirmed.

### U1 — Track 2 queued improvements (S–M, behind the numbered items)

- **Status:** unverified · **Effort:** ? · **User value:** ? · **Autonomous:** no
- **Detail:** Four queued items: (1) dividend-by-period bucketing → a purification-due total; (2) median (not mean) expected spend; (3) internal-transfer linking so transfers net to zero (protects net-worth, cash-flow, and Zakat figures); (4) a CSV import→map→dedupe wizard for read-only brokers (e.g. Fidelity, which SnapTrade can't trade or fully cover).

---

## Owner action checklist (condensed)

- [ ] **O1** Landing SEO item #3: halal-investing content pages — plan/discuss with owner before build
- [ ] **O2** khanstyle02 + mmfarooki: bot consent disclosures not yet accepted
- [ ] **O3** Decide fate of the tiny momentum sleeve (strategy 43c1b866)
- [ ] **O4** Owner: provision Alpaca Market Data API keys
- [ ] **O5** Verify Order Ticket live-quote chip under real market-hours load
- [ ] **O6** Submit sitemap in Google Search Console + re-scrape social debuggers (landing SEO)
- [ ] **O7** Verify the E*Trade money transfer landed in khanstyle02's account (ecfea3bb)
- [ ] **O8** Repoint + re-enable DCA strategy 0fd266ae once funds land (~2026-07-08)
- [ ] **O9** Revisit the full-auto allowlist boundary before any compensated/commercial arrangement
- [ ] **O10** Owner: complete the custodian→bank→Individual E*Trade transfer and pick/fund the DCA target account
- [ ] **O11** July 9: sell 2 SPTE at break-even and retire breakout strategy e528f2d7
- [ ] **O12** CSP nonce hardening (deferred from the 2026-07-05 security audit)
- [ ] **O13** APP_ENCRYPTION_KEY missing from local .env.local (blocks local SnapTrade debugging)
- [ ] **O14** Provision ZOYA_API_KEY and verify the Zoya adapter mapping against the live API
- [ ] **O15** Run a $1 fractional impact dry-run on the chosen broker account
- [ ] **O16** Confirm SnapTrade per-broker trading/fractional capabilities (Schwab, Webull, Chase, Firstrade)
- [ ] **O17** Named Sharia advisory board on the Methodology page
- [ ] **O18** Rotate the CRON_SECRET (value passed through chat)
- [ ] **O19** July 9: create the DCA CORE strategy (primary wealth engine)
- [ ] **O20** mmfarooki full-auto onboarding — blocked on the beta tester's own setup
- [ ] **O21** Build the equal-weight fractional $50/week halal basket DCA engine (Robinhood, khanstyle02)
- [ ] **O22** Monetization strategy decision

_Generated from a 74-agent verification workflow (wf_d94e432d-445). Regenerate by re-running the backlog sweep._
