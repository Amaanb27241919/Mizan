# Dependencies
node_modules/
.pnp
.pnp.js

# Build
dist/
dist-ssr/
*.local

# Environment — NEVER commit your API keys
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.gstack/

# Sensitive runtime state — never commit
.snaptrade-users.json
.dev.log

# PWA icons are committed to git for Vercel deployment
# server.js has an ensurePwaIcons() fallback for fresh local dev clones
.vercel

# Claude Code local state
.claude/

# SnapTrade migration backups — contain user_secrets; never commit
scripts/_snaptrade-backups/

# Visual-audit screenshots — local debugging artifacts, not source
bento-audit-shots/
