# MĪZAN — State-of-App Audit

> **Living document.** Re-run every few weeks to track drift between what's built and what's deployed.
> Last audited: 2026-08-30 (updated) · All findings from direct file reads, no guessing.
>
> **2026-08-30 (budgeting — the METHOD was replaced, not the layout; owner decision)** — owner, twice: "doesn't look like mainstream fintech apps" (08-25), then "the budgeting UI doesn't make sense to me very confusing" (08-27). The first report was answered with four *features* (pace colour, seed-from-history, recategorisation rules, category groups), each of which made the screen denser — the second complaint is the evidence that was the wrong read. Presented with the actual fork (top-down vs restyle vs both) plus side-by-side previews, the owner chose **top-down, like Origin**. Shipped `46bfbe4`.
> **What replaced what.** Zero-based envelopes led with **To Budget** = income − everything assigned, a figure that only means anything once you have accepted the method, rendered as the largest thing on the page. Now: `income = save + budget`; `budget = Σ category limits + Everything else`; `left = budget + carried-in − spent`. Math in the new pure `src/lib/budgetPlan.js` (30 tests); the surviving month/category/pace primitives moved to `src/lib/budgetCategories.js` (was `envelope.js` — renamed because the name had become a lie). The zero-based chain (`leftoverByCategory` / `lastMonthOverspent` / `computeMonth` / `computeSeries`) was **deleted, not left dormant**, so there is exactly one definition of "what's left".
> **"Everything else" is the load-bearing piece, not a cosmetic row.** Under envelopes, spending in a category the user never created was invisible — $500 of rent appeared nowhere unless they first built an envelope for it, which is why the model never survived contact with real Plaid data. The remainder of the budget is now itself a bucket, so the arithmetic closes on any transaction set.
> **Storage:** the monthly total + rollover switch live in the TRACKED_KEY `mizan_budget_plan`, **not** in `budget_months` — a column there is a schema migration and those need an explicit ask (CLAUDE.md §8). Migration 027's tables carry the category limits unchanged; its `carryover` column is now vestigial and always written false. Rollover became ONE switch for the whole budget (it was a `ROLL ON/OFF` pill on every row).
> **Four defects found by RENDERING it, after the suite was already green** — the recurring lesson on this repo. (1) The desktop two-column rail **did not exist**: an inline `grid-template-columns` outranks a media query, so the rule never applied and desktop drew a 260px gauge floating in 1440px. (2) At 320px "Everything else" rendered as "Everyt…". (3) `spentByCategory(null)` threw — a default parameter fires on `undefined` only, the **third** occurrence of that exact trap in that file. (4) The sub-tab still advertised "give every dollar a job". Also: the E2E spec hardcoded `2026-08` in fixtures *and* assertions, so it was scheduled to fail on the 1st for reasons unrelated to budgeting; dates now derive from the clock.
> **Verification:** 644 unit + 170 E2E (desktop / 320 / landscape), build clean. Both new guards mutation-tested — zeroing Everything-else absorption and lifting the row controls out of their disclosure each turned the suite red. **Local-setup note:** another app (`Jarvis`) squats on port 4173 and `reuseExistingServer` silently handed the entire suite to it; run `MIZAN_E2E_PORT=4188 npx playwright test` until that port is free.
>
> **2026-08-20 (ethical/BDS overlay — owner-requested; BACKLOG N15 closed)** — owner asked to "add the feature where I can [tell] users if these are stocks to boycott due to ties to Israel", citing Musaffa/Manzil as fast-moving competitors. **The feature already existed** (commit `6cdecde`, 2026-07-02) and the competitive premise was inverted: Musaffa's Alpaca announcement lists screening/purification/Zakat/education and **no** boycott feature; Zoya has no such feature either, only community threads *asking* for one ("Stocks to boycott", "Boycott Israel Updated List") plus public criticism that it marks Caterpillar and Chevron compliant. Mizan was ahead — the overlay was just unmaintained and near-invisible.
> **Three real gaps, all now closed.** (1) **Unsourced claims.** The list stated reasons in Mizan's own voice with no citation — real defamation exposure for factual assertions about named public companies. `ETHICAL_EXCLUSIONS` is now `ticker → { sources[], activity }` against an `ETHICAL_SOURCES` registry; every `reason` reads "*activity* — listed by *body*", and a unit test fails the build if any entry loses attribution (proven by mutating a source key to a typo → 2 tests red). 9 → 13 tickers. **The stale figure in N15 was corrected:** OHCHR's **September 2025** update lists **158 enterprises across 11 countries**, not ~112. (2) **Screener-only by construction.** The pref was `useState` inside the Screener, so `mapPosition` computed `h.bds_` for every holding and **nothing read it** — grep found one assignment, zero reads. Moved to `src/lib/ethicalOverlay.js` (`useEthicalOverlay`, mirroring `useScreenStandard`); now on Holdings and Overview too. Also found: `persistUserState("mizan_ethical_overlay", …)` had **always been a silent no-op** because the key was not in `TRACKED_KEYS`, so the choice never synced across devices in the overlay's entire life. Added. (3) **Zero tests** → 49 unit + 15 E2E, both mutation-tested.
> **Staleness is the real enemy here and is now automated.** Sources are web pages and PDFs, so nothing can poll them; instead the list carries `ETHICAL_RECONCILED` + a 180-day cadence, escalated by `checkEthicalListReview()` in the daily cleanup sweep (same shape as `checkLegalReview`). Reconciling means **removing** as well as adding — OHCHR dropped 15 enterprises in a single pass for ceasing the listed activity, and a name left on after de-listing is a false accusation.
> **Owner decisions taken:** source = UN OHCHR + AFSC cited per ticker; surfacing = Screener + Holdings + Overview (not the bot buy-universe). **Flagged for the owner:** AFSC names **Microsoft and NVIDIA**, both of which sit in the demo persona and most diversified portfolios — the overlay is opt-in and default-off, but the blast radius is large. OHCHR's Israeli-domiciled banks/telecoms were **deliberately omitted** rather than guessed, since the name→ticker mapping was not verifiable.
> **Also fixed in passing:** `<Tag>` silently dropped a `title` prop, so the "BDS" abbreviation had no tooltip anywhere; and `playwright.config.js` hardcoded port 4173 with `reuseExistingServer` on, so an unrelated app already holding that port made the **whole suite run against the wrong application** — one absence-assertion still passed. Port is now `MIZAN_E2E_PORT`-overridable. Build 0 errors · 524 unit (was 475) · 143 E2E passed / 28 skipped, 0 failed.
>
> **2026-08-19b (agent-driven backlog triage — 6 read-only agents over all 88 items' eligible subset; two documentation defects found)** — owner asked whether agents could work the backlog "without burning thru credits". **The premise needed correcting first: sub-agents do not reduce token spend, they increase it** — each carries its own context and re-reads files. What they buy is wall-clock time and a clean parent context. For THIS repo that trade is unusually expensive: `MizanApp.jsx` is ~75k tokens to read and `handlers.mjs` ~66k, so a naively-briefed agent burns ~140k before doing anything. Every brief therefore banned opening either file whole (grep-to-locate, `sed` for ~40 lines), banned builds and test runs, and shipped precise `file:line` pointers that **I extracted once** so no agent paid the orientation cost twice.
> **Bucket F (6 items):** F2, F4 and F7 were **already done** — all three shipped in `0f12c1f`, whose in-code comments cite "F7" by name. **F5 was understated**: recorded as "1 HIGH deferred", actually **5 HIGH** (`brace-expansion`, `nanoid`, `postcss`, `undici`, `vite`) — still all dev-only, re-verified as absent from `dependencies`, and the entry now splits the non-breaking half (`npm audit fix` clears the eslint/jsdom ones) from the vite@8 migration that remains correctly deferred. F14b was one-third stale. F3 re-confirmed against **production**: 8 ratios, `max(updated_at)` 2026-06-17.
> **Buckets N/M/P (33 items):** 2 done (N11, N26), 6 partial (N4, N6, N12, N17, N18, P1), 25 genuinely real. **The two buckets behaved oppositely and the reason is worth keeping:** F was 50% already-fixed because F items are *fixes to code that already exists*, so ongoing work kept completing them incidentally. N/M/P is 6% because those are *net-new surfaces* nothing went near. **Parked work stays parked; adjacent work does not finish it for you** — so N/M/P effort estimates are broadly trustworthy where F's were not. Adjustments were made in both directions: N4 and N12 dropped to a single missing piece, while **N6 deliberately kept its M estimate** because the remaining per-token classifier is most of its work (`lib/sharia.mjs:313` literally reads `// Per-token verdicts are BACKLOG N6.`).
> **⚠️ TWO DOCUMENTATION DEFECTS — statements about production that were false:**
> 1. **The geo-block was never narrowed.** BACKLOG P3 *and* the `region-gate-and-usd-only` memory both asserted that since 2026-07-31 non-US visitors reach the app and only Plaid is gated. Verified against `main`: `middleware.ts` has **exactly one commit ever** (`540ffda`, 2026-05-25) and still rewrites **every** non-US visitor to `/us-only.html` on `matcher: "/:path*"` — the whole app. `lib/geo.mjs` and `src/lib/region.js` **do not exist** on main; `grep canConnectBank|REGION_UNSUPPORTED` = 0. The narrowing is real but lives only on the unmerged branch `region/canada-narrowing` (`d1f9495`). CLAUDE.md §2 had this right; the backlog and the memory did not. **A future session reading that memory would have believed Canadians could reach Mizan. They cannot.** Both corrected.
> 2. **CLAUDE.md instructed removing a gate that does not exist.** §8 and §10 described the Order Ticket as `{false && ...}` double-gated with "both gates must be removed together". Zero occurrences in the file — the only gate is `!isAdmin` at `MizanApp.jsx:7098`, so **the ticket renders live for admins today**. Both sections corrected.
> **Cost, recorded because it is the answer to the owner's actual question:** ~733k tokens across 6 agents (~336k for F, ~397k for N/M/P), ~13 minutes wall-clock. It bought 5 items closed, 7 re-scoped, and two false production claims corrected — one of which had survived at least three prior sweeps. **Agents earn their cost when the output is a decision, not a diff**; pointing them at implementation inside a 13k-line monolith is where it gets ugly.
> **Every load-bearing agent claim was re-verified inline before being recorded** (the cited commit exists; the function has a body not just a comment; the audit count is real; the prod table has 8 rows; `middleware.ts` really is unchanged). One F-agent was right on substance but cited a commit that had to be confirmed — treat summaries as leads, not findings.
>
> > **2026-08-19 (capture pass — verified state, and found the usage tracking could not answer its own question)** — owner asked to capture everything and save the documents. Verification, not transcription:
> - **All five legal documents confirmed saved and consistent** — web page + `legal/` + `public/legal/` PDFs, SHA-matched pair-for-pair, all dated August 2026. Six public pages (`/privacy`, `/terms`, `/contact`, `/security`, `/data-retention`, `/access-controls`) render with **zero overflow at 320px**.
> - **Migrations 027 + 028 confirmed live in prod:** 3 tables, 9 RLS policies, 2 RPCs. `plaid_transactions` now 2,046 rows — the figure the corrected Privacy Policy describes.
> - **⚠️ `nav_usage` was recording only TOP-LEVEL tabs.** Querying the live table (11 rows, 7 destinations, 2 users) showed nothing but `overview`/`portfolio`/`settings`… — no `portfolio/backtest`, no `goals/zakat`. `recordNavView` had been wired into the top-level `setNav` only, and every sub-tab strip goes through `setSub`. **The feature could not answer the question it was built for** — "do Backtest and ETF Overlap earn their slots?" — because both are sub-tabs. Fixed centrally in `TabBar` via a `track` prefix rather than at each call site; all five strips now carry one. Verified live: `portfolio/backtest`, `portfolio/overlap`, `goals/zakat` now record. Guarded, and mutation-tested (reverting yields "only top-level paths recorded: portfolio").
> - **Sample size caveat, stated because the number is tempting:** 20 views across 2 users is a working pipeline, **not evidence**. Nothing about which surfaces earn their place should be concluded until there are weeks of data from real users.
>
> > **2026-08-18c (legal documents — two false disclosures, now single-sourced and on a review clock)** — owner asked to recreate the Privacy Policy as a web page and set a monthly/quarterly update cadence. The page **already existed** at `/privacy`; the real finding was that neither copy was accurate.
> **Verified against the live system rather than reformatted:**
> - The policy stated bank transactions were **"not stored… fetched live each session"**. `plaid_transactions` holds **2,039 rows across 4 users spanning 2026-03-01 → today**. Corrected to state they are stored and for how long.
> - The processor list omitted **Anthropic**, while `/api/advisor` sends holdings, cost basis and screening verdicts to `api.anthropic.com`. Added, with what actually leaves the app and a note that nothing is sent if the Assistant is never opened.
> - The new `nav_usage` counters (migration 028) were undisclosed. Now §2.6, framed honestly: *"a counter, not an activity log… no financial information of any kind, and no IP address or device details."*
> - The PDF still pointed at `mizan-puce.vercel.app`; production is `app.mizan.exchange`.
> **Neither error was deliberate** — the policy was written in May 2026 and both features shipped afterwards. That is precisely why the reminder has to be automatic.
> **Structure:** the web page is now the **single source of truth** and the PDF is a rendering of it (`scripts/generate-legal-pdfs.mjs`, `npm run gen:legal-pdfs`), writing `legal/` and `public/legal/` together so they cannot diverge. Security / Access Controls / Data Retention are PDF-only with no web equivalent and are skipped rather than emitted blank — they now read three months older than the other two, which the reminder will surface. `LEGAL_LAST_UPDATED` in `src/lib/legal.js` is the one date the pages render *and* the reminder reads.
> **Cadence:** quarterly (90 days), owner decision. Implemented as `checkLegalReview()` inside the existing **daily** `/api/cron/cleanup` sweep — the Vercel plan allows only daily crons, and a reminder derived from the document's own date cannot drift from what users are reading. An unparseable date is treated as **overdue**, so a typo in the constant cannot silently disable it.
> **⚠️ Two defects caught by READING the rendered PDF, not by tests:** a duplicated sentence ("Recent transactions (merchant, amount, date, category)." twice — my edit left the original lead-in) and JSX whitespace collapsing into `Theseare stored` / `SeeAnthropic's`. Twelve green tests passed straight through both. Guards added for the duplication; the spacing class is a known JSX hazard now noted in CLAUDE.md.
> **⚠️ A guard of mine that could not fail:** the first Anthropic assertion matched `/Anthropic/`, which still passed when the processor entry was deleted because the name survived in the link text. Tightened to assert the list entry itself, then re-mutated. A test that cannot fail reads as coverage while providing none — same lesson as the service-worker and Plaid-category fixtures.
> **468 unit tests.**
>
> > **2026-08-18b (readability, information architecture, usage tracking)** — owner asked whether Mizan has too many tabs for a young or seasoned investor to grasp, then asked how industry apps stay readable on any device.
> 1. **Measured the nav rather than guessing:** 6 top-level tabs, **21 destinations, max depth 3**. The count is fine (Copilot ~5, Monarch ~7 — peers have fewer because they do less). The *shape* was not. **Zakat — the #2 value proposition — lived behind a tab labelled "Goals"**, which gives no hint it is there, and Portfolio→Tools→Backtest was the only depth-3 branch, hidden behind a junk-drawer label describing none of its five unrelated contents. Fixed: label is now **"Plan"** (nav id stays `goals` — localStorage, `?tab=`, the manifest shortcut, `nav_usage` and every `data-tour` hook key on it), and Portfolio is **one flat strip of 9**. **21 destinations before and after; nothing was removed, it stopped being nested.** Depth 3 → 2.
> 2. **The real readability problem was size, not layout.** Mizan rendered **39% of its type at 9–10px** (91 sites at 9px, 408 at 10px, plus 8px and 9.5px) — under the ~11px Apple and Google both treat as the practical minimum for secondary text. Migrated **1,260 sites** to an 11-step fluid `clamp()` scale (utopia.fyi method, which `~/.claude rules/web/coding-style.md` already prescribed and the codebase had **zero** uses of). Expressed in **rem, not px** — measured, `--fs-2xs` goes 11.06px → 13.75px when the root moves 16px → 20px, while a hardcoded 10px never moves; fluid-by-viewport alone ignores iOS Dynamic Type. **One deliberate exception:** the iOS focus-zoom rule stays a literal 16px, because a rem token drops under Safari's threshold the moment a reader *lowers* their OS text size — a browser behaviour trigger is not a design token.
> 3. **Three regressions the migration caused, plus one it exposed.** Header overflowed 17px at 320px **but only with connected accounts** (the broker force-refresh button appears then) — the guard had only ever run against the empty state, the same one-state blind spot as the fixture bugs. Sweeping 320→1440 to fix it then exposed a **pre-existing** bug: the header clock strip hid only below 600px but needs ~1010px to fit, so **every width from ~601 to ~1010 overflowed — iPad portrait included**. The dock ran 14px over at 320px once labels reached the floor; fixed with padding, never by shrinking type back under it.
> 4. **Onboarding was unreachable on a phone** (owner screenshot, Safari on a real iPhone — the first real-device check of this work, and it found something). The overlay was `position:fixed` + `align-items:center` with **no `overflow-y`**, which clips content at *both* ends: the last rows and the finish button could not be scrolled to at all. Rows also wrapped to 6–7 lines because a three-column grid left ~156px for the description. Rows are now **~300–400px → 106px at 320px**. `e2e/onboarding.spec.js` **did not exist** — `e2e/support/app.js` seeds `mizan_onboarded:"1"` so feature tests reach features, and its own comment promised a spec that was never written, so that screen had never been rendered by any test.
> 5. **Usage tracking added** (owner-requested; CLAUDE.md §8 requires an explicit instruction and this is it). Migration **028** `nav_usage` — **counters, not an event log**: a running count per (user, destination), so there is no timeline to reconstruct when someone used the app, no financial data of any kind, and no ip/user_agent. At most ~21 rows per user. RLS select-own with **zero write policies** — writes go only through a `SECURITY DEFINER` RPC. ⚠️ Caught during build: the API route runs under the service role where `auth.uid()` is NULL, so the first RPC would have silently recorded nothing; an admin variant taking the verified user id fixes it. ⚠️ **Unverified:** whether the privacy policy (a PDF) covers usage analytics — owner should confirm.
> **Repos consulted:** container queries are baseline (~95%) and the 2026 default for component-level responsiveness, but Mizan is an inline-styled monolith with no reusable slots — large refactor, small payoff, deliberately **not** adopted. Fluid type was the opposite and is what shipped.
> **Guards:** 8 new E2E — 5 asserting no text renders below 10.9px on any tab (this is what caught `font-size` in CSS *strings*, which a JSX-only migration missed), 1 that the scale is genuinely fluid and capped, 2 pinning the IA (max depth 2; "Plan" replaces "Goals" and Zakat is reachable). All mutation-tested. **456 unit + 113 E2E.**
>
> > **2026-08-18 (screening standard, envelope budgeting, return figures, density)** — owner feedback round. Five things, two of which were features users could never reach:
> 1. **The screening standard picker did nothing.** `lib/sharia.mjs:106` computed `status` as a CROSS-STANDARD VOTE (halal if ≥5 of 7 frameworks pass); the picker fed one checkmark column while every compliance surface read that vote via `sh_`. Switching AAOIFI→Dow Jones changed nothing anywhere. Standards genuinely disagree (AAOIFI caps receivables at 49%, Dow Jones at 33%), and a vote across seven of them is nobody's methodology. New pure `src/lib/shariaStatus.js` resolves the verdict through the chosen standard, shared via localStorage + a window event (mirroring `useHideValues`) because the picker sits five components deep from `mapPosition`. Crypto still resolves to **review, never halal**; a prohibited sector stays haram even when `byStandard` is missing. Picker moved to the top of the Screener with that standard's own thresholds beneath it.
> 2. **Budgeting was entirely dark.** `Budgeting.jsx` (429 lines), the `budgets` table and `/api/budgets` all existed, all worked, and the component was **imported nowhere** — CLAUDE.md §2 described a shipped "Budget tab" no user could reach. Prod confirmed `budgets` held **0 rows / 0 users**, so superseding it needed no data migration. Rebuilt as ENVELOPE (zero-based) budgeting per owner choice and mounted in Finances. Migration **027** (`budget_months` + `budget_entries`, RLS + a CHECK pinning `month` to the 1st so one month cannot split into two buckets; constraint verified live). Math lives in pure `src/lib/envelope.js`, ported from `actualbudget/actual` — stores only user intent (`budgeted`, `carryover`) and DERIVES leftover / overspend / To-Budget, so nothing can drift. **Only 4 of 12 users have a linked bank**, which makes `manual_income` the primary path rather than a fallback. Islamic presets ship with it (Zakat and Hajj/Umrah carry over toward a lump obligation; ordinary spend resets). ⚠️ Envelope balances are PATH-DEPENDENT — `/api/budget` returns the whole history for exactly that reason.
> 3. **Three different return figures on one screen.** The chart series baselined at cumulative CONTRIBUTIONS but ended at MARKET VALUE, so every range absorbed all-time gains — 1M read **+94.77%**, All read **+1039.86%**. The headline is now money-weighted XIRR from the same `performance.js` function the RETURN & RISK panel uses, so the two cannot disagree by construction; the curve no longer invents alpha in any sub-window (real nightly snapshots still override it point-for-point). Units disambiguated: the dollar is a TOTAL, the percentage a RATE (`%/yr`), and the panel is labelled `on cost`. Chosen over time-weighted because TWR needs a valuation at every cash-flow date (Mizan has snapshots only from signup) and DCA investors find it counter-intuitive — it is the top open request on Ghostfolio, which ships TWR only.
> 4. **"COMPLIANCE 33.1%" → "CONFIRMED HALAL".** The arithmetic was already right and deliberately so (halal share of screened equity), but the word made 33.1% read as "67% of my money is haram" when 51.9% was merely *pending review*. One word; no math touched.
> 5. **Density, measured not guessed** (390px): Finances was **11.4 screens / 808 words**, double every other tab, and **79% of it was RECENT TRANSACTIONS** rendering 50 rows inline — a desktop page size on a phone. Cut to 15 (Load more still pulls 50) → **5.4 screens / 369 words**. Guarded at <8 screens.
>
> **⚠️ The recurring lesson, twice in one session.** A fixture that encodes your assumption rather than what the provider actually sends is not a test. Mounting the budget crashed the entire Finances tab (`category.trim is not a function` — Plaid sends an ARRAY plus `personal_finance_category.primary`) and **86 green E2E did not notice**, because the fixtures used string categories. Found by rendering the tab and looking at it. Same class as the 2026-08-15 service-worker discovery. Fixtures and code both fixed; `categoryOf()` carries the note.
>
> **Repos mined** (owner ask): `actualbudget/actual` 28.2k — the envelope math and its store-little/derive-everything property, which shaped migration 027. `maybe-finance/maybe` 54.3k — period as a first-class row, lazy bootstrap. `firefly-iii` 24.4k — corroborates period-scoped available-to-budget as its own entity. `portfolio-performance` 4.0k — `IRR.java` uses Newton goal-seek with a bisection fallback; **checked Mizan's `xirr()` against it and it already matches (Newton 100 iters, then bisection with proper bracketing). No change warranted** — recorded because a negative result is worth as much as a change. `ghostfolio` 9.1k — TWR-only; its top open request is the money-weighted return Mizan already had.
>
> **2026-08-15 (PWA — install surface for Chrome/Android + Safari/iOS, and a test harness that was lying)** — owner ask: optimise for Google and Safari PWA. Two genuine platform defects, several install-surface gaps, and one significant harness discovery:
> 1. **iOS status bar was invisible for every light-theme user.** `apple-mobile-web-app-status-bar-style` was `black-translucent`, which forces the clock/battery/signal to render in **white** over the page background — and Mizan's default face is the paper canvas `#faf8f4`. iOS has no dynamic contrast for this. Apple has also **deprecated** the value. Now `default` (dark text, view starts below the bar); the existing `env(safe-area-inset-top)` padding degrades cleanly to 0 in that mode.
> 2. **Chrome's richer install UI was never eligible** — it requires at least one `screenshots` entry of the matching `form_factor`, and the manifest had none, so Android/desktop fell back to the bare install prompt. Added 3 narrow (390×844) + 2 wide (1440×900), generated by `scripts/generate-pwa-screenshots.mjs` in **demo mode** (which Settings itself describes as being for screenshots) so they show a real portfolio rather than three empty states.
> 3. **`orientation: portrait-primary` locked installed apps to portrait**, cancelling the same day's landscape work for exactly the users who install. Removed — owner decision, rotation allowed.
> 4. **Manifest `id`** added, set **byte-identical to `start_url` (`/?source=pwa`)**. This is not cosmetic: with `id` absent the computed identity IS start_url, so every existing install is keyed on that string. Setting `id` to the tempting `"/"` would make Chrome treat this as a *different app* — existing installs stop updating and reinstalling creates a duplicate. There is a unit test pinning it.
> 5. **Shortcuts** (Zakat / Screener / Net worth) plus a `?tab=` deep-link reader in MizanApp's nav initialiser, validated against the real tab list and stripped via `replaceState` after use. The reader was **required**, not decorative: the app previously only read the URL for the Plaid OAuth resume, so shortcuts would have silently restored whatever tab localStorage last held and the shortcut menu would have been a lie.
> 6. Also: 180×180 `apple-touch-icon` (iOS was rescaling a 192; the file is verified fully opaque, since iOS flattens alpha onto **black**), `launch_handler: navigate-existing`, `lang`/`dir`, and **offline.html rebuilt** — it was still on the pre-rebrand terminal palette (`#0d1311` canvas, the retired `#c9a24b` gold), i.e. the one screen a user sees when the app is failing was the one screen that looked like a different product.
> **Verified NOT a problem, by measurement:** the `maskable` icon declaration. Decoding the PNGs showed all ink inside the 40%-radius safe zone (**0 pixels outside**) and the files fully opaque, so Android's crop is safe. The assumption going in was that they'd be clipped.
>
> **⚠️ The harness finding, which matters more than any of the above.** While generating screenshots, the shots rendered **"Nisab unavailable"** despite a fixture supplying live nisab values. Root cause: the E2E suite runs against the **production build, which registers `/sw.js`** — and **requests issued by a service worker bypass Playwright's `page.route()`**. So every `/api/**` call went to the real preview server, hit the SPA rewrite, and returned `index.html` with a 200; the app's JSON parse failed and every surface fell back to its empty state. **The entire fixture layer in `e2e/support/app.js` had never done anything.** It went unnoticed because most specs assert empty states — which is exactly what a failed parse produces — so they passed *because* the fixtures were broken. Only `screener.spec.js` actually needed fixture data, which is why only it failed (those 5 failures were previously filed as BACKLOG F15, "the app or the assertion is broken"; **it was neither**). Fixed with `serviceWorkers: "block"` in `playwright.config.js`; all 7 screener tests pass with **no app change**. Generalisable lesson: *a test that passes identically with and without its fixtures is not testing what it claims.* Tradeoff accepted: the E2E suite no longer exercises the service worker, so SW caching/offline needs its own check.
>
> **Guard:** `src/test/pwaManifest.test.js` (12 tests — id/start_url identity, no orientation lock, every referenced asset exists, declared icon sizes match the real files, Chrome's screenshot rules incl. the ≤2.3 aspect cap and one-ratio-per-form-factor, description ≤300 chars, shortcuts only target deep-linkable tabs, no deprecated iOS status bar value, 180×180 apple-touch-icon, pinch-zoom allowed). Mutation-tested: 5 deliberate regressions all caught. Suite now **417 unit + 53 E2E, zero failures** — the first fully green E2E run.
> **iOS launch images — DONE (owner approved the cost).** 22 files / **836KB** in `public/splash/`, covering 11 iPhone metric families × light + dark, wired to 22 `apple-touch-startup-image` tags. Generated by `scripts/generate-ios-splash.mjs` (`--check` re-verifies) from the transparent `logo.png` lockup — rendered natively for light, painted as an ivory silhouette via CSS mask for dark, so the composition is identical across themes. Android needed nothing: Chrome synthesises its splash from `background_color` + icon. **Portrait only** — landscape would double the matrix for a case that barely happens (people tap a home-screen icon upright) and degrades to a plain background, not a break. **Known residual:** iOS selects by `prefers-color-scheme`, i.e. the *system* appearance, while Mizan's theme is in localStorage — so someone forcing the app dark on a light-appearance phone still gets the light splash. Unavoidable without iOS exposing the app's own preference; generating both still fixes the common case. Two guards added (14 tests total): every tag's media query is parsed and the referenced PNG must match `device-width × dpr` exactly, and every covered device must carry both colour schemes. Mutation-tested — a deleted file and a wrong-sized file were both caught.
>
> **2026-08-15 (responsive — the app was never sized for a real phone, in either dimension)** — owner ask: make Mizan adaptable to all phone resolutions in the PWA, "height as well", across "every screen from login to every tab and sub-tab". Swept 15 real device viewports (320→430 portrait, 568→932 landscape) against login, all 6 tabs and every sub-tab. **This is the first time anything rendered the app below desktop width with touch emulation on**, and it found six classes of defect:
> 1. **The bottom dock overflowed** by **71px @320, 29px @344, 13px @360**. It scrolls horizontally, so nothing was clipped — the last item (Settings) was simply off-edge and undiscoverable. **360px is the most common Android width in the world.** Fixed by yielding the optional tour launcher below 400px (still in Settings → Replay tour + Cmd+K) and tightening pill padding below 374px.
> 2. **The Screener's control row ran up to 169px past the viewport on EVERY phone width up to 430**, taking the **"Re-screen" button** with it — the Screener was unusable on a phone. Cause: `flexShrink:0` with no wrap on a ~460px group. `html{overflow-x:clip}` meant no scrollbar ever hinted at it.
> 3. **The notification bell painted on top of the HALAL wordmark badge** at 320/360/390. The header's action group was `flex-shrink:1; min-width:0`, so it was squeezed *narrower than its own contents*, and `justify-content:flex-end` spilled the surplus **leftward** under the brand. Note the parent rects did **not** overlap — only the leaf children did, which is why box-level measurement missed it.
> 4. **Height was never handled at all.** Zero `dvh`/`svh` in the codebase: all 10 viewport-height values used `vh`, which on a phone is the **URL-bar-retracted** height. Everything now resolves through `--mz-vh` (`dvh` with a `vh` `@supports` fallback), defined in *both* THEME_CSS and `index.html` so Login/legal pages get it too. The Assistant panel also had contradictory constraints — `minHeight:60vh` vs `maxHeight:calc(100vh-280px)` — and since min-height beats max-height in CSS, **the cap was dead on every phone under 700px tall**.
> 5. **Landscape got nothing.** Every touch-target rule was keyed on `max-width:640px`, but a landscape phone is **568–932px wide with 320–430px of height** — past every breakpoint. Same device, same sub-tabs: 40px tall in portrait, **33px in landscape**. Rules are now keyed on `pointer:coarse` ("is this a finger?"). Added a short-viewport block that unsticks the header and hides the clock strip, which was wrapping to 3 lines and inflating the header on the screen with the least room.
> 6. **Sub-44px controls throughout** — chart range chips (23px), filter chips (26px), Goals/Settings pills (30–32px), form controls (40px), checkboxes (14px), a 4px range slider, and the whole Login screen (Login renders *instead of* MizanApp, so THEME_CSS never loads there and it needs its own sizing). Also removed **`user-scalable=no`** from the viewport meta — a WCAG 1.4.4 failure that blocked pinch-zoom in an app whose densest surfaces are numeric tables. It was only there to stop iOS focus-zoom, which the existing 16px font rule already handles correctly.
>
> **A technique that failed and was reverted, recorded so it isn't retried:** the first attempt grew only the *hit region* via a 44px `::after`, keeping chips visually small. Measuring the *effective* region with `elementFromPoint` showed why that is a trap — in a row of chips, and especially a wrapping row, each invisible region overlaps its neighbours' and the last-painted wins. Chips ended up with **8–20px of usable reach, less than their own box**, and a tap aimed at one chip could land on another. Silent mis-taps are worse than a small target, so the controls got real height instead.
>
> **Guard:** `e2e/responsive.spec.js` — overflow/clipping across login + every tab + every sub-tab, dock fit, header leaf-overlap, landscape sub-tab height, and `--mz-vh` resolution. `playwright.config.js` gained `hasTouch: true` (**what makes Chromium report `pointer:coarse`** — without it the touch rules never evaluate and the suite passes by not testing them) and a `phone-landscape` project. All guards were **mutation-tested**: reverting each fix turns the matching test red. That process caught a real hole in the guard itself — the first version walked only top-level tabs and passed against the reverted Screener bug, because the Screener is a sub-tab. Sub-tab traversal is now part of it, and it immediately found a further defect (Backtest's two `<input type=date>` fields refusing to shrink below their intrinsic width, holding the panel ~60px wider than a 320px viewport). ⚠️ Verified by measurement **and** by screenshot at 320/360/390/667-landscape in both themes.
>
> **2026-08-14 (demo — the Overview figures were regenerating themselves on every page load)** — owner report: "the demo account data is bugging out … related to the demo overview figures loading." Confirmed and fixed at the source. `DEMO_ACTIVITIES` (MizanApp.jsx) was built with **`Math.random()` at module load** across 10 call sites, so **every page load produced a different transaction history**. Because the Overview's activity-derived figures come from that history, they moved on their own between refreshes: all-time contributions **$275,400–$280,200**, YTD contributions **$26.3k–$30.2k**, YTD fees **$319–$449**, YTD withdrawals **$0–$1,700**, activity count **393–408**, and the money-weighted return by a **full percentage point** (−1.91%…−0.91%).
> **Why it read as a *loading* bug specifically:** first paint hydrates `snapAccounts`/`snapActivities` from `localStorage` (`:11504-11505`) — i.e. the **previous** load's random history — and then `fetchSnapHoldings` overwrites it with a freshly-randomized one (`:11760-11761`). The demo Overview therefore rendered one set of numbers and visibly **jumped** to another a moment later. Determinism fixes the jump too, since the cached copy now equals the regenerated one.
> **Two more defects in the same block, both making the figures *wrong* rather than merely unstable:** (1) **contributions exceeded the portfolio's value** — $277k deposited against $264k held — so a book containing nothing but winners reported a **negative** return on the Overview; the schedule is now ~38% seed + ~23% drip and the demo returns a stable **+13.97%**. (2) **Cost basis didn't reconcile**: the add-on BUY lot was priced at `(cost+price)/2`, so the basis implied by the activity feed disagreed with the basis on the holding itself — 3% on SCHD, **52.8% on PLTR**. Lot multipliers are now chosen so `0.6×(0.85·ac) + 0.4×(1.225·ac)` is exactly `ac` → **0% drift on all 21 positions**. This is the same drift class the `DEMO_ACCOUNTS` balance normalizer was written to kill; the fixture now self-reconciles on both axes.
> **Fix:** a seeded mulberry32 PRNG (4 lines, no dependency) replaces `Math.random()` — dates and skipped months still look organic, but the fixture is byte-identical on every load. **Rule going forward: no `Math.random()` in a demo fixture.** A demo that disagrees with itself between refreshes reads as a broken app, and it is indistinguishable from a real data bug until someone measures it.
> **Guarded by `src/test/demoFixtures.test.js`** (8 tests, suite now **405 passing across 22 files**, was 397/21). It slices the demo block out of the monolith and evaluates it twice in a fresh `node:vm` context — the only way to observe module-load nondeterminism without extracting the fixture (CLAUDE.md §8 forbids splitting MizanApp.jsx); it fails loudly if the `const _pos =` / `const DEMO_SHARIA` markers move. **The guard was verified to bite**: against a mutated copy with `Math.random()` reintroduced it fails, against current code it passes. Also pins the balance invariant, basis reconciliation, and contributions-below-value.
> **Known and deliberate:** there are **no `SELL` activities** in the demo, so the Performance panel's realized P&L is **$0** — the stale comment claiming sells was corrected rather than inventing them, since adding sells means re-deriving every position's units. Some individual lots sit *above* current price (AMAGX/HLAL/SCHD add-on lots) — realistic and intentional; the test asserts the **blended** basis shows a gain, not every lot. ⚠️ **Not visually verified** — the Chrome extension was not connected, so this was proven numerically (by evaluating the fixture directly) rather than by looking at the rendered Overview.
>
> **2026-08-11 (hygiene — both parked prototypes are now on branches; `main`'s working tree is finally clean)** — the OpenBB screening adapter joined the Canada narrowing on a branch. Two bodies of finished, tested, deliberately-unmerged work had been living in `main`'s working tree — Canada since 2026-07-31, OpenBB since 2026-07-30 — because committing to `main` auto-deploys and neither was approved to ship. That is a safe-looking state that is actually fragile: **any `reset --hard`, `checkout .` or `stash` erases it**, which nearly happened to the 176-line OpenBB adapter while cleaning up an unrelated pre-commit-hook test (avoided by using `reset --soft` plus a targeted per-file restore).
> **Where they live now:** Canada → **`region/canada-narrowing`** (`d1f9495`) · OpenBB → **`screening/openbb-adapter`** (`a8828bd`). Both pushed to origin, so neither depends on this machine any more. Production is unchanged by either: Canada was never deployed (prod stays US-only) and OpenBB is inert without `OPENBB_API_BASE`.
> **Splitting OpenBB out was not a clean file move** — `lib/sharia.mjs` held BOTH the OpenBB adapter and the 2026-08-10 crypto verdict fix, and patch-staging is interactive and unavailable here. The crypto fix was separated by snapshotting the combined file, restoring `HEAD`, re-applying only the crypto hunk, committing that, then restoring the snapshot so the remaining diff was OpenBB-only — verified by grepping the diff for crypto lines (0) before branching. **Lesson worth keeping: don't let two unrelated changes accumulate in one file across sessions; the untangling is riskier than the work.**
> `main`'s working tree is now empty for the first time in weeks, so `git status` is a usable signal again rather than permanent background noise. BACKLOG F9 and CLAUDE.md's `middleware.ts` entry both name their branch — check the branch out before rebuilding either.
>
> **2026-08-10 (display audit — the black flash on every cold load, and three more)** — owner-asked sweep of web + PWA rendering. Four real defects, none of which showed up in normal use because they all live in the first ~200ms or on hardware not being tested.
> **(1) Every cold load flashed near-black.** `index.html`'s bootstrap CSS still painted `background:#07080E; color:#C8CCE4; font-family:'Sora'` — a terminal palette from an earlier design era. The app has been light-first paper (`#faf8f4`) since the rebrand, so first paint was black until React mounted and injected THEME_CSS. Most visible in the **installed PWA**, where there is no browser chrome to soften it. Fixed: first paint now matches the app, plus a tiny pre-paint `<script>` that resolves the stored theme (mirrors MizanApp's `mizan_theme_mode`, default light, `auto` → OS) so dark users don't get the inverse flash. Verified against the BUILT output in all three modes: default → `#faf8f4`, stored dark → `#0e1626`, auto → follows OS.
> **(2) The PWA splash was a color from neither theme.** `manifest.webmanifest` had `background_color`/`theme_color` `#0d1311`. Now `#faf8f4` (the default face). ⚠️ **These two fixes had to ship together** — correcting the splash alone would have made the black bootstrap flash MORE conspicuous, not less. (3) `index.html`'s per-scheme `theme-color` metas were stale the same way (`#0d1311` / `#f5f2eb`) → `#0e1626` / `#faf8f4`.
> **(4) The new Screener lookup input was clipped on small phones.** It hardcoded `width:262`; at a 320px viewport the tile leaves **248px** (`main` 16+16, `BentoTile` 20+20), and `html{overflow-x:clip}` means overflow is silently CUT OFF rather than scrollable. Now `flex:0 1 262px` + `minWidth:0`. **Rule: don't hardcode pixel widths inside a BentoTile** — 248px is the real budget at 320px.
> **Also removed** the render-blocking Google Fonts stylesheet for **Sora + JetBrains Mono**. Sora is used nowhere in the app; JetBrains Mono appears only as a fallback BEHIND IBM Plex Mono, with system Menlo/Monaco right after it — two whole families downloaded render-blocking to cover a fallback a system font already handles. Preconnects kept (THEME_CSS still `@import`s Fraunces / IBM Plex from the same origin).
> **Verified good:** prod 200 with **zero console errors**; login renders correctly at 375/768/1280; manifest icons + `offline.html` reachable; deployed bundle contains the new Screener strings. **Service worker re-checked and is sound** — HTML navigations are network-first and Vite hashes asset filenames, so a new build cannot be masked by a stale cache; the MIME-poisoning guard is in place; not bumping `CACHE_NAME` is therefore fine. The signed-out page is **deliberately light-only** (`Login.jsx` hardcodes the paper face) — left as-is, and the light bootstrap now matches it exactly.
> ⚠️ **The AUTHENTICATED app was NOT visually verified** — the Chrome extension wasn't connected and the headless browser can't pass Supabase auth (session lives in localStorage, not cookies). The Screener lookup + typeahead have now shipped twice without anyone seeing them render.
>
> **2026-08-10 (feature — ticker typeahead on the Screener lookup)** — owner-asked, same afternoon: the lookup shipped that morning required you to already know the ticker, which mostly defeats its purpose. Someone asking whether a company is halal shouldn't have to answer "which symbol is that" first. **Finnhub `/search` IS on the free tier** — verified against the live production key rather than assumed, which is now the standing habit after the dividend calendar spent months silently returning `200 {}`. Real payloads also settled the filtering rules: `berkshire` returns **BRK.A**, so dotted class shares must survive the symbol filter (`SYMBOL_RE` already allows them); `toyota` returns **nothing**, so name coverage is partial — suggestions degrade to empty and typing the ticker directly still works. New route `GET /api/market/symbols?q=` (auth-gated like the rest of `/api/market/*`), with validation + normalization split into pure `lib/market/symbolSearch.mjs` so it's testable without a network. **6h response cache** (company names are static) plus a 250ms client debounce is what keeps a keystroke-driven feature off Finnhub's 60/min budget — a popular prefix hits upstream once and then serves from memory for every user on the instance. Rate limiting follows the existing `marketdata.ticket` precedent: its own `marketdata.search` bucket so a person typing is never starved by background quote polling.
> **Compliance:** IMPERSONAL — a name matcher is a pure function of the typed string. Results keep the provider's text-relevance order and are **never re-sorted by any judgment** (pinned by a test). Deliberately did NOT attach a halal verdict to each suggestion: a dropdown of names each stamped "Halal ✓" reads as a curated list of things to buy, which is the line. The verdict appears only after the user picks ONE symbol. Combobox keyboard pattern (arrows / Enter / Escape / aria-activedescendant); `sugOpen` is set only by typing so the fetch effect can't re-open the menu over a result after selection. Route wiring verified by direct handler call (401 vs. 404 on an unknown path). **Still not visually verified** — auth-gated. Tests 362 → **376**.
>
> **2026-08-10 (feature — the Screener can screen a ticker you don't own; and the crypto verdict that fix exposed)** — from **real user feedback**, the first work of that class since maintenance mode began: an accountant told the owner he pays an Edward Jones rep to research halal names, and that Mizan removing that middleman — while keeping everything else — would win him. **Mizan couldn't do it.** `AAOIFIScreener`'s `tickers` came from `holdings`, the watchlist was never screened, and `screenTicker` had exactly ONE call site fed by that list — so the app could confirm a position you already held was halal but could not answer "is NVDA halal?" about anything else. `/api/screen` has accepted an arbitrary symbol since it was written, so this was a missing input box, not missing plumbing. The Screener now has a lookup that reuses the existing verdict modal, so a looked-up symbol and a held one are explained identically.
> **Compliance classification (required before any such surface ships):** a screening verdict is **IMPERSONAL / Tier 1** under `lib/compliance/policy.mjs` — the same religious/factual classification for everyone, a pure function of the symbol, never of who is asking — which the policy explicitly permits. **The line to hold: one symbol in, one verdict out.** Ranking, sorting by desirability, or a curated "halal picks" list would convert an impersonal fact into a personalized recommendation and is the RIA line. Do not add one.
> **Latent bug this exposed — `screenSymbol` was auto-blessing crypto.** It short-circuited every ticker in `CRYPTO_RE` (BTC/ETH/SOL/DOGE/ADA/DOT/LINK/AVAX/MATIC/XRP/LTC/BCH) to `status: "halal"` with **every one of the seven standards marked pass** — an answer the ratio engine never computed, since an asset with no balance sheet has no debt ratio. The client masked it by forcing `review` for HELD crypto (`MizanApp.jsx`), but that override keys on the **connector-reported asset type**, which a lookup has no access to: typing DOGE would have returned "Halal", all standards green. Same failure class as the July nisab bug — a confidently-wrong religious claim delivered with full confidence. Fixed at the source: `review`, the scholar-dependent reason the client already used, and `pass: null` on every standard. The client override is now redundant belt-and-braces rather than the only thing standing between a user and a wrong verdict. Per-token verdicts stay BACKLOG N6.
> **Not visually verified** — the Screener is behind auth and no session was available, so the tile was shipped on build + unit tests. Worth a click-through. Tests 356 → **362**.
>
> **2026-08-10 (accuracy + ops — the dividend purification cron had NEVER worked, and the cron heartbeats were racing the runtime)** — found by a routine maintenance health sweep, not a user report. Two independent defects, both of the "reports healthy, does nothing" class.
> **(1) Dividend purification alerts never fired, ever.** `audit_log` holds **zero** `dividend_upcoming` and **zero** `purification.notif_sent` rows for the entire life of the feature. Probing Finnhub with the production key shows why: `/quote?symbol=AAPL` → `200` + live data (key is fine), `/stock/dividend` → `403 "You don't have access to this resource"`, but `/calendar/stock-dividend` — the one the cron used — returns **`200` with an empty body `{}`** for any date range. It is not on our plan and does not say so. `json.dividendCalendar` was therefore always undefined, `items` always `[]`, and `fetchWithRetry` only throws on 5xx/network, so nothing caught it: the cron logged `cron.dividend_check.ok` and wrote an "ok" heartbeat daily while doing nothing. **A 200 that means "no access" is invisible to a status check** — the general lesson, and the same shape as the July nisab failure (a stale-but-plausible answer beats a loud error at hiding itself). Dividend purification is a core promise of this app; a Muslim investor receiving an impure dividend and never being told is the worst failure mode it has. **Fixed by deleting the third-party dependency**: the cron now walks each SnapTrade-linked user's OWN synced activities (14-day window), picks dividends on tickers with a known AAOIFI impurity ratio, and pushes the estimated purification amount. Strictly better data — it fires on dividends actually RECEIVED, with the real amount. This also fixed a second bug in the old path, which pushed every purifiable ticker to **every user with a push subscription** whether or not they held it; it now notifies the holder only. Dedupe moved from ex-date to SnapTrade **activity id** (one `audit_log` read per user, diffed in memory), audit row written BEFORE the push so a retry can't double-notify. `isDividendActivity` is deliberately narrower than the client's display bucketing: a `DFEE` is money out, and **interest is riba** — a different ruling that must not be surfaced as a purifiable dividend.
> **(2) Every cron heartbeat was fire-and-forget.** All five wrote `cron_jobs` as an unawaited `.upsert(...).then(...)` inside a `finally` immediately before returning — a race against the serverless runtime freezing the invocation once the response is sent. The fastest-returning crons lose it most often, which is why `dividend_check` last recorded a heartbeat on **2026-07-18, 23 days earlier**, while `cron.sync` / `cron.cleanup` / `cron.activation` all showed same-morning audit rows and the Vercel schedule was plainly healthy. **The monitoring was partly reporting on its own write races**: `checkCronStaleness` reads this ledger, so a dropped heartbeat is indistinguishable from a dead scheduler. Sharpest case was `botHeartbeat("closed")` firing immediately before the market-closed `return` — the majority of bot-signals invocations, and the one heartbeat with `alert: true` guarding a full-auto strategy that moves real money. All five now go through one awaited helper, `recordCronRun()`; recording failure is logged, never thrown. `cron.dividend-check` also flipped to `alert: true` — it was silent, which is why 23 days of nothing went unnoticed. **Known remaining wart:** `run_count` is still hardcoded to `1` and never increments (BACKLOG F14). Tests 344 → **356**.
>
> **2026-07-31 (region — US-only wall diagnosed + narrowing BUILT, then DEFERRED; ⚠️ NOT DEPLOYED)** — ⚠️ **READ FIRST: production is still US-only.** The narrowing described below was written and tested (374 passing); on 2026-08-01 the owner deferred Canada under MAINTENANCE mode ("since it's just one user… visit this another day"). `/us-only.html` still blocks every non-US visitor in prod. Everything after this sentence describes work that lives on branch **`region/canada-narrowing`** (commit `d1f9495`), NOT on `main` — it sat uncommitted in the working tree for ten days until 2026-08-10, when it was moved to a branch so a stray `git add -A` on `main` couldn't auto-deploy a change the owner had parked. Check that branch out before rebuilding any of it. The *diagnosis* below stands regardless and is the reason to revisit. Owner-asked, from a real user report: a Canadian user hit a full-page "MĪZAN is currently US-only". Cause was `middleware.ts` at the **repo root** (outside `src/` and `lib/`, which is why a scoped grep missed it — noted here so the next sweep doesn't miss it either), added 2026-05-25 `540ffda`, rewriting **every** non-US visitor to `/us-only.html` on `matcher: "/:path*"`. The stated reason was Plaid, but the block was far wider than its cause: Sharia screening, the Zakat worksheet, the Assistant, goals, the watchlist and **SnapTrade brokerage linking** have no US dependency at all — and SnapTrade is itself a Canadian company supporting Questrade and Wealthsimple, so the one connection type that would have worked for a Canadian was behind a wall erected because of the *bank* provider. **Narrowed:** the middleware now blocks nothing and only publishes the edge-geolocated country as the readable `mz_country` cookie; the SPA uses it to render an explanation instead of a dead Connect Bank button. The **authoritative** gate is server-side on `/api/plaid/link-token` + `/api/plaid/exchange`, reading the un-forgeable `x-vercel-ip-country` header and returning `403 REGION_UNSUPPORTED` (client handles it exactly like the existing MFA gate). `DELETE /api/plaid/item` is deliberately **not** gated — someone who linked in the US and moved must always be able to unlink. Eligibility lives once in `lib/geo.mjs`, imported by both middleware and handlers, and **fails OPEN on unknown country** (unresolvable IPs are common and benign; failing closed would cost real US users the integration). **Consequence surfaced, not hidden:** letting non-US users in exposes that Mizan is USD-only and never converts — `/api/snaptrade/all` didn't even carry a currency field. It now does, and `src/lib/region.js` flags non-USD accounts so the Zakat hero + Overview ZAKAT DUE tile warn "MIXED CURRENCY — FIGURES UNRELIABLE" rather than quoting a confidently wrong obligation (same call as "Nisab unavailable"). Unknown currency is assumed USD deliberately — SnapTrade omits it for many US custodians. **Still open: real FX conversion (BACKLOG F10), and whether nisab is evaluated in USD or home currency is a fiqh question, not an engineering one.** `public/us-only.html` is now orphaned (nothing routes to it) — owner to decide delete vs. repurpose. Also corrected a stale BACKLOG claim that no `middleware.*` file existed, which had been load-bearing in the CSP-nonce deferral. **Follow-up same day:** the Portfolio → Tools → **Tax** tab is now hidden for non-US visitors — `TaxPlanner` applies the US 30-day wash-sale rule and a "federal + state marginal rate", none of which is Canadian law (superficial loss rule spanning 30 days *before and after*, 50% inclusion rate, provincial rates, sheltered TFSA/RRSP), so it was wrong advice about money rather than merely irrelevant; both the tab strip **and** the `sub==="tax"` content are gated, since `sub` can already hold "tax". A live query of SnapTrade's `/brokerages` confirmed **Wealthsimple and Questrade are trade-enabled** for Canadians (TD Direct / CIBC Investor's Edge / BMO InvestorLine read-only), so brokerage linking genuinely works — Canada is reachable, not yet fully supported. Remaining Canada gaps logged as **F10–F13, P3, U2, O25–O27**; the highest-risk unknowns are whether Finnhub screens TSX tickers at all (suspected broken → every Canadian-listed holding strands on "review") and whether SnapTrade's currency field matches the shape the new guard reads (if not, the guard silently never fires). Tests 332 → **374** (`src/test/region.test.js`).
>
> **2026-07-30 (feature — in-app notifications; and the permission gate that made alerting dead code)** — owner-asked. Mizan already DETECTED the things worth telling a user about (a holding flipping non-compliant, a dividend landing, a price target crossing) but delivered every one ONLY as a browser `Notification`, behind `Notification.permission === "granted"`. Two consequences: (a) a user who never granted permission — most of them — got **nothing**, and since the detectors `return`ed before doing any work, the dividend path never even seeded `mizan_seen_dividends`; (b) even when granted, an OS toast is fire-and-forget with no record in the app. **Detection is now decoupled from delivery**: events always land in the synced, capped `mizan_notifications` TRACKED_KEY (pure store + detectors in `src/lib/notifications.js`), and the browser toast is an optional extra channel built from the SAME copy so the two can't disagree. Header bell (`NotificationBell`) carries the unread badge and opens the panel; `bell` icon added to `Icon.jsx`. The compliance case matters most — a holding turning haram is the single most important thing this app can say to a Muslim investor, and it was gated behind a browser prompt. The screening baseline now advances only for changes actually REPORTED, so an unreported one stays pending instead of being swallowed. **Caught pre-ship:** `NOTIF_KINDS` routed to Portfolio *sub*-tabs (`screener`/`activity`/`watchlist`) but `setNav` only accepts top-level ids and renders nothing otherwise — tapping a notification would have blanked the page; targets are validated against `NAV_TARGETS` and unknown values drop to null. Tests 301 → **328**.
>
> **2026-07-30 (fix — the client no longer clobbers server-merged history; CONFIRMED in prod)** — the merge below worked (cron 19:22: rktking59 8→36 pts, mussaeed1018 46→60, six users gained a series they never had, all `total`s valid, one legitimately **negative**). Then at **20:07 a browser tab open since before the cron wrote its stale in-memory array back over two accounts**, discarding the backfill — `persistUserState` upserts the whole value, so last-writer-wins loses data on an append-only key. Proven by the row shapes: the merged rows carry the cron's `{date,total}`, the clobbered ones the client's `{date,total,cash,accounts}` at a later `updated_at`. Fixed with **`persistMergedUserState(key, mergeFn, local)`** in `src/lib/userState.js` — re-reads the stored row and merges only this tab's own day; on a read error it stays local-only rather than writing (a blind write is precisely what it prevents). `mergeNetWorthHistory` **moved to `src/lib/netWorth.js`** so client and cron share one definition (`lib/handlers.mjs` imports across the boundary — verified in prod: `/api/admin/db-status` returns 401 not 500, proving Vercel traced the file into the serverless bundle). Also unified the retention cap: the client kept 3650 days and the cron 365 on the SAME key, so every nightly run would have silently trimmed a decade to a year → one `NW_HISTORY_CAP`. Tests → **332**. ⚠️ Owner still needs to re-run the snapshot cron to restore the two clobbered accounts (legacy key untouched, merge idempotent).
>
> **2026-07-30 (accuracy — ONE bank-inclusive net-worth history; the chart no longer disagrees with its own headline)** — found while answering "is the net-worth fix based on Holdings AND Finances history?". It wasn't. There were **two** histories with **two** compositions: the client effect in `MizanApp.jsx` wrote `mizan_networth_history` = brokerage `balance` sum ONLY (what the Overview chart, Goals and PerformancePanel draw), while the nightly cron wrote a separate `networth_history` = brokerage + net bank (read only by the weekly digest). The headline is `tot` = brokerage + bank + Plaid investments + manual assets, and the chart **pins its final point to `tot`** — so every earlier point was understated and the entire non-brokerage balance fell into the range gain. Live deltas at the time: khanstyle02 +$1,505.59, rktking59 −$1,848, mussaeed1018 −$2,794, akhan.industries −$9,859 (card debt). Fixed: composition extracted to the pure `src/lib/netWorth.js` (`netWorthParts`/`hasSnapshotableData`/`isBrokeragePlaid`), called by BOTH the headline and the snapshot effect; the snapshot now records the same bank-inclusive total; the cron writes the canonical `NW_HISTORY_KEY` (= `mizan_networth_history`) and **merges the legacy `networth_history` forward, preferring the legacy bank-inclusive value on overlapping dates** (one-time, self-healing repair of past points); the weekly digest reads the same key (`total`, falling back to legacy `value`). Also dropped the `total > 0` snapshot guard — it silently skipped anyone whose debt exceeds their investments, i.e. exactly the users a net-worth line matters most to; the guard is now "has any data loaded". NB: the `CONTRIBUTION`→`DEPOSIT` parsing fix is brokerage-**activity** only; Plaid transactions do not feed the contribution model. Tests 281 → **292** (`src/test/netWorth.test.js`). **After deploy the owner must click Admin → Maintenance → Net-worth snapshot → Run now once** to trigger the historical merge.
>
> **2026-07-30 (ops — everything the owner had to ask an agent for is now automated + in the admin panel)** — owner directive: "we need everything automated and managed thru the admin panel." Three gaps closed. **(1) Cron staleness was not actually a monitor** — `checkCronStaleness` only ran inside `/api/admin/db-status`, so the "a cron stopped running" alert could only fire while the owner was already looking at the panel. Now also run daily from `/api/cron/cleanup`, and `cron.activation` joined `CRON_DEFS` (26h, alerting). **(2) New config/credential preflight** (`checkConfig` + `CONFIG_CHECKS` in `lib/anomaly.mjs`, detector #6) — the class of outage no test or build can catch, all three of which happened: `ALERT_FROM` on an unverified domain (Resend 403'd every email for days), Anthropic out of credits (Assistant down), `CRON_SECRET` unset (fail-closed → whole fleet 401'd). Checks env presence for `CRON_SECRET`/`ENCRYPTION_KEY`, verifies the `ALERT_FROM` domain against the live Resend `/domains` list, and spends **one** Anthropic token to prove the key is both valid AND funded (presence proves nothing — that was the 07-21 failure). Surfaced as an admin "CONFIG & CREDENTIALS" tile + emailed daily by cleanup. **(3) Activation is now self-serve** — `/api/admin/users?activation=1` returns per-user `connected`/`bank`/`brokerage`/`nudged_at`/`activation_state`, so "who hasn't connected, and who did the cron email?" is a column in the Users table instead of a hand-written SQL query; `POST /api/admin/users/:id/nudge` sends the same email to one person for the cases the cron deliberately skips (active-but-unconnected = usually blocked, a human call). Email copy extracted to `activationNudgeEmail()` and the filter to the pure exported `activationState()` so cron and manual paths cannot drift. `buildAudience()` gained an opt-in 60s memo — the panel loads `/stats` and `/users` together and would otherwise hit SnapTrade once per user twice per refresh. Tests 257 → **281** (new `src/test/adminAutomation.test.js`, 24 cases replaying the exact ALERT_FROM and out-of-credits failures).
>
> **2026-07-27 (security — at-rest encryption ACTIVATED in prod; owner set `ENCRYPTION_KEY`, agent ran the backfill; O24 SnapTrade half DONE)** — the 2026-07-23 session's 5 commits (tour + F7 + hygiene/security) DEPLOYED to `app.mizan.exchange` (READY, prod 200, auth gates 401) and the landing "What's New" ticker gained a guided-tour entry (`mizan-landing`). Then encryption was activated: owner set `ENCRYPTION_KEY` in Vercel Prod; a fresh git build (`c756f79`, which also shipped an owner-only `POST /api/admin/encrypt-backfill` route + Admin→Maintenance "Encrypt stored secrets" button) bound it (`ENC_ENABLED` true). The owner clicked the button → **all 10 `user_snaptrade` secrets encrypted** (per-row encrypt→decrypt round-trip verified in-route, 0 errors). A guarded agent pass then confirmed the plaintext `snaptrade_user_secret` column is **NULL for all 10 rows** (0 plaintext remaining) — the encryption-enabled write path had already nulled them on normal secret writes since the key bound. **SnapTrade secrets are now AES-256-GCM at rest with zero plaintext.** `extractUserSecret` reads ciphertext-first. **Remaining:** (a) drop the now-empty plaintext column (migration 017) after a ciphertext-only code change (`SNAP_SECRET_COLS`/`extractUserSecret`/`encryptedSecretFields` still name it) — LOW priority; (b) **Plaid `access_token` STILL plaintext** — needs a NEW migration (ciphertext cols on `plaid_tokens`) + encrypt/decrypt wiring; (c) `user_state` still plaintext. Also still owner-only: rotate `CRON_SECRET`. Supersedes the 2026-07-12 "dormant / plaintext" finding.
>
> **2026-07-23 (session — interactive onboarding tour + backlog loose-ends + hygiene/security; owner-asked "run multiple agents, build the walk-through, upgrade the code + remove dead code"; committed, deploy per owner)** — parallel multi-agent session: 3 isolated-worktree agents (walk-through / backend / dead-code) + integrator, disjoint file ownership, each gated on `npm run build` + `npm run test` before commit. **Shipped:** (1) **Interactive spotlight product tour** (`src/components/GuidedTour.jsx`, new; replaces the old static `FeatureTour`) — takes a guided lap through all six tabs (spotlight the nav-dock button → `onNav` → spotlight a real on-tab element, with a rAF mount-poll + graceful nav-button fallback), opens with a **"Tour with sample data" vs "Tour my account"** choice (sample flips demo ON for the tour then restores the exact prior state; a **module-load guard scrubs demo + demo caches if a mid-tour tab-close orphans the `mizan_tour_active` marker**, so demo can never persist for a real user), gentle one-time `TourNudge` toast on Overview (yields to onboarding + name nudge), launchable from the `?` dock / Cmd+K palette / Settings "Replay tour". `data-tour` anchors across `MizanApp.jsx` (nav buttons, net-worth tile, Screener/Zakat tabs, advisor input, connect CTA) + a `dataTour` passthrough on `BentoTile`; new synced `TRACKED_KEY mizan_tour_seen`; a11y (role=dialog, focus mgmt, Esc, ←/→, `prefers-reduced-motion`). Self-review caught + fixed an `onNav`-in-deps re-navigation bug (`onNavRef`). (2) **F7** — shared Finnhub news (5m)/earnings (30m) response cache + retry-aware backoff (`lib/handlers.mjs`, `src/test/finnhubCache.test.js`). (3) **Security** — `lib/fetchWithRetry.mjs` now strips query strings (API tokens like Finnhub `?token=`) from retry/failure logs (fixes a token-in-logs leak affecting every caller); the 3 `lib/anomaly.mjs` detectors that discarded the Supabase `error` now surface it (a failing query no longer silently disables a detector). (4) **Dead-code removal** — `FeatureTour`, `EarningsWidget`, `BotDashboard`, `NotificationsPanel` (MizanApp.jsx) + unused React imports / `CADENCE_DAYS` / `UNIVERSE_SYMBOLS` / `getLogContext` / `withTiming` (repo-wide), all grep-proven zero-ref; lint warnings 40→37. **Stale-doc corrections (verified against code):** BACKLOG **F2** (bot-cron duplicate-execution races) was ALREADY fully fixed in prod (`0f12c1f`: atomic `updated_at` CAS lease + terminal ambiguous-failure state + `botLease.test.js`) — only F7's cache was actually missing; BACKLOG **F4** (day-weighted Modified-Dietz return) ALREADY shipped (`6f3db36`: `modifiedDietzReturn` + 121-line test). **Verification:** integrated build 0 errors, **242/242 tests**. **Encryption Step-0 (owner follow-up O24):** verified it is SAFE to set `ENCRYPTION_KEY` — `extractUserSecret` decrypts when ciphertext is present and falls back to the plaintext column otherwise, so the mixed window won't break; `scripts/encrypt-existing-secrets.mjs` exists; migration 017 self-guards against dropping un-migrated rows. **Plaid token encryption needs a NEW migration** — 016 added ciphertext cols only to `user_snaptrade`/`user_keys`, not `plaid_tokens`. Still owner-only: set `ENCRYPTION_KEY` (then I backfill + apply 017), rotate `CRON_SECRET` (Vercel env + Supabase Vault `cron_secret` must match + fresh build). **Repo hygiene:** the sensitive `Mizan Trade DD.md` + 11 stale untracked hyphenated email templates were moved OUT of the (public) repo working tree to an off-repo archive; `reauthentication.html` restored to HEAD (the working-tree copy was an older regression).
>
> **2026-07-22 (feature — first/last name on every user, owner-asked; migration 026 APPLIED to prod)** — owner: users are unrecognisable by email alone in the admin portal. **Schema:** `026_user_names.sql` adds nullable `profiles.first_name` + `last_name`, rewrites the `handle_new_user` trigger to copy them out of `auth.users.raw_user_meta_data` on signup, backfills anything auth already knew (`first_name`/`last_name`/`full_name`/`name`, single-word full names → first only), and indexes `(lower(last_name), lower(first_name))`. **Applied to prod via MCP + verified** (trigger reinstalled, `first_name` present in its body). Backfill found **nothing to fill — all 11 existing users had no name anywhere in auth metadata**, so all 11 get the in-app prompt. **Collection is two-sided:** (1) sign-up now takes First/Last (`Login.jsx` → `signUpWithPassword(email, password, meta)` → `options.data` → trigger), so no new account can start nameless; (2) every pre-026 account gets the **`NameNudge`** — gated on the server's own `needs_name` flag (client never re-derives "filled in"). **Owner explicitly chose gentle over forced:** it is a **bottom-right glass toast on the Overview tab only**, never a blocking modal, never covers the app, and defers to the onboarding modal so a fresh user never sees two prompts at once. It reappears once per app load until answered. **"Not now" is offered for the first 3 dismissals** (`NAME_SKIP_LIMIT`), after which the toast stays put with no skip button — but still never blocks. The skip count is a **synced `TRACKED_KEY` (`mizan_name_prompt_skips`)**, so 3 skips are per-USER not per-device (hydration completes before `MizanApp` mounts, so the `useState` initializer reads the synced value; `"3"` is valid JSON so it survives the persist/hydrate round-trip intact). **Backend:** `POST /api/user/profile` (auth'd, trims/collapses whitespace, 60-char cap, letters/spaces/hyphens/apostrophes only via a Unicode-property regex so Arabic/accented names pass, **service-role write** — `profiles` has no user UPDATE policy and adding one would expose `is_root`/`suspended` on the same row; audited as `user.profile.name_set`). `/api/user/features` now also returns `first_name`/`last_name`/`needs_name` off the same row read it already did — no extra round-trip. Rate limiting is the existing path-matched global gate (`default` bucket, same as `/api/user/email-digest`). **Surfaces:** admin Users table gains a **Name** column (unset renders a gold `NOT SET` so gaps are obvious), and name-over-email now labels admin **message threads** (list + header), the **audit log** User column, and the **broadcast** recipient picker; Settings → Account gained a name editor and the profile hero shows name with email beneath. **Verification:** `npm run build` + crash-lint green (0 errors), **234/234 tests pass**, and the new endpoints were exercised **end-to-end against the real prod schema** with a genuine minted user JWT (admin `generateLink` → `verifyOtp`, no email sent) — valid save returns normalized `"  Test  Given "`→`"Test Given"`, missing last name → 400, `<script>` → 400, `Aḥmad Al-Farūqī` → 200, and `/api/user/features` then reported `needs_name:false`. The test row was **reset to NULL afterwards** (re-verified: 11 users, 0 named) so the owner still sees the nudge. **Then the whole UI lifecycle was driven headlessly in Playwright against a disposable QA account** (the owner's own account has TOTP MFA, so a minted magic-link session is AAL1 and can't reach the app UI): nudge appears → "You can skip this 3 more times" → skip → dismissed + count persisted 1/2/3 (correct singular at "1 more time") → **4th load has NO skip button** and reads "Please add your name to continue using Mīzan fully" → hidden on the Goals tab (Overview-only) → save → gone, and **still gone after reload**. Zero page errors. The same run proved the **migration-026 trigger** for real: a user created WITH `user_metadata` landed in `profiles` as `Yusuf`/`Rahman` automatically while one created without stayed NULL — i.e. the sign-up path works end-to-end. **Both QA accounts were hard-deleted afterwards** (`profiles` back to 11, zero `mizan-qa%` rows). **Dev-server note:** `server.js`'s API allowlist was missing `/api/user/*`, so those routes returned index.html for GET and 404 for POST in local dev, silently no-opping anything gated on them — **added `/api/user/` to the allowlist**. The one remaining local 4xx is `/api/audit` (also unproxied in dev, pre-existing, fine in prod via the Vercel catch-all).
>
> **2026-07-20 (prod email fix + Speed Insights + in-app messaging, owner-asked follow-ups to Broadcast; commits `13bb94a`/`1b732a5` + Vercel env)** — three things surfaced while dogfooding Broadcast: **(1) PROD EMAIL WAS SILENTLY BROKEN — `ALERT_FROM` had regressed to the unverified `alerts@omni-flow.net`** (the owner's *other* company's domain, exactly what memory [[email-sender-domain]] warns against), so Resend `403`'d EVERY app email — invites, weekly digest, Plaid re-auth, bug receipts, broadcasts — for an unknown window. Surfaced as Broadcast's `0 sent · 1 failed`; the real reason was only in Vercel logs (`user_email.resend_failed`). Only `mizan.exchange` is verified in Resend. **Reset to `MIZAN <alerts@mizan.exchange>` via the Vercel REST API as `type:encrypted`** — the current CLI `vercel env add` creates **write-only `sensitive`** vars that read back empty (`pull` and API `decrypt=true` both return `""`), so they can't be verified; POST `/v10/projects/mizan/env` with an explicit `value` is deterministic and readable. `ALERT_FROM` is a single point of failure for the whole pipeline — check its domain vs `api.resend.com/domains` first if email "isn't arriving." **(2) Vercel Speed Insights** — `@vercel/speed-insights` mounted in `src/main.jsx` via the **`/react`** entry (NOT `/next` — this is a Vite SPA); script + beacon are same-origin (`/_vercel/speed-insights/*`) so the vercel.json CSP (`'self'`) needed no change; verified `script.js` 200 on prod. **(3) In-app two-way support thread** (owner has no inbound mailbox, so the app itself is the contact channel) — **migration 025 `messages`** (bigserial, `sender` user|admin, `read_at`; service-role writes, RLS select-own like `plaid_transactions`; **applied to prod**) + 4 routes in `handlers.mjs`: `GET/POST /api/messages` (user's own thread; POST emails the operator with `reply_to`=user) and `GET/POST /api/admin/messages` (operator lists threads with unread badges, opens one, replies; POST emails the user; reads clear the other party's unread same-request). UI: **Settings → Messages** (`UserMessagesPanel`) + **Admin → Messages** (`AdminMessagesPanel`, thread list + conversation + reply, support-only compliance note). Also: `sendUserEmail` now sets `reply_to = OWNER_EMAIL` (opts.replyTo override) so any email reply reaches a real inbox; the Broadcast reminder copy was re-pointed from "reply to this email" → "Settings → Messages". **Verification:** build + crash-lint green; all 4 message routes prod-verified **wired + auth-gated** — 401 unauth for GET **and** POST (an earlier "500" was a curl shell-escaping artifact that mangled the JSON body → `JSON.parse` threw in `api/[...path].mjs` before the handler; a clean body returns 401). The authenticated happy-path is unverified from here (no session to log in with) but uses the same `verifyUser` pattern as the working bug-report/invite routes. Deploys READY on `app.mizan.exchange`; owner testing live.
>
> **2026-07-20 (feature — Admin Broadcast / re-engagement email panel, owner-asked; email-only, logs to `audit_log`)** — owner asked for a way for Admin to send users reminders/emails after a connection audit found **only 4 of 9 registered users have connected anything, and 2 of those are the owner's own accounts** (5 registered-but-unconnected: samirwali20, mmfarooki, cbak605, ahmedawan.5418, jahlilb04; live SnapTrade pull + `plaid_tokens`). Chose **Tier 1 (one-way admin broadcast)** over a two-way chat (disproportionate build + support + RIA/advice-line risk for a free-text admin↔user channel). **Shipped:** new owner-only **Admin → Broadcast** sub-tab (`BroadcastPanel` in `MizanApp.jsx`, pre-seeded with the "connect your accounts" reminder copy — segment selector All/Not-connected/Connected/Custom with live counts, recipient chips + custom checkbox picker, compose form, inline approximate preview, **"Send test to myself"** for the exact branded render, one-way compliance banner) + two routes in `handlers.mjs`: **`GET /api/admin/broadcast-audience`** (per-user connection status — bank from `plaid_tokens` DB, brokerage from a **live SnapTrade `/accounts` call per user**, best-effort, bounded to 5 concurrent + 6s timeout each; a SnapTrade error → brokerage `null`/unknown → user treated as *connected* to under-nudge rather than mis-email someone linked) and **`POST /api/admin/broadcast`** (takes explicit `userIds` — **never** client emails — and resolves addresses server-side from `profiles`; `testToSelf:true` routes to the calling admin only; always skips suspended; caps 500 recipients; sends via the branded `sendUserEmail` shell; writes **ONE** `audit_log` summary row `action="admin.broadcast"` with sent/failed/skipped + recipient list). Both gated by the existing `requireAdmin()` (root/OWNER_EMAIL). Per owner: **email-only** (no web push), **`audit_log`** (no new table → no migration). **Verification:** `npm run build` + crash-lint green (0 errors); `node --check` + ESLint on `handlers.mjs` clean. **NOT locally end-to-end testable** — `server.js` dev routes only `/api/snaptrade|finnhub|polygon|export|advisor` to the handler; **all `/api/admin/*` run only through the Vercel catch-all** (`api/[...path].mjs`), so the new routes behave identically to the existing (working) `/api/admin/invite` locally (GET→SPA, POST→404). Owner testing live on their personal (root) account. **Note:** the audience endpoint does live SnapTrade calls on each load — fine at 9 users; add a cache before it scales.
>
> **2026-07-15 (audit + 2 trust fixes + demo rescale, owner-asked "is anything wrong / make Mizan successful"; commits `570d18f`/`1f8f0a7`/`107763e`, deployed READY + prod 200)** — ran a 3-front audit (security, compliance/RIA, financial-math correctness) via parallel agents. Two confirmed defects fixed, both trust-critical for a Sharia-compliance product: **(1) Demo persona leaked into REAL users' net-worth history** — the client daily net-worth snapshot effect in `MizanApp.jsx` wrote `mizan_networth_history` to localStorage AND Supabase `user_state` with NO `demoMode` guard, so a real user toggling demo persisted the ~$41M/$55M demo persona permanently (the sibling intraday effect was guarded; this one missed). Fixed with `if(demoMode)return;` + dep-array; `mizan_bank_balance` effect got a raw-flag guard (TDZ-safe). **Existing corruption cleaned** (owner-approved, read-only-verified first via Supabase MCP): 5 of 7 users with history had demo-scale (≥$1M) entries — DELETEd 3 all-demo rows + stripped 1 each from 2 mixed users; post-clean max net-worth value anywhere = $32,863 (was $55.5M). **(2) Sharia screening failed OPEN to "halal"** — in `lib/sharia.mjs`, when debt couldn't be read (no D/E ratio + no matching concept) it collapsed to `0`, passing the leverage test at "0.0%" → false "halal" (the worst output this product can produce). Now fails CLOSED: `bsConceptOrNull` + a `debt=null` UNKNOWN sentinel; `evaluateAgainst` treats unknown debt as `pass:null` → standard unverifiable → verdict "review"; `der===0` stays a known zero so genuinely debt-free names still pass; verdict exposes `debtKnown`. Scope = debt only (fail-closing non-permissible-income would flip every Finnhub-screened stock to "review"). Bot gate unchanged per owner (trades halal+review, blocks haram). First-ever engine tests in `src/test/sharia.test.js` (`verdictFromFundamentals`/`evaluateAgainst` now exported). **(3) Demo persona rescaled ~$55M → $435,481** (owner-asked; the 9-figure persona read as a mockup) — the ENTIRE persona (accounts 11→6, bank, manual assets, transactions, sadaqah) rescaled to an internally-consistent upper-middle-class Muslim household, verified from file literals ($264k brokerage + $58k bank + $113k manual); balances still satisfy the cash+positions invariant. Build + crash-lint green (0 errors); **231/231 tests pass** (+9 sharia); 2 deploys READY on `app.mizan.exchange`. **Follow-up SAME DAY (`890d147`) — RIA exposure CONFORMED in code** (owner chose compliant-not-RIA, keep both features working for beta): the bot **NL strategy generator** (`/api/bot/strategy/nl`) no longer invents tickers (structures user-named symbols only; open-ended asks → impersonal Halal-Bogleheads presets) and the **Rebalancer** was reframed from "SELL — trim" imperatives to neutral your-target math; both carry a self-directed disclaimer. Beta users keep LIVE semi-auto (they approve every real order = self-directed = compliant); full-auto stays owner-allowlist-only. **Still OPEN (owner-only actions, logged BACKLOG O24/O17/O23):** at-rest encryption dormant (`ENCRYPTION_KEY` unset → SnapTrade/Plaid secrets plaintext) — owner must set the key; no named Sharia scholar/board; optional securities-counsel review of the self-directed framing. See [[compliance-box-and-networth-history-deltas]], [[sharia-screening-source-of-truth]], [[demo-balance-invariant]].
>
> **2026-07-15 (feature — price charting + the compliance boundary in code, owner-asked; commits `5af2163`→`84e1710`, deployed READY + verified)** — owner asked to "remove market sector and add" a production price chart + a **compliance boundary enforced in code** drawing the read-only line at *personalization of advice*, not trade execution. The spec assumed Next.js/TypeScript; recon confirmed the real stack (Vite/JSX SPA + `lib/handlers.mjs` catch-all) and the plan was adapted with owner sign-off on 4 decisions: remove the **TradingView S&P-500 heatmap** (kept the portfolio Sector Allocation donut); feed the chart from the **existing Polygon `/api/polygon/bars`** (Finnhub free tier has no `/stock/candle`); **add `lightweight-charts`** (v5, MIT, dynamic-imported → its own ~63KB-gz async chunk); do both deliverables. **What shipped:** (1) removed `MarketHeatmap` (last third-party script embed on the app surface). (2) **New IMPERSONAL market-data layer** — `GET /api/market/candles` + `GET /api/market/quote`, auth-gated (`verifyUser`→401 before any upstream fetch), validated (`lib/market/candles.mjs`: symbol regex, resolution whitelist, bounded window), whose responses are a **pure function of the request** (no userId in the pipeline). Extracted a shared `getPolygonBars()` (24h `polygon_cache` + `fetchWithRetry` backoff + stale-on-failure) now used by both the new route AND the unchanged `/api/polygon/bars`; sub-day resolutions key their cache by multiplier so they can't collide. `/api/market/*` → tight `marketdata` rate bucket. (3) **`PriceChart.jsx`** (holdings accordion) — candlestick+volume, 7 timeframes, crosshair, ResizeObserver, theme-reactive, loading/empty/error+retry, a11y (aria-label + sr-only table), optional 12s live-quote line polled only while visible; neutral SMA/EMA/volume toggles labeled "DATA, NOT SIGNALS"; **no buy/sell/target/signal UI** by construction. (4) **Account-servicing holdings overlay** (`holdingsOverlay.js`) — the user's own executed BUY/SELL as neutral markers + a "Your average cost" line, privacy-gated, no judgments. (5) **Compliance layer (the core deliverable)** — `lib/compliance/policy.mjs` (three tiers: impersonal / account-servicing / prohibited + prohibited-pattern list + `assertImpersonal`), `advisor-prompt.mjs` (hardened server-owned `/api/advisor` system prefix reframed from "give actionable answers" to **explain-don't-recommend**, with few-shot refusals; replaces the inline prefix, keeps caching), `advisor-filter.mjs` (deterministic post-generation backstop that rewrites personalized-advice output to the compliant redirect, sentence-aware negation guard so the model's own refusals survive, immutable body rewrite preserving tool_use, every flag audited as `compliance_filtered`). (6) **Tests + docs** — `src/test/market-endpoints.test.js` + `src/test/compliance.test.js` (**+57 tests, full suite 222 green**), `docs/COMPLIANCE.md` (tiers, why the chart is impersonal, why the overlay is account-servicing, the prohibited list, and that this is an ENGINEERING guardrail pending legal review + RIA registration — not legal advice). Build + crash-lint green (0 errors); **222/222 tests pass**. **Deployed READY** to `app.mizan.exchange` (7 commits incl. this doc entry). **Verified in prod:** home 200; `/api/market/candles` 401 unauthenticated; a fresh-context headless shell smoke = 0 console/page errors with the lightweight-charts chunk correctly deferred off the login page. **Data path verified for real holdings** — SPTE (held on `akhan.industries`, the 2026-06-30 bot fill), SPUS/SPWO/SPSK (DCA basket), HLAL each return 501 real daily bars → 501 normalized candles; **owner confirmed the in-app render** (candles + dashed "Your average cost" line + "Bought 2" SPTE marker). Landing "What's New" ticker updated + live (`mizan-landing` `94ae35b`). CSP cleanup DONE (2026-07-15, owner-directed): now that `MarketHeatmap` is gone, all TradingView origins were removed from the vercel.json CSP — `https://s3.tradingview.com` from `script-src`, `https://*.tradingview.com` + `wss://*.tradingview.com` from `connect-src`, and `https://*.tradingview.com` from `frame-src` — leaving `https://cdn.plaid.com` as the only external `script-src` origin (JSON validated); the CSP-nonce backlog item now has only Plaid Link as an inline-injection concern. **Standing requirement (owner-directed, 2026-07-15):** every new grounded-AI feature must clear a **compliance-tier review gate** before shipping — classify its output into the three tiers in `lib/compliance/policy.mjs` (impersonal / account-servicing / prohibited), route it through the hardened advisor prompt + `advisor-filter.mjs`, and confirm it emits no personalized buy/sell/hold recommendation or suitability judgment.
>
> **2026-07-15 (fix — Robinhood / single-account CSV dedupe + recovered dropped cash txns, user-tested; commit `4a67109`, deployed READY)** — follow-up to `8cdb193` after the owner tested with a **real Robinhood + Fidelity export**. Method: extracted `parseCSV`/`fingerprintRow`/`dedupeActivities` into a Node harness and ran BOTH real CSVs through them (the app can't be driven headless). Two gaps surfaced: **(1) Robinhood/Coinbase exports have NO account-number column** (`account=null`), so the `8cdb193` `broker#last4` fingerprint could never collapse a CSV import onto the *renamed* SnapTrade feed → dupes survived (Fidelity worked only because its CSV carries `Account Number`). Fix = a **second, account-BLIND but number-COMPATIBLE dedupe pass** (`looseKey` + `importMatchesReal`): an `_imported` row drops when a real SnapTrade txn matches `broker+date+symbol+units+amount` AND (one side lacks a number OR the last-4 agree). It only ever removes imported rows and never mis-merges two numbered accounts at one broker (harness-verified: `#3091` vs `#8063` identical-txn stay separate). Wired into the live merge (`fetchSnapHoldings`) AND the "Dedupe history" button (`dedupeImports` pass 2). **(2) `parseCSV` silently DROPPED real cash transactions** — Robinhood `RTP` (instant-transfer deposits, e.g. a **$150**), `GOLD` (Gold-subscription fee); Fidelity `Electronic Funds Transfer Received` (a **$50** deposit) — all fell through `inferType` and were skipped, understating cash-in / flow (which feeds XIRR + net-worth-from-activity). Added the trans-code mappings (in/out by amount sign); non-cash `SPL` (split) / `Realized Gain/Loss` stay intentionally skipped. Harness-verified on the real files: Robinhood parse **89→93 rows**, Fidelity **212→213**; the $150/$50 deposits now import; Robinhood cross-source dedupe **COLLAPSES** with AND without a SnapTrade number. Build + crash-lint green; deploy READY on `app.mizan.exchange`. The importer **auto-detects the broker** (`detectBroker`: `Trans Code`+`Activity Date`→Robinhood). **Known residual:** dividend-reinvestment micro-lots can round *quantity* differently between CSV and SnapTrade, so a few fractional rows may not collapse (flagged to owner). Owner clears already-cached dupes via **Sync + "Dedupe history"**.
>
> **2026-07-14 (fix — CSV-import activity dedupe by account number, user-reported; commit `8cdb193`, deployed READY)** — owner reported a CSV activity backfill (a Fidelity multi-account export) didn't dedupe against the SnapTrade-synced feed, so identical transactions showed twice. **Root cause:** the activity fingerprint (`fingerprintRow`, `MizanApp.jsx`) keyed the account on `institution_name` — the *display label* — which is the user's **renameable Mizan nickname** on the SnapTrade side but the broker's **own account name** on the CSV side (`Fidelity — ROTH IRA` vs `Fidelity — <nickname>`). Diverging labels → different fingerprints → the CSV row never collapsed onto the real SnapTrade transaction. Display and dedup were conflated in one field. **Fix — key the account on a STABLE broker account NUMBER (`broker#last4`)** when present, falling back to the full label otherwise (single-account Robinhood/Coinbase exports — unchanged, zero regression). Four surgical touch points: (1) `/api/snaptrade/accounts` now returns the broker `number`; (2) `parseCSV` carries the CSV `Account Number` column onto each row (`account.number`); (3) SnapTrade activities are enriched with that number from the accounts cache (nickname kept for **display**, number used for **dedup**); (4) `fingerprintRow` derives `broker#last4` — rename-immune yet still distinct per sub-account. Matches whether SnapTrade returns the full number or a last-4 mask (node-verified: full & masked both → `FIDELITY#3091`; ROTH vs Default stay distinct). Build + crash-lint green; deploy READY on `app.mizan.exchange` (smoke 200). **Known edge:** if a broker's SnapTrade number is a wholly different internal ID than the statement number, that account's dupes persist → a date+symbol+amount-within-broker fallback is deferred until observed. **Owner action to clear already-cached dupes:** Sync once (re-enriches SnapTrade rows with the number) + hit **"Dedupe history"** (Settings → Import History); new imports dedupe automatically.
>
> **2026-07-14 (trading — enacted the DCA plan on the root account, owner-asked)** — owner asked to "enable the second strategy waiting for funds" + start trading on `akhan.industries` toward a realistic $20–100/mo. **Investigated the live DB first (source of truth over 8–12-day-old memory):** only **2 strategies existed**, both on `akhan.industries` (`b1403200`) → **E\*Trade Individual `302ebba3`** — NOT the memory-described parked DCA basket `0fd266ae`, which had been **DELETED** (gone; do not chase it in future sessions). Reality vs memory: the **breakout `e528f2d7`** (the "mechanics test") was **still `enabled`/`mode=full`** holding 2 SPTE (never retired per the old July-9 checklist), and the **momentum sleeve `43c1b866`** sat dormant. Owner confirmed: account `302ebba3`, **semi-auto**, ~$200 investable. **Actions (all via SQL — no code, no migration):** (1) **Created the DCA core `3de2c0d1`** — `strategy_type=dca`, `mode=semi`, `enabled=true`, Halal Bogleheads basket `params.basket` = SPUS 0.50 / SPWO 0.30 / SPSK 0.20, `dca_amount=$50`/wk, `dca_cadence_days=7`, `capital_allocated=$175`, matching `buildBogleheadsPreset` exactly (verified the preset builder + the DCA executor branch at `handlers.mjs:5980` against live code before writing). Semi → the cron only INSERTS a `pending` signal each week; **no fill without the owner's approval tap** (`isFullAuto` requires `mode='full'`, so a semi strategy never auto-executes even though the account keeps `account_full_auto=true` + owner `full_auto_enabled=true`). (2) **Retired the breakout `e528f2d7`** (`enabled=false`) — but deliberately **did NOT sell the 2 SPTE** (cash no longer scarce + SPTE is halal = the basket's tech sleeve; kept as a long-term hold; a market sell is an irreversible order not to be auto-fired). This removed the only `mode=full` strategy → nothing on the account auto-trades now. (3) **Verified the Trade-tab render path** end-to-end (`GET /api/bot/strategies` = `select(*)` unfiltered by account/enabled; frontend renders `dca` with a "DCA" tag + "holds (no auto-sell)" subtitle at `MizanApp.jsx:6467`; `computeStrategyProgress` is try/caught + guards `target>0` so a `profit_target_pct=0` DCA row can't 500 the list) — **owner opened Trade and confirmed the DCA card shows**. (4) **Cleanup (owner: "remove non-active/unneeded strategies")** — **deleted** the dormant momentum `43c1b866` (no position); **kept** the breakout disabled per owner's explicit choice (deletion cascades `pending_signals` via `ON DELETE CASCADE` → would wipe the 2-SPTE @ $48.81 fill record; owner chose to preserve it). **Honest framing recorded:** $20–100/mo is a **capital** target, not a strategy one — ~$200 = the accumulation phase (dividends a few $/yr, partly purified away); DCA-core buy-and-hold is the realistic path as it compounds. The eToro/Webull-transferable lesson = recurring auto-invest (= this DCA), minus their CFDs/margin/riba-cash. **End state:** 1 active semi-auto DCA + 1 disabled breakout (SPTE record); next weekday-RTH cron tick drops a pending SPUS buy to approve. See [[trading-july9-plan]], [[bot-execution-model]], [[bot-snaptrade-impact-401]].
>
> **2026-07-13 (accuracy pass — figures/Zakat audit, owner-asked)** — owner asked whether every figure links/pulls with a ≤$1–5 delta, especially Zakat. Traced each figure source→surface. **Zakat verified solid** (effectively $0 cross-surface delta): the Overview ZAKAT tile and the Zakat tab call ONE pure `computeZakatWorksheet` (`src/lib/zakat.js`) off the SAME picker selection + worksheet, so they can't diverge; nisab is single-sourced server-side (`/api/metals/spot`), and the client fallback (`nisabFromSpot`) mirrors the server formula line-for-line; 401k/custodial accounts flow in via the derived balance; **74/74 zakat+performance tests pass**. Only residual is intraday price drift (2.5%×drift, refreshes on Sync — balances are anchored to the last SnapTrade sync). Two real defects found + fixed: **(1) Compliance box % inflated (`7051047`)** — the Overview COMPLIANCE tile computed `halalPct = (netWorth − haramEquity)/netWorth`, so all cash / bank / manual assets (gold, real estate) AND every "review"/unscreened holding counted as "compliant"; an all-unscreened portfolio read **"100.0%" over "0 of 8 halal"** and contradicted the Allocation donut right below it. Now = confirmed-halal share of **screened equity** (`halalV/equityTotal`); the %, the count, the donut and the Screener tab all reconcile (one set of `halalV/reviewV/haramV/equityTotal` drives donut + tile); subtitle spells out review + non-compliant counts; color = red only if actually holding non-compliant, green ≥95% halal, gold when the rest is merely pending review, "—" when no holdings. **(2) Net-worth history undercount (`c62a7df`)** — the nightly-snapshot cron summed only `balance.total.amount ?? total_value` (NO cash+positions derivation; SnapTrade-only), so **4 of 8 prod users had $0 net-worth history** while their live headline was non-zero — a discontinuity far larger than the cents-level delta users expect. Now mirrors `/api/snaptrade/all`: derives `cash + Σ(units×price)` for any balance-less 401k/custodial account, and adds the user's net Plaid bank position (`depository − (credit + loan)`, investment excluded to avoid double-counting a broker linked via both providers); total round2'd. Structural caveat noted in-code: **manual assets are client-only (localStorage)** so the server snapshot can't mirror them — history tracks *connected* net worth (brokerage + bank). **Owner-noted pending: Zakat fine-tuning** — app is marketing-ready; owner will iterate Zakat accuracy separately while these ship. Build + lint green; deploys pushed to `main` (auto-deploy). See [[compliance-box-and-networth-history-deltas]], [[zakat-worksheet]], [[demo-balance-invariant]].
>
> **2026-07-13 session changes** (owner-driven; all deployed READY on `app.mizan.exchange`): (1) **Zakat 401k + credit cards (`87cad13`)** — two real defects the owner hit working the Zakat tab. A SnapTrade-linked **401k wasn't populating** the Zakat account picker (and understated Net Worth): `/api/snaptrade/all` set `balance` from `acct.balance.total.amount` only, but several 401k custodians (Empower, Fidelity NetBenefits, Principal) return NO top-level balance on `/accounts` → `balance:0` → dropped by the picker's `balance>0` filter. Fixed by deriving `balance = cash + Σ(position market value)` when no total is reported (mirrors the demo normalizer; the real path had never enforced the invariant). And **connected credit-card balances never reached the worksheet** — the liability rows were manual-only; added a tickable "Connected cards" checklist under "What you owe" (revolving credit only — long-term loans stay in the manual "Long-term debt due" row; shares `excludedAccounts`; `computeZakatWorksheet` gained a `connectedLiabilities` param, wired into BOTH the tab and the Overview tile so they can't diverge). See [[zakat-worksheet]], [[demo-balance-invariant]]. (2) **Recurring Subscriptions tile rebuilt — owner reported it under-counts (`152e8f7`, `15700d7`, `e27a530`)** — verified against his real Plaid data (766 outflows / ~132 days, ~20+ subs, several stopped in May–Jun). Three fixes, all in shared, tested helpers in `lib/recurring.js` used by BOTH the Plaid-native path and the client fallback: **(a)** stop hiding subs Plaid **mis-tags as transfers** — Plaid returns Grok/xAI ($30/mo) and Coinbase tagged `TRANSFER_OUT`; new `isSubscriptionCandidate` only drops `TRANSFER_OUT`/`LOAN_PAYMENTS` for real money-movement merchants (brokerage funding, Zelle/Venmo, remittances). Plus **cadence-aware "stopped"** (`isRecurringActive`, replacing a flat 45-day window) and row cap 25→50. **(b)** `detectFixedPriceSubscriptions` — Plaid's ML doesn't flag subs with only 2–4 charges, so they were never in `outflow_streams` for the filter to rescue; a fixed-price detector (CV ≤ 0.15 across ≥2 months) now SUPPLEMENTS Plaid (the tile UNIONs the two, deduped by merchant). **(c)** `detectUsageBasedSpend` — metered `GENERAL_SERVICES` vendors charged ≥6× over ≥3 months with variable amounts (Anthropic API) surfaced as a monthly **run-rate** with a violet "USAGE" badge. Owner keeps the paid Plaid recurring product as the accurate primary. See [[recurring-subscriptions-classification]].
>
> **2026-07-12 session changes** (all deployed READY on `app.mizan.exchange`): (1) **Goals tab fixes (`625bc90`, `f0f12d5`)** — connection-less users were stuck forever on "Loading goals…": the fetch lived in a `useCallback` keyed off `demoGoals`→`snapAccounts`, and MizanApp passes `visibleAccounts` (an inline `.filter(...)`, new array every render) so the effect re-fired every render and re-set `loading=true`. Fixed by keying the effect on `demoMode` only + a `cancelled` guard. Also: off-brand purple button `#5A3FE0`→navy; savings-goal account picker was including credit/loan accounts (owed balance counted as savings progress) — now `isDebtAcct`-filtered; and the whole Goals sub-tab was rendering `FU` as IBM Plex Sans (`const FU = FP`) instead of **Fraunces** — restored the editorial display face. See [[unstable-prop-effect-refire]]. (2) **Net-worth snapshot write-spam + perf (`e5f3b09`, `d151126`)** — same unstable-`visibleAccounts` class: the daily-snapshot effect ran `localStorage.setItem` + a non-debounced Supabase `persistUserState` upsert **every render**. Memoized `visibleAccounts`/`visibleAccountIds`; lifted `netWorthHistory` to state (was JSON.parsed inline twice per render); `mfaListFactors`→`useCallback`. (3) **Sharia screening RESTORED — was broken for ALL users (`b0fd7e9`)** — Finnhub's free `stock/metric` silently dropped the raw balance-sheet dollars (totalDebt/cash/assets all null), so the AAOIFI ratio engine saw zeros and **every non-haram stock stuck on "review" — no green Halal ever**. Fix (same key): pull the real balance sheet from the still-free `stock/financials-reported`, and derive `debt = (totalDebt/totalEquity ratio, still provided) × book-equity` (robust across REITs/financials where raw debt concepts vary). Validated live vs 12 tickers AND the owner's real portfolio (AAPL/MSFT/NVDA/AMZN/TSLA→halal; O(REIT)/T/F/ORCL/AVGO→haram; JPM/MO/LMT→sector). Applies to all real users (demo uses hardcoded `SHARIA_MAP`). See [[sharia-screening-source-of-truth]]. (4) **Plaid live balances on Sync (`e02dfee`)** — diagnosed "not real-time" = **balances only** (transactions ARE current — webhooks delivering, verified). `/api/plaid/accounts` always used cached `accountsGet`; added `?live=1`→`accountsBalanceGet` wired into the manual Sync (refresh({live:true})). See [[plaid-balance-freshness]]. (5) **ETF Overlap Analyzer moved** Screener→**Portfolio→Tools** (`da75537`). (6) **⚠️ SECURITY/PRIVACY — secrets are PLAINTEXT, encryption is DORMANT** — verified against prod DB: `ENCRYPTION_KEY` was never set, so `ENC_ENABLED=false` and `user_snaptrade.snaptrade_user_secret` (8/8), `plaid_tokens.access_token`, and `user_state` are all plaintext (ciphertext columns empty). RLS protects the app's anon/authenticated API but service-role/dashboard **bypasses RLS entirely**. The landing page's "Admin-blind by design / structurally invisible / secrets encrypted at rest" claims were **FALSE** — corrected the copy (mizan-landing `6ae5b88`) to per-user-isolation + limited/logged operator access + infra at-rest, and corrected CLAUDE.md (`9be8736`). Owner chose to KEEP operator access (encryption-at-rest with a server key, not E2E). **PENDING owner action:** set `ENCRYPTION_KEY` → then backfill SnapTrade + build Plaid encryption + drop plaintext cols. See [[secrets-plaintext-encryption-dormant]]. (7) **Email / invites (BACKLOG F8, DONE)** — invites were Supabase-Auth-native (unbranded) + landing in Spam. Built an app-side branded invite (`POST /api/admin/invite` + Admin→Users form), fixed the branded-email footer/`→`-charset/added a Learn-more button, generated all **6 branded Supabase Auth templates** into `supabase/email-templates/` (+ gen/push scripts), and **fixed the actual spam cause: added the missing DMARC record** to Vercel DNS (`mizan.exchange` is on `*.vercel-dns.com`; `vercel dns add _dmarc …`). See [[email-sender-domain]]. PENDING (optional): owner pushes the auth templates via `scripts/push-auth-email-templates.mjs`.
>
> **2026-07-08 session changes**: (1) **Verified backlog + MAINTENANCE MODE (commits `4749ec0`, `0826dc2`)** — swept the last 7 days of session notes + memory + audit/roadmap docs into **`BACKLOG.md`** (68 items, each **verified against real code** — the sweep reclassified 5 "pending" items as already shipped and 2 as obsolete). Owner decision: **stop self-initiated feature building** — an OPERATING MODE banner at the top of `CLAUDE.md` now restricts future work to user-feedback / real-bug / explicit-ask; parked features go to `BACKLOG.md` (add-and-move-on). See [[maintenance-mode]]. (2) **Fixes F1/F2/F4/F7 (commits `778cdb7`, `6f3db36`, `0f12c1f`)** — **F1**: extracted the Zakat/nisab math out of the monolith into a pure, unit-tested **`src/lib/zakat.js`** (+35 Vitest cases; behavior-preserving). **F2**: closed the lock-less `/api/cron/bot-signals` duplicate-execution races #3–#6 via a per-strategy **atomic claim-lease** on `bot_strategies.updated_at` (new pure `lib/botLease.mjs`, +8 tests, same CAS idiom as `eafae77`) + terminal `'rejected'` resolution for ambiguous full-auto fills — no migration. **F4**: day-weighted **average-capital return (Modified-Dietz/ROAI)** added to `performance.js` + a panel stat (+9 tests). **F7**: routed the 4 Finnhub proxy endpoints through `fetchWithRetry` (429/Retry-After-aware). (3) **Zakat calc unified on a SIGNED bank balance (commit `7403ae7`)** — the Overview ZAKAT DUE tile and the Zakat tab disagreed for nonzero bank balances (Overview added cash but ignored overdrafts; tab deducted overdrafts but omitted cash). `computeZakat` now treats the bank balance with its natural sign (positive cash zakatable + added, negative deducted as short-term debt) and BOTH surfaces route through it. Effect: tab rises for cash holders (fixes undercount), Overview falls for overdraft users (fixes overcount). (4) **TWO runtime crashes fixed + a lint gate to prevent recurrence (commits `81b961b`, `04abbca`, `840f742`)** — the F1 extraction left `negativeBank` referenced in the Zakat-tab JSX but no longer declared → `ReferenceError` **crashed the Zakat & Sadaqah page** (fixed by destructuring it from `computeZakat`). A crash-focused **ESLint gate** was then added (`config/eslint.mjs`, referenced via `npm run lint`, wired into `npm run build` so a `no-undef` fails the build AND the Vercel deploy) — running it caught a **second, pre-existing crash**: the **Dividend Income Planner** referenced `mask`/`fmtUSD` never defined in its scope, so that shipped tool had been throwing on render since 2026-07-05 (fixed). Config lives at `config/eslint.mjs` not the default filename because a global config-protection hook guards `eslint.config.*`. Only crash-class rules (no style noise); 134 Vitest cases pass; all deploys verified READY on `app.mizan.exchange`. See [[eslint-crash-gate]]. (5) **Brute-force IP-block self-lockout FIXED (user-reported bug, commit `207a336`)** — owner reported "Plaid Connection error — Temporarily blocked due to suspicious activity." Root cause was **Mizan's own** guard, not Plaid: `isIpBlocked()` at the top of `_handle()` (`lib/handlers.mjs`) 429'd **every** endpoint for a 24h window whenever an IP crossed the brute-force threshold, and the owner's own IP (`45.31.92.200` — verified via `audit_log`: **458 successful sign-ins** + `admin.run_cron`) tripped it with 5 failed logins in 31s (a mistyped password). Deeper flaw: the client self-reports its own failed logins (`src/lib/auth.jsx` → `/api/auth/track-failure`) and real sign-in is client→Supabase, so the guard gave **~zero protection against real attackers** (they never call the reporter) and **only ever fired on honest users**. Immediate fix: expired the live `ip_block` row in Supabase `security_events` (kept for forensics) → owner regained access with a refresh, no deploy needed. Permanent fix (owner chose "scope + shorten"): the `isIpBlocked` gate now applies **only to `/api/auth/*`** (a valid session always reaches its independently-auth-checked data endpoints — a password typo can't lock a user out of Plaid/portfolio), `BLOCK_HOURS 24→0.5` (30 min), `BRUTE_THRESHOLD 5→10` (`lib/anomaly.mjs`). Build green (lint-gated); deploy `dpl_2kRR…` verified READY on `app.mizan.exchange`, zero downtime. See [[ip-block-self-lockout]].
>
> **2026-07-06 session changes**: (1) **Debt payoff tracker SHIPPED, then made flexible (commits `c1cc295` → `26c5313`)** — new **Debts** section in the Goals tab (`Goals.jsx`), an Origin/Copilot-style tracker where each debt counts DOWN to $0. Three track modes: **manual** (log payments any time), **recurring** (a set amount on a cadence paid from a funding account — autopay auto-counts each period, else one-click "Confirm"), and **balance-linked** (reads a Plaid credit/loan account's live balance). An **`creditor` "owed-to"** field lets a debt be owed to a bank OR a person; **interest-free (qard hasan) is the default**, APR optional (framed for an overdue interest-bearing card). Overview "Savings Goals" widget gained a compact total-debt strip. Persists to `localStorage mizan_debts` (added to `TRACKED_KEYS` — cross-device via `user_state`, **no migration**). Standalone tracker; does NOT feed net worth (linked accounts already counted via Plaid). (2) **Phase 1 — Debt payoff PLANNER (commit `0dcb37e`)** — a `computePayoffPlan()` pure simulation (month-by-month, pays each debt its minimum, funnels an optional extra + freed-up minimums to the priority debt, accrues interest) → debt-free date, total interest, payoff order, inline-SVG **burn-down chart** (no Recharts). Three strategies: **riba-first** (clear interest-bearing debt fastest — Islamic default), avalanche, snowball. Amortization (`monthsToPayoff`) makes interest-bearing debts project a real date. Verified in demo: $400/mo extra under riba-first cut payoff 13y8mo→1y4mo, interest $9,696→$453. (3) **Phase 2 — Performance analytics (commits `87ece9b` + `b7fca08`)** — new Overview **`RETURN & RISK`** panel (`PerformancePanel.jsx`, collapsed by default, shown only when holdings exist) backed by a pure, dependency-free **`src/lib/performance.js`** (16 Vitest cases, 64 total pass): **money-weighted return (XIRR)** (Newton's + bisection, Excel-fixture validated — the IRR Ghostfolio's code stubs out), **realized/unrealized P&L split**, and **risk metrics** (max drawdown / annualized volatility / Sharpe at a 0% riba-free rate, flow-adjusted so deposits don't read as gains, gated behind ≥20 daily snapshots). (4) **Cross-metric number reconciliation (part of `b7fca08`)** — the panel was the first surface to cross-reference deposits, balances, and positions, exposing that **demo account `balance` literals had drifted from `cash + Σ(positions)`** (the real SnapTrade invariant), understating Net Worth ~$13.8M vs Allocation/Market-Value/panel. Now a normalizer after `DEMO_ACCOUNTS` derives each balance from cash+positions (can't drift; demo net worth ~$44M→~$58M, previously mis-stated low). Panel **Total P&L is position-derived** (realized+unrealized) so it equals the Position-P&L tiles AND the hero's Total Return exactly; Allocation total = Market Value + Cash; Cash on Hand = broker cash + bank. All reconcile. (5) **Verified NON-bug: cost-basis-reset** — research had flagged a possible missing reset on full position close. Traced all 3 P&L paths: unrealized uses the broker's per-position avg cost (broker resets on close), YTD-realized is a disclosed avg-cost approximation (Known Limitation #3), and the only self-accumulating book — the bot's `botRealizedPnl` (`lib/handlers.mjs:1301`) — already resets at line 1338. Nothing to fix. (6) **Benchmark roadmap doc (`MIZAN-BENCHMARK-ROADMAP.md`, commit `833696e`)** — source-level study of Origin, Copilot Money, Maybe (→ fork `we-promise/sure`), Firefly III, Actual, Ghostfolio (+paisa/wealthfolio/investbrain) mapping how each models debt/payoff + other features vs Mizan. Key findings: Origin & Copilot have debt *visibility* but no payoff planning (Mizan now beats them); nobody ships true TWR/IRR/risk (so Mizan's avg-cost is industry-normal, and XIRR+risk put it ahead). Tracks Phase 3 (rules engine + Islamic categories + recurring detection → debt-payment auto-linking) and Phase 4 (materialized balance table + reverse calculator — unblocks dense risk curves) as NEXT. All Phase 1/2 deploys verified READY on `app.mizan.exchange`. (7) **Phase 3 (start) — debt-payment auto-detection + linking (commit `0a83a1f`)** — new pure **`src/lib/recurring.js`** (11 Vitest cases): detects recurring outflows from transactions (Actual's find-schedules: normalize payee, ≥2 occurrences, cadence from median gap), adapts Plaid's native `/recurring` streams, and scores/matches a stream to a tracked debt by creditor + amount — the step Copilot skips (it detects recurrings but never links them to a liability). Goals debt cards now show "Detected a recurring $X/mo payment to <merchant> — Link payments"; linking imports the stream's posted payments and a reconcile effect keeps pulling new ones (idempotent via a stable `extId`) so paydown auto-advances from the bank feed — the owner's "sync monthly statements" ask. Recurring-debt autopay is disabled on link (no double-count); Unlink reverts. Real reads `/api/plaid/recurring` → falls back to detecting from `/api/plaid/transactions`; demo uses a Guidance Residential stream matching the Auto Financing debt. **Boundary held per owner:** personal debts live in Goals; credit-card subscriptions/bills stay finance-categorized (Finances untouched). No migration. Verified in demo: detect → link ($14,500→$10,900, 3 payments) → unlink (reverts). *Remaining Phase 3:* rules engine + Islamic budget categories (§16 gap); apply the recurring engine to the Finances subscriptions view. See [[benchmark-roadmap-phases]].
>
> **2026-07-05 session changes**: (1) **ETF Overlap Analyzer SHIPPED (commit `8e786bf`, migration `024`)** — first of a set of "features non-Muslim finance apps have, adapted for halal investors." A new Screener-tab panel compares 2–4 halal ETFs / Amana funds to expose duplicated holdings (the halal fund universe is small + heavily overlapping — verified SPUS vs HLAL = **62.8% overlap across 20 shared names** even on top-25 data, so "diversified across three funds" is often one bet in a trench coat). **`lib/etfHoldings.mjs`** (new): 11-symbol universe = 7 halal ETFs (SPUS/HLAL/UMMA/SPWO/SPTE/SPRE/SPSK) + **4 Amana mutual funds** (AMANX/AMAGX/AMDWX/AMAPX — added at owner's request; he holds them + many seasoned Muslim investors do), curated bootstrap holdings, an Alpha Vantage `ETF_PROFILE` parser, and the pure overlap engine (overlap % = Σ min(wₐ,w_b); equity-vs-sukuk flagged not-comparable). **Backend:** `GET /api/etf/universe` + `GET /api/etf/overlap?symbols=…` (any signed-in user, not trading-gated) with a **cache → Alpha Vantage → curated fallback** in `getHoldingsRecord()`. New table **`etf_holdings_cache`** (migration 024, service-role-only RLS, same pattern as `polygon_cache`). ETFs served by Alpha Vantage `ETF_PROFILE` (free tier **25 req/day** → holdings fetched server-side only + cached ~24h; 7 ETFs = 7 calls/day, well under cap); the 4 Amana funds are curated (AV is ETF-only; MFs report holdings only quarterly via N-PORT). SPUS/HLAL are also curated-seeded so the analyzer works BEFORE the key is set — AV overwrites daily once keyed. **Frontend:** `ETFOverlapPanel` in the Screener tab (`sub==="screener"`) — fund pickers, headline overlap % (amber ≥50% = redundancy warning / navy / slate), shared-holdings table, per-fund source provenance, both themes. **NEW ENV (pending): `ALPHAVANTAGE_KEY`** — owner adding to Vercel prod; until then ETFs beyond curated SPUS/HLAL show "awaiting data source." Verified: overlap engine unit-tested (SPUS×HLAL 62.8%, SPUS×AMAGX 31.4%, AMAGX×AMANX 25.9%, SPUS×AMDWX 0% EM, SPUS×AMAPX not-comparable sukuk); build green; migration applied; deploy READY; both routes 401 unauth. See [[etf-overlap-analyzer]]. (2) **`ALPHAVANTAGE_KEY` went LIVE + verified (commits `13b5b43`, `03d0504`)** — owner set the key in Vercel; a fresh git build bound it. First live run confirmed AV covers the niche tickers with FULL lists (**HLAL returned 210 holdings** from `alphavantage`). Two fixes surfaced by that run: the overlap route now fetches symbols **SEQUENTIALLY** (AV's free tier throttles concurrent bursts, which made the 2nd symbol fall back to curated — SPUS did while HLAL got AV), and a curated row for an **ETF** now expires in **~1h** (`ETF_CURATED_FALLBACK_TTL_MS`), not the 90-day mutual-fund TTL, so a transient throttle can't pin an AV-covered ETF to stale curated data for months. Also: **all 7 ETFs curated-seeded** (permanent fallback if AV can't serve one) + **`normTicker()`** strips exchange prefixes (`AMS: ASML`→`ASML`, `KRX: 005930`→`005930`) so international funds cross-match (UMMA×SPWO catches Samsung/TSM/ASML = 22.1%). GOTCHA: `vercel env pull` shows `ALPHAVANTAGE_KEY` (+ `PLAID_SECRET`) as empty because they're **Sensitive** vars the CLI can't read back — NOT actually empty; verify via `etf_holdings_cache.source`. (3) **TAB REORG — collapsible sections across all 6 tabs (commits `f3717a9` → `4c1b566`)** — owner: long vertical tile-stacks bury features. New **`<CollapsibleTile>`** primitive (MizanApp.jsx, right after `BentoTile`): a BentoTile whose title + one-line subtitle header is ALWAYS visible while the body folds — so the stack of headers is a scannable table-of-contents (discoverability) while views stay short (compactness). Per-section open state persists in `localStorage` (`mizan_ct_<key>`); a **`flat`** variant (header bar, no card wrapper) wraps multi-tile panels without card-in-card. Applied: **Overview** (Top Holdings + Accounts default-open per owner, Market Heatmap collapsed w/ lazy `open`-gated TradingView build), **Settings** (Security/Privacy/CSV/Legal collapse; Connections + Broker Docs open), **Portfolio** (Watchlist + ETF Overlap collapse) **+ sub-tabs grouped 7→5** — top row Holdings · Screener · Activity · Assets · **Tools**, where Tools reveals a slate-accented secondary `TabBar` (Rebalance/Tax/Backtest); `sub` keeps its per-tool values, `topActive` is derived, no render branches changed. **Goals/Zakat** (Methodology + Sadaqah log collapse; Zakat hero + Dividend Purification open), **Finances** (Spending/Debt/Subscriptions analytics collapse w/ month+total in subtitle; bank hero + accounts + Recent Transactions open), **Trade** (Bot Activity + Realized P&L collapse; Refresh buttons moved into body — can't nest a button in the header). Principle: primary stays open, secondary folds under a visible header. See [[tab-reorg-collapsible]]. (4) **Dividend Income Planner SHIPPED (commit `dc37b63`)** — new `DividendPlanner` under **Portfolio → Tools → Dividends** (the Tools group also gained this 5th tool). Forward-projects annual dividend income from a starting balance + monthly contributions + DRIP + price growth over a 5–30y horizon, with the Mizan-unique **gross → minus purification → net you keep** view (only the purified portion is reinvested). Starting yield blends from the user's real halal-fund holdings (`ETF_LIST` published yields) when known. Gross-vs-net area chart + assumptions panel + purification-owed total. (5) **Trading: recalibrated to a REALISTIC DCA-core plan + July-9 activation** — owner first wanted momentum-core targeting $5–20/wk, then recalibrated after realizing his Robinhood +5–10% gains come from **capital + whole shares + holding 2+ yr** (buy-and-hold), NOT trading. Plan flipped to **DCA-core long-term halal accumulation** (primary) with momentum as a **tiny optional sleeve**. Honest expectation: low-double-digit % over 1–2+ yr on invested capital, scaling with deposits — NOT weekly income ($5–20/wk was impossible; ≈$15–25/yr on ~$200). Universe = **broaden to halal STOCKS, keep the Sharia screen** (no code change needed — bot universe already has halal large-caps; affordability filter picks them as capital grows). Dormant momentum `43c1b866` (acct `302ebba3`) had a **crypto-miner universe** (MARA/RIOT/CLSK — NL builder picked non-halal names despite a "halal" description); REBUILT halal + demoted to `deploy_pct:25`, `enabled=false`. July-9 checklist: sell 2 SPTE at break-even + retire `e528f2d7`, create the DCA core (SPUS + SPSK smoother, weekly, hold), momentum optional. See [[trading-july9-plan]]. (6) **Screening wired 100% engine-driven — killed hardcoded halal/haram (commits `c0bfbbc`, `027b9f8`)** — owner caught crypto (DOGE/XRP) showing "halal" on his Robinhood import. Root cause was `mapPosition`'s fallback chain (`live || SHARIA_MAP || crypto?"halal"`), NOT the engine. Fixes: (a) real users' `sh_` resolves ONLY from the live `/api/screen` verdict, else "review"; the demo persona's hardcoded `SHARIA_MAP` is now demo-mode-only (no leak). (b) Crypto is flagged "review" (token-specific + scholar-dependent) by the **connector's asset type**, not a hardcoded coin list — no more auto-halal. (c) The **bot's** halal gate was ALSO hardcoded (`HARAM_TICKERS` blocklist + curated universe, marked `sharia_passed:true` without screening) — now the entry engine screens each picked candidate via `screenSymbol` and blocks a definitive "haram" (audit-logged), while still trading halal ETFs (which screen review/unknown — halal by construction, so it blocks only haram, never review/unknown). `HARAM_TICKERS` is now a cheap pre-filter safety net, not the authority. Screening is now engine-driven end-to-end. See [[sharia-screening-source-of-truth]]. (7) **Security hardening from a read-only audit (commit `1c34aed`)** — a `security-reviewer` pass verified the core is solid (RLS coverage, IDOR scoping, AES-256-GCM secrets, admin gating, Plaid webhook ES256 sigs, no SSRF/SQLi) and found: **CRITICAL** — `/api/advisor` + `/api/advisor/count` had NO auth gate (`verifyUser` ran only afterward for audit), so anyone could bill Claude to Mizan / use it as a free relay → added `verifyUser→401` before any Anthropic call (also tightens the rate bucket to the per-user 60/hr cap). **HIGH** — `npm audit fix` patched form-data (CRLF, via plaid) + opentelemetry (DoS, via sentry) + undici, all prod deps (esbuild/vite left — dev-only, needs breaking vite@8). **MEDIUM** — CSV formula-injection guard in `csvEscapeCell` (=/+/-/@ → prefixed `'`); tight `marketdata` rate bucket (150/hr) for the public Finnhub/Stooq/Polygon proxies + anon now respects `min(action,anon)` caps; gated `/api/alpaca/*` behind `canUseTradingBot` (shared paper account). **LOW** — removed dead `full_auto_enabled` from strategy-PATCH allowlist; constant-time `CRON_SECRET` compare. **Deferred (needs care):** move CSP `script-src` off `'unsafe-inline'` to a nonce-based policy (compounds the localStorage-JWT exposure; CLAUDE.md §8 gates CSP changes). (8) **AI feature #1 — plain-English Sharia-screening explanations (commit `60e86fd`)** — the Screener "Why →" verdict modal now has an "✦ Explain in plain English" button: feeds Claude ONLY the verdict facts (status, industry, AAOIFI ratio tests + numbers vs thresholds, non-permissible income %, crypto asset-type) → warm 3–5 sentence explanation (why, which test, practical meaning). It explains, never re-judges; told never to invent a number. Reuses the auth-gated `/api/advisor`. First grounded-AI feature beyond the chat advisor. (9) **Codex second-opinion review of the whole session (`codex exec review --base 9ee5fa1`, fixes `86bb174`)** — no critical/security findings (advisor auth, rate-limits, bot halal-gate all passed); caught 2 real bugs Claude missed: crypto could still show "halal" from a same-day CACHED verdict (override only ran for stale tickers → now forced regardless of freshness, idempotent) and the ETF Overlap "Retry" was a no-op (`setSel([...s])` left `sel.join(",")` unchanged → added a `retryN` nonce). **NEXT — AI roadmap (memory `ai-roadmap`, tracked as pending tasks #5–9): grounded-AI features on the auth-gated `/api/advisor` — Zakat/purification guidance, proactive portfolio insight card, AI-written weekly digest, per-token crypto screening, security-event explainer. Also pending: July-9 trading activation ([[trading-july9-plan]]); CSP nonce hardening (deferred security).** (4) **Landing-page SEO + competitor intel (`mizan-landing` repo — marketing site, NOT app code; commits `cae8b2c`/`bd01886`/`a6742e5`)** — investigated **joinmizan.com = Mizan Markets LLC** (Floral Park NY; founders Yusuf & Eesa Samad; claims RIA + Alpaca Securities), a *same-name* halal-investing competitor that is **PRE-LAUNCH / waitlist-only** (no product, no App Store, `#`-placeholder contact, broken signup). Countered by SEO-optimizing the landing head to own "Mizan halal" search intent while they're pre-launch: `<title>` now "Mīzan — **Halal Investing** & Sharia-Compliant Portfolio Management" (word "halal" was absent from title/meta before — brand was spelled "Mīzan" with a macron nobody types), meta description leads with plain "Mizan" + "halal investing", added `SoftwareApplication` **JSON-LD** (`alternateName:"Mizan"`), `robots.txt` + `sitemap.xml`, and fixed `canonical`/`og:url`/`og:image` to the **resolving `www.mizan.exchange` host** (apex `mizan.exchange` 308-redirects to www; the old `og:image` on `mizan-landing-one.vercel.app` had started 301-redirecting → broke social scrapers). All verified live. PENDING (owner clicks): submit sitemap in Search Console + re-scrape social debuggers. Brand "Mizan" is crowded in Islamic finance → wordmark likely unregistrable broadly; a cheap USPTO clearance search advised before any TM spend. #3 (halal-investing content pages for ranking) was scoped but **parked** — plan/discuss before build. See [[joinmizan-competitor]], [[mizan-landing-prod-url]].
>
> **2026-07-03 session changes**: (1) **Halal Bogleheads frontend picker SHIPPED (commit `88c559f`)** — completes the "frontend selector = follow-up" left open on 2026-07-02. New `GET/POST /api/bot/bogleheads` endpoint (single source of truth, `canUseTradingBot`-gated): GET returns the `HALAL_BOGLEHEADS_SLEEVES` menu, POST `{account_id, sel}` returns a server-validated `buildBogleheadsPreset`. The Trade builder now shows a "Halal Bogleheads" **Quick Preset** tile with per-sleeve pickers (core US/Intl/Sukuk radios + optional tech/REIT toggles) that builds into the EXISTING `nlResult` review→Activate flow (no new modal). Added a **WEEKLY AMOUNT** field in the review for `dca` strategies (writes `params.dca_amount`) so the basket DCAs per-period instead of lump-summing the full allocation — fixes both the picker and the NL-bogleheads path, which previously left `dca_amount=0` (cron fell back to `capital_allocated` = deploy-all-at-once). **Gating model confirmed & preserved:** the Trade UI + `/api/bot/strategies` + `/api/bot/bogleheads` are gated to trading-enabled users (frontend `isAdmin` = `d.trading_bot`), while `/api/cron/bot-signals` executes for ALL enabled strategies with no admin gate (backend for all users). Owner's DCA `0fd266ae` was converted to the basket (SPUS 0.50 / SPWO 0.30 / SPSK 0.20) but stays `enabled=false` on the read-only custodial `366265e9` — dormant until funds land (~2026-07-08, see [[bot-snaptrade-impact-401]]). See [[bot-execution-model]]. (2) **Persistent auth session RESTORED (commit `9ee5fa1`)** — reverted the 2026-07-01 per-tab `sessionStorage` experiment (`1d19884`). `src/lib/supabase.js` now stores the Supabase session in **`localStorage`** (the SDK default, `persistSession:true`) so a login survives refreshes, new tabs, new windows, and browser restarts, ending ONLY on an explicit `signOut()` (or token expiry with no refresh). Owner's request: "each tab/session stays logged in until the user actually signs out." **Critical:** the one-time `sb-*-auth-token` localStorage purge that the sessionStorage config added was REMOVED in the same commit — it ran on every load and would have wiped the persistent session, logging users straight back out. SW `CACHE_NAME` bumped v10→v11. One-time effect: anyone still holding a sessionStorage-only session re-logs-in once, then it persists. No incognito leak (private windows always get isolated storage). Both deploys verified READY in prod. See [[per-tab-session-storage]]. **Do NOT re-introduce per-tab scoping as a "fix."**
>
> **2026-07-02 session changes**: (1) **Full-auto decoupled from `is_root` → `full_auto_enabled` ALLOWLIST (commit `05e6de0`)** — `canUseFullAuto()` now gates on the owner-controlled `full_auto_enabled` flag (set only via SQL, no self-serve) instead of root; strategy create/edit full-auto layer + the per-account opt-in PATCH now call `canUseFullAuto` (were `isRoot`). Done on the owner's explicit, informed decision to let a permission-granted **family** beta tester auto-trade their OWN account. The in-code COMPLIANCE comment was rewritten, not deleted (keep the allowlist to accounts the owner is personally authorized to trade; revisit before any compensated/commercial arrangement). Allowlist now: `akhan.industries` (root), `khanstyle02` (owner's own secondary — flag was already stale-true, so decoupling ACTIVATED its full-auto), `mmfarooki@outlook.com` (family). See [[trading-bot-beta-access]]. (2) **`mmfarooki` enabled for Trade + full-auto** — `trading_bot_enabled=true` + `full_auto_enabled=true`; still needs consent + a trade-permission reconnect + a funded **non-custodial** account + a `mode='full'` strategy before anything executes. Sent a branded invite email (4 setup steps + pros/cons + explicit no-guarantee-of-profit disclaimer). (3) **Email fully migrated to the verified `mizan.exchange` domain** — was leaking from the old `mizan.app`/omni-flow config. `ALERT_FROM` code default → `MIZAN <no-reply@mizan.exchange>` (commit `970b8cc`); owner set the Vercel `ALERT_FROM` env → `alerts@mizan.exchange` + rotated the Resend key; Supabase Auth custom SMTP sender → `no-reply@mizan.exchange` via Resend. Verified: auth-email links resolve to `app.mizan.exchange` (no omni-flow leak, via admin `generateLink`), app emails send from `alerts@mizan.exchange` (live test to owner + real invite to mmfarooki, both HTTP 200). NO `omni-flow`/`mizan.app` refs remain in `lib/` or `src/`. (4) **Email header logo (commit `9c04238`)** — `renderBrandedEmail` now shows `/logo.png` (navy scales + MĪZAN lockup) instead of the CSS text wordmark, `alt="MĪZAN"` for image-blocking clients; covers all app emails. Note: the site-wide `Cross-Origin-Resource-Policy: same-origin` header blocks the hosted logo in a raw `file://`/browser cross-origin load, but real email clients proxy images server-side (CORP is browser-only) so it renders in inboxes. Per-asset CORP overrides in `vercel.json` did NOT take effect (Vercel serves static assets same-origin regardless of rule order/source — tested 3 ways) and were reverted as moot (commit `e0e5603`); the clean single-rule hardened header config is restored. See [[per-tab-session-storage]]. (5) **Halal Bogleheads DCA basket (commit `7488b5d`)** — the `dca` strategy_type now supports a weighted multi-ETF basket (`params.basket`), the Islamic three-fund lazy portfolio with SELECTABLE per-sleeve options: US (SPUS|HLAL) · International/World ex-US (SPWO|UMMA) · Sukuk (SPSK) + optional tech (SPTE) / REIT (SPRE) tilts. `HALAL_BOGLEHEADS_SLEEVES` + `bogleheadsBasket()`/`buildBogleheadsPreset()`/`bogleheadsSelectionFromText()` in handlers.mjs. Cron buys the most-underweight AFFORDABLE member each period (rebalance via new contributions — whole shares, never sells; tax-efficient + halal). NL builder short-circuits on "bogleheads/three-fund/lazy portfolio" → returns the preset (any option ticker named fills its sleeve); response carries the sleeve menu for a review-UI selector (frontend selector = follow-up). Directly answers Amal Invest's "halal ETFs are expensive/underperform" critique with a low-cost DIY index basket. See [[bot-execution-model]]. (6) **Ethical / BDS screening overlay (commit `6cdecde`)** — optional layer ON TOP of the AAOIFI verdict; never changes `sh_`. `ETHICAL_EXCLUSIONS` (curated divestment-target list) + `ethicalScreen()` in `lib/sharia.mjs`; every `/api/screen` verdict carries `ethical:{excluded,reason,list}`; `mapPosition` exposes `h.bds_`. Screener has an "Ethical/BDS ON/OFF" toggle (pref `mizan_ethical_overlay`, default off) → red "BDS" pill + explainer. FOLLOW-UP: apply to the bot buy-universe. See [[sharia-screening-source-of-truth]]. (7) **Activity tab ↔ Trade tab sync (commit `f2665c2`)** — the Activity tab now merges the bot's executed fills (from `/api/bot/activity`), tagged "BOT", deduped against the broker feed by ticker+side+units within ~4 days so the authoritative SnapTrade row wins once it syncs. Display-only (NOT merged into `snapActivities`, so net-worth/flow/dividend calcs stay broker-sourced). Executed-fills-only by design. (8) **CHANGELOG + auto-regen (commits `4e98ac4`, `10a8059`)** — added `CHANGELOG.md` (Keep-a-Changelog, generated from Conventional-Commit history, grouped by ship date) + `scripts/gen-changelog.mjs` generator + a `.githooks/pre-commit` hook (wired via `core.hooksPath=.githooks`, auto-set by the package.json `prepare` script) that regenerates + stages CHANGELOG on every commit (best-effort, skips rebase/merge; one commit behind by design). Curated narrative stays here in the state-audit.
>
> **2026-07-01 session changes**: (1) **Market-hours gate (commit `bd4c639`)** — the bot only places MARKET orders, which the broker rejects outside RTH (`impact 403` code **1019** "Outside market hours" — the reason a DCA SPUS buy failed firing at 00:00 UTC). New `usMarketStatus()` in `lib/handlers.mjs` (America/New_York wall-clock via `Intl`, DST-safe, + hardcoded 2026–early-2027 market-holiday set) gates `/api/cron/bot-signals` (early-return `skipped:"market_closed"`) and `executeSnapTradeOrder` (`425 market_closed`, defense-in-depth covering the manual approve path). pg_cron still fires `*/15` all UTC hours but off-RTH ticks no-op. Swap for Alpaca `/v2/clock` when keyed (adds holidays + half-day early closes — see [[alpaca-data-pending]]). (2) **DCA blocked by a READ-ONLY (custodial) connection — not code, not funding.** A second DCA strategy `0fd266ae` (SPUS, $100, `mode=full`) was pointed at `366265e9` ("E-Trade - Original", `type=CUSTODIAL`). With all gates armed (`account_full_auto(b1403200,366265e9)=true`, profiles gates true) a live in-hours test failed at symbol resolution: SnapTrade `/accounts/366265e9/symbols` → **402** (read-only connection) → order rejected `422 could not resolve symbol`. Funded ≠ trade-enabled, AND E\*Trade bars API trading on custodial (UGMA/UTMA) accounts (the deeper `1140` wall) — reconnecting can't fix it. **Parked ~1 wk**: DCA strategy set `enabled=false`, custodial `account_full_auto` reverted to `false`; owner is moving funds custodian→bank→an **Individual** E\*Trade account, then repoint `0fd266ae.account_id` there + re-enable. See [[bot-snaptrade-impact-401]]. (3) **First external beta trader** — `mmfarooki@outlook.com` (`fe9f7326…`) granted `trading_bot_enabled=true` (Manual/Semi only; still needs consent + a trade-permission reconnect). (4) **Trade-tab "Brokerage Trading Support" panel (commit `903fec8`)** — new `TradeConnectionsPanel` under the Trade TabBar (any bot-enabled user): a reconnect-with-trade-permission notice (read-only connections can't place orders) + "Reconnect for trading" button (opens the trade-mode ConnectModal) + a collapsible per-broker capability matrix (Robinhood trade+fractional, E\*TRADE/Schwab whole-shares, Fidelity read-only, Coinbase crypto-only, custodial UGMA/UTMA can't API-trade). (5) **Per-tab auth session (commit `1d19884`)** — the Supabase session moved from localStorage → **`sessionStorage`** (`src/lib/supabase.js`) so a NEW tab/window starts logged out (localStorage is shared across a browser profile's tabs; sessionStorage isn't). One-time purge of any stale `sb-*-auth-token` in localStorage; SW `CACHE_NAME` bumped v9→v10. Logs every existing user out ONCE on next load. Verified end-to-end via Playwright against prod (tab1: session in sessionStorage, none in localStorage; tab2 new tab: no session, login screen). See [[per-tab-session-storage]].
>
> **2026-06-30 session changes**: (1) **Live full-auto trading went active on a funded account** — `akhan.industries` (is_root) connected a regular **E\*Trade Individual** account (`302ebba3…`, $100 settled cash + **$100 buying power**) after the prior custodial account (`366265e9…`) stayed `buying_power=$0` (E\*Trade blocks API trading on custodial/UTMA accounts). Wired full-auto to it: strategy `e528f2d7` (SPUS breakout, `mode=full`, `position_size_pct=100`) targets `302ebba3` + `account_full_auto(b1403200,302ebba3)=true`. All gates pass → `isFullAuto=true`. (2) **bot-signals now fires every 15 min on weekdays** via a new GitHub Actions workflow (`.github/workflows/cron-bot-signals.yml`, `*/15 * * * 1-5`) hitting `/api/cron/bot-signals` with the `CRON_SECRET` bearer — Vercel Hobby caps native crons at daily, and the repo is public so Actions minutes are free. Verified end-to-end (HTTP 200, strategy evaluated). vercel.json keeps the daily `0 14 * * 1-5` entry as a backstop. (3) **Brokerage cash + buying power fix** (`/api/snaptrade/all`) — was reading `acct.balance.cash.amount`, which most brokers (E\*Trade, Robinhood) don't return (cash + buying_power live on `/accounts/{id}/balances`); funded accounts showed **$0 cash on Overview** and $0 buying power in the Trade panel. Now fetches `/balances` per account in parallel and sources both from there (live on the real-time plan). (4) **SnapTrade force-refresh toast fix** (`/api/snaptrade/refresh`) — real-time/PAYG accounts return 402 or a 4xx whose detail names the real-time plan (only 403+code-1141 was recognized), so a benign no-op surfaced the alarming "rejected the refresh for all connections" 502. Broadened benign detection (402 / 403+1141 / real-time-plan detail), kept 401/404 as hard failures, partial success now reported OK, and the genuine-failure branch now logs per-auth status/code/detail (was a blind 502). (5) **bot-signals scheduler moved to Supabase `pg_cron`** — GitHub Actions' `schedule:` trigger proved unreliable (the new `*/15` workflow registered `active` but went ~1.5h / 5 slots without a single scheduled fire; GitHub scheduled crons are best-effort and routinely drop short-interval jobs — a real risk for a breakout-reactive bot). Replaced it with a Supabase `pg_cron` + `pg_net` job (`bot-signals-15min`, `*/15 * * * 1-5` UTC) that calls `public.trigger_bot_signals()` → reads `CRON_SECRET` from **Supabase Vault** by name (`cron_secret`, plaintext never in the job definition) and `net.http_get`s `/api/cron/bot-signals` with the bearer. The GitHub `schedule:` block is now **commented out** (commit `1b92024`) to prevent two schedulers double-firing the same tick and racing on the lock-less handler (could open two positions); the workflow stays as a manual `workflow_dispatch` backstop, and vercel.json keeps its daily entry. **One operational step pending: the owner must store `CRON_SECRET` in Vault** (`select vault.create_secret('…','cron_secret',…)`); until then the job fires every 15 min but safely no-ops (function returns early when the secret is absent). See [[bot-snaptrade-impact-401]] + [[vercel-hobby-cron-limit]].
>
> **2026-06-30 (cont.) — bot went live + first real fill + tuning**: (1) **Vault secret stored → pipeline live.** `vault.create_secret(CRON_SECRET,'cron_secret')` done; `trigger_bot_signals()` now actually HTTP-calls `/api/cron/bot-signals` (verified `net._http_response` 200). The pg_cron had been "succeeding" but no-opping for days (returned early, missing secret) — `cron.job_run_details` "succeeded" only means the SQL ran, check `net._http_response.status_code` for true end-to-end. (2) **AFFORDABILITY BUG fixed (commit `1d3c42c`)** — the real reason nothing traded: the entry engine picked the highest day-momentum name in the WHOLE universe regardless of price, then floored to whole shares → AMD (+8% @ $582) won the pick on a $100 budget → qty=0 → traded nothing while cheaper qualifying names (SPTE) sat ignored. Adding high-priced halal stocks to a small-account universe starved it. Fix: compute deployable budget first, only consider a candidate if `floor(deployable/price) >= 1`; strongest AFFORDABLE name wins; pricier names activate when capital is raised. (3) **Per-strategy entry threshold** — `params.entry_threshold_pct` (default 1.5%, owner's set to 1.0%) replaces the hardcoded global. (4) **Trailing-stop exit (commit `47ba3a4`)** — high-water in `params.high_water`, arms at `trail_activate_pct` (10%), sells on `trail_pct` (6%) pullback from peak, profit-only. (5) **Universe widened** — owner's SPUS strategy now 6 halal ETFs (SPUS/HLAL/UMMA/SPSK/SPRE/SPTE) + 5 halal blue-chips (NVDA/AMD/AAPL/GOOGL/META, fund-to-activate >$100). (6) **FIRST LIVE AUTONOMOUS FILL** — bought **2 SPTE @ ~$48.81** (~$97.62) on E\*Trade `302ebba3`, status=executed, no error. End-to-end proven. (7) User's strategic conclusion: short-term momentum on slow halal ETFs is a strategy/asset mismatch. **(8) DCA strategy_type BUILT** (commit `7cd25de`, migration `023`) — `strategy_type='dca'` long-term accumulation: buys whole shares of the target on a fixed cadence (`params.dca_cadence_days`, default 7) up to `params.dca_amount`, capped by capital exposure, and HOLDS (own cron branch before the momentum/exit engine — never auto-sold; manual exit ideally >1yr). One-attempt/day guard; cadence advances only on a real fill. Owner DCA strategy `0fd266ae` = SPUS weekly, $100, full-auto, account `302ebba3`; verified the branch runs end-to-end (first test rejected `impact 403: exchanges not open` — market was closed, NOT a bug). **(9) Per-strategy entry threshold** (`params.entry_threshold_pct`), **affordability-filtered pick** (entry now requires `floor(deployable/price) >= 1` — fixed the "picks AMD @ $582 on a $100 budget → trades nothing" bug), and **trailing-stop exit** (`params.trail_pct`/`trail_activate_pct`, high-water in `params.high_water`). **(10) Watchlist auto-syncs all strategy tickers** + client-side `fixTicker()` typo correction. **(11) PENDING: Alpaca data API** (free extended-hours prices + market-hours gate so the bot stops placing orders after-hours; the all-hours `*/15` cron currently no-ops via broker rejection outside RTH). **(12) DCA UI cleanup** (commit `02cbe00`) — DCA strategies now render as accumulate-and-hold (DCA tag, "ACCUMULATE · Nd", "CAPITAL DEPLOYED" bar, no meaningless 0% target/stop). **(13) SnapTrade broker-capability research** — what can actually trade (+fractional) via SnapTrade: **Fidelity = READ-ONLY (no order placement)**, **E\*Trade = whole-shares-only**, **Robinhood = trade + fractional + instant deposits** (the match for fractional baskets), Schwab = trade but likely whole-share via API, Coinbase = crypto-only, Chase/Firstrade unconfirmed. **(14) PENDING: equal-weight fractional $50/wk basket DCA** (~15–17 wks) over a user halal symbol list — must run on **Robinhood** (Fidelity can't trade, E\*Trade can't do fractional); blocked on symbol list + full-auto-vs-semi for `khanstyle02` (beta = full-auto root-only). See [[bot-execution-model]], [[bot-snaptrade-impact-401]], [[alpaca-data-pending]], [[snaptrade-broker-capabilities]].
>
> **2026-06-25 session changes**: (1) **Trading Bot opened to beta users** — migration `022_trading_bot_beta` adds `profiles.trading_bot_enabled` (allowlist, default false, independent of `is_root`) + `profiles.trading_bot_consent_at`. `canUseTradingBot()` = root OR `trading_bot_enabled`; `is_root` still solely gates admin/keys/anomaly. **Full-auto is now root-only and unreachable for beta users** (server rejects `403 full_auto_root_only` on create/edit + per-account opt-in PATCH). **First-use consent gate** (`POST /api/bot/consent`, experimental/not-an-RIA/not-advice) blocks strategy-create / signal-approve / order-place for non-root until accepted; UI hides the builder behind a consent card and shows only Manual/Semi layers. `/api/user/features` now returns `is_root` + `trading_bot_consented`. (2) **In-place strategy editing** — `PATCH /api/bot/strategies/:id` now edits ticker/account/type/universe + all risk params (was create-only), re-enforces the stop-loss gate and read-modify-writes `params`; new pre-filled Edit modal in the UI. (3) **Realized-P&L ledger** — `botRealizedPnl()` (per-ticker avg-cost walk over executed signals) + `GET /api/bot/trades` (aggregate realized + win rate + closed round-trips); new "REALIZED P&L · CLOSED TRADES" tile + per-strategy realized line. **(3b) Bot Activity timeline** — `GET /api/bot/activity` (all signals + outcomes from `pending_signals`, newest-first ≤100) + a "BOT ACTIVITY · ALL ACTIONS" tile in the Signals sub-tab (BUY/SELL, FILLED/PENDING/APPROVED/REJECTED/EXPIRED/FAILED, side/qty/ticker/strategy/price/time + inline error). Reads the bot's own ledger so a full-auto fill shows there the instant the cron runs, before the broker-synced Portfolio → Activity tab. Built after full-auto went live on `akhan.industries` so the user can watch autonomous trades without opening the brokerage. (4) **Overview hero relabeled** "Total Portfolio Value" → **"Net Worth"** (it computes net worth, collided with Portfolio's holdings-only "Market Value"). (5) **First-run fixes** — `demoMode` now **defaults OFF** (new users see real $0 + Welcome card, not the demo persona's ~$41M; demo is opt-in via `mizan_demo=1`); onboarding **CSV/document step removed** (Welcome·Connect·AI·Done); new connection **immediately** `fetchSnapHoldings()` so every tab updates even with zero holdings. (6) **CRON OUTAGE FIXED** — `CRON_SECRET` was never set in Vercel (a typo'd `CRON_SECRE` existed), so the fail-closed `cronUnauthorized()` 401'd **every** cron — nothing ran for the app's life (empty `cron_jobs`, zero `cron.*` audit rows, `cron.stale` alert). Set `CRON_SECRET` in Vercel Production → crons live. Also fixed: bot-signals cron 500'd on `.catch is not a function` (×3 Supabase-builder `.catch()` misuses — expire/reset/dividend-notif), and `cron.sync` success now writes an `audit({action:"cron.sync"})` row so the staleness detector + admin "last sync" have a real signal. NEW ENV (now set): `CRON_SECRET`. (7) **whats-new-banner** project skill added (curates the landing ticker after a commit).
>
> **2026-06-24 session changes**: (1) **Trading Bot** — strategies now SCREEN a halal universe and PICK the ticker (cron `screenSymbol` over `params.universe_tickers` / `HALAL_UNIVERSE_DEFAULT`), size from capital, price from market; buys only from screener, sells only from the exit engine (no shorting); one open position per strategy; exit engine quotes the held ticker. Three layers (manual/semi/full) are now a **per-strategy** choice (`params.layer`, no migration — DB `mode` derived) switched via an inline selector behind an **ack gate**. Order Ticket reframed as ad-hoc manual override. (2) **Sharia screening unified** — new `lib/sharia.mjs` provider seam (Finnhub now, **Zoya** when `ZOYA_API_KEY` set) + `GET/POST /api/screen`; `h.sh_` now flows from the live verdict (`shariaScreen` state via a root effect) with `SHARIA_MAP` only as pre-load fallback, so Screener / Overview compliance / Rebalancer halal-mode / Purification all read ONE verdict. Purification uses a screen-derived non-permissible-income % when the provider supplies it (Zoya). NEW ENV: `ZOYA_API_KEY` (+ optional `ZOYA_API_BASE`) — not yet provisioned.
>
> **2026-06-17 session changes**: Range-aware portfolio gains (1D/1W/1M/3M/YTD/1Y/All); real chart axes (XAxis months+year, YAxis $0→auto); Activity net-flow + range label; TaxPlanner normSym() helper; Screener cache freshness dot; fake sparkline replaced with position count + cost basis. CLAUDE.md rewritten to elite engineering brief (17 sections). Session hooks added (.claude/settings.json) — build check on edit, auto-update audit date on Stop.

---

## ═══ SECTION 1 — ARCHITECTURE & STACK ═══

### Frontend
| Item | Detail |
|------|--------|
| Framework | React 18.3.1 (JSX, not TypeScript) |
| Build tool | Vite 5.4.1 |
| Entry | `src/main.jsx` → `src/App.jsx` → `src/components/MizanApp.jsx` |
| Charts | recharts 2.12.7 (dashboards/donuts) · **lightweight-charts 5.2.0** (holdings candlestick price chart — `src/components/charts/PriceChart.jsx`, dynamic-imported so it stays out of the initial bundle) |
| Charts data prep | `src/components/charts/holdingsOverlay.js` (pure — user's executed trades → chart markers) |
| Routing | Client-side SPA (no router library — tab state in `useState`). Two URL entry points only: `/oauth-redirect` (Plaid resume) and **`?tab=` deep links** (added 2026-08-15 so the manifest's app shortcuts land where they claim; validated against the real tab list, stripped via `replaceState` after use) |
| PWA | Service worker `public/sw.js` (cache-v19) · manifest with `id`/shortcuts/`launch_handler`/**5 install screenshots** (Chrome's richer install UI) · **22 iOS launch images** in `public/splash/` · VAPID push. Orientation unlocked 2026-08-15 (rotates). iOS status bar is `default` — **never `black-translucent`**, it renders the clock white over the paper canvas and Apple deprecated it. Guarded by `src/test/pwaManifest.test.js` |
| Fonts | Google Fonts: Fraunces (display/stats), IBM Plex Sans (body), IBM Plex Mono (labels) |

**Bundle (from last `npm run build`):**

| Chunk | Uncompressed | Gzip |
|-------|-------------|------|
| `index-*.js` (main app) | 543 KB | ~153 KB |
| `charts-*.js` (recharts) | 547 KB | ~155 KB |
| `lightweight-charts.production-*.js` (async — loads only when a holding chart mounts) | 197 KB | ~63 KB |
| `supabase-*.js` | 207 KB | ~53 KB |
| `vendor-*.js` (react/react-dom) | 0.03 KB | ~0.05 KB |
| **Total (initial load; lightweight-charts is deferred)** | **~1.30 MB** | **~361 KB** |

### Backend
- **Runtime**: Node.js (ESM modules)
- **Dev server**: `server.js` (285 lines) — unified Vite middleware + API handler on port 3000
- **Production**: `api/[...path].mjs` — single Vercel catch-all serverless function (Hobby plan, `runtime: "nodejs"`)
- **Route logic**: `lib/handlers.mjs` (~6,070 lines — **see Section 7**)
- **Support libs**: `lib/alerts.mjs`, `lib/anomaly.mjs`, `lib/crypto.mjs`, `lib/fetchWithRetry.mjs`, `lib/logger.mjs`, `lib/notify.mjs`, `lib/rateLimit.mjs`, `lib/sentry.mjs`, `lib/sharia.mjs`, `lib/etfHoldings.mjs`
- **Market data (pure)**: `lib/market/candles.mjs` — validation (symbol regex, resolution whitelist, bounded window) + Polygon→chart normalization for the price chart; unit-tested, no I/O
- **Compliance**: `lib/compliance/policy.mjs` (three data tiers + prohibited-pattern list + `assertImpersonal`), `lib/compliance/advisor-prompt.mjs` (hardened `/api/advisor` system prefix — imported by the route + `/api/advisor/count`), `lib/compliance/advisor-filter.mjs` (deterministic post-generation backstop that rewrites personalized-advice output to the compliant redirect). See `docs/COMPLIANCE.md`.

### Database (Supabase / PostgreSQL)
26 migrations — all sequential (011 gap was patched; note two files share the `023` prefix — `023_bot_strategy_type_dca` + `023_email_digest`). Bot tables (020–022) added in the 2026-06-24/25 sessions; `023_bot_strategy_type_dca` (allows `strategy_type='dca'`) added 2026-06-30; `024_etf_holdings_cache` (service-role-only holdings cache for the ETF Overlap Analyzer) added 2026-07-05; `025_messages` 2026-07-20; `026_user_names` (profiles.first_name/last_name) 2026-07-22:

| Table | Key Columns | RLS | Notes |
|-------|-------------|-----|-------|
| `audit_log` | id, user_id, action, target, metadata, ip, user_agent, created_at | ✅ SELECT own | Append-only, service-role writes |
| `user_snaptrade` | user_id (PK), snaptrade_user_id, snaptrade_user_secret_enc, snaptrade_user_secret_nonce | ✅ Full CRUD | **AES-256-GCM encrypted** (plaintext column dropped by mig 017) |
| `user_state` | (user_id, key) PK, value jsonb, updated_at | ✅ Full CRUD | Generic key/value store |
| `user_keys` | user_id (PK), finnhub_key, polygon_key, encrypted boolean | ✅ Full CRUD | `encrypted` column now `true` — keys AES-256-GCM encrypted at application layer |
| `plaid_tokens` | id, user_id, access_token, item_id, institution_name, transactions_cursor | ✅ **No client policy** | access_token server-only (intentional) |
| `plaid_accounts` | id, user_id, item_id, account_id, name, type, subtype, balances | ✅ SELECT own | |
| `plaid_transactions` | id, user_id, item_id, account_id, transaction_id, amount, category_primary/detailed, date, pending, raw_data | ✅ SELECT own | Cursor-based sync |
| `rate_limits` | id, user_id, window_key, count, created_at | ✅ **No client policy** | Service-role only, atomic RPC |
| `profiles` | id (PK = auth.users.id), email, **first_name, last_name**, is_root, suspended, suspended_at | ✅ SELECT own (no UPDATE policy — name writes go through the service role) | Auto-created on signup via trigger; the trigger copies first/last name from auth metadata (mig 026) |
| `push_subscriptions` | id, user_id, endpoint, p256dh, auth | ✅ SELECT own | Per-device Web Push endpoints |
| `polygon_cache` | id, ticker, from_date, to_date, timespan, data, cached_at | ✅ **No client policy** | Shared market-data cache, 24h TTL |
| `cron_jobs` | id, job_name, last_run_at, last_status, run_count | ✅ **No client policy** | Scheduler ledger |
| `account_nicknames` | (user_id, account_id) PK, nickname | ✅ Full CRUD | |
| `budgets` | (user_id, category) PK, monthly_limit, currency | ✅ Full CRUD | |
| `goals` | id, user_id, name, target_amount, target_date, account_ids[], track_mode, manual_progress | ✅ Full CRUD | |
| `security_events` | id, ip, event_type, user_id, metadata, created_at | ✅ **No client policy** | **NEW (mig 018)** — DB-backed IP blocks replacing in-memory Map |
| `purification_ratios` | id, ticker, impurity_pct, source, updated_at | ✅ SELECT public | **NEW (mig 019)** — AAOIFI impurity % per ETF; seeded with SPUS/HLAL/UMMA/SPSK/SPRE/SPTE/AMAGX/AMANX |
| `bot_strategies` | id, user_id, ticker, account_id, strategy_type, params jsonb (layer, universe_tickers, entry/exit rules), mode, capital_allocated, profit_target_pct, stop_loss_pct, max_drawdown_pct, time_horizon_days, max_trades_per_day, enabled | owner-gated | **NEW (mig 020)** — owner/beta trading-bot strategies; `params.layer` is the user-facing execution layer, DB `mode` is the full-auto safety gate |
| `pending_signals` | id, user_id, strategy_id, ticker, side, qty, suggested_price, status (pending/approved/executed/expired/rejected), executed_at, expires_at, error_msg | owner-gated | **NEW (mig 020)** — bot BUY/SELL signals; executed rows ARE the realized-P&L ledger (`botRealizedPnl`) |
| `account_full_auto` | (user_id, account_id) PK, enabled (default false), updated_at | owner-only RLS | **NEW (mig 021)** — per-account Layer-3 opt-in; third gate of the full-auto triple-gate (root + master switch + this). PATCH now root-only |
| `profiles` (+cols) | …, `trading_bot_enabled` (default false), `trading_bot_consent_at` | ✅ SELECT own | **NEW cols (mig 022)** — beta bot allowlist (independent of `is_root`) + first-use consent timestamp |

**RLS functions (service-role only):**
- `increment_rate_limit(user_id, window_key, max)` — atomic upsert rate counter
- `get_user_sessions(user_id)` / `revoke_session()` / `revoke_other_sessions()` — bridge to `auth.sessions`

### Auth
- **Method**: Email + password via Supabase Auth (`supabase.auth.signInWithPassword`)
- **MFA**: TOTP (Google Authenticator-style) via Supabase MFA — enroll/unenroll/verify fully implemented in `src/lib/auth.jsx`
- **AAL2 enforcement**: Server-side `requireAAL2()` in `handlers.mjs` gating Plaid link operations — requires MFA-verified session
- **Session handling**: Active sessions panel (list/revoke individual sessions / revoke all others) via `006_sessions.sql` RPCs
- **Admin role**: `profiles.is_root` column (DB source of truth) + `VITE_OWNER_EMAIL` env bootstrap fallback

### Hosting
- **Frontend + Backend**: Vercel (project `mizan`, org `team_1jpYtfQNP39boDKElAshCOOL`)
- **Single function**: `api/[...path].mjs` handles all `/api/*`
- **Cron jobs** (vercel.json):

| Path | Schedule | Purpose |
|------|----------|---------|
| `/api/cron/sync` | `0 6 * * *` (daily 6 AM UTC) | SnapTrade sync across all users |
| `/api/cron/cleanup` | `0 3 * * *` (daily 3 AM UTC) | Data cleanup |
| `/api/cron/nightly-snapshot` | `55 4 * * *` | Net worth snapshot |
| `/api/cron/weekly-digest` | `0 13 * * 1` (Mon 1 PM UTC) | Weekly digest push notification |
| `/api/cron/dividend-check` | `0 11 * * *` | Dividend detection + purification push notification |
| `/api/cron/bill-reminders` | `0 14 * * *` | Bill reminder notifications |
| `/api/cron/bot-signals` | `0 14 * * 1-5` (vercel.json, daily backstop) **+ Supabase `pg_cron` `*/15 * * * 1-5` UTC** (job `bot-signals-15min` → `public.trigger_bot_signals()`) | Trading-bot strategy evaluation + signal generation/execution. Vercel Hobby caps crons at daily, so the every-15-min weekday cadence runs in Supabase: `pg_cron` fires `trigger_bot_signals()`, which reads `CRON_SECRET` from Vault (`cron_secret`) and `pg_net.http_get`s the endpoint with the bearer. Moved off GitHub Actions 2026-06-30 (its `schedule:` was unreliable — see header note (5); the workflow's `schedule:` is now commented out, kept only for manual `workflow_dispatch`). **Requires the `cron_secret` Vault entry** or the job no-ops. |

### File Count & Large File Flags
- **Total source files**: ~120 (excluding node_modules/worktrees)
- **🚨 MizanApp.jsx**: **9,201 lines** — contains ALL views, ALL state, ALL data structures
- **🚨 handlers.mjs**: **4,239 lines** — every API route in one file
- Both are far over the 800-line guideline

---

## ═══ SECTION 2 — EXTERNAL INTEGRATIONS & APIs ═══

| Integration | Status | Endpoints That Call It | Key Env Var | Tier | Prod? |
|-------------|--------|----------------------|-------------|------|-------|
| **SnapTrade** | ✅ Fully wired | `/api/snaptrade/status`, `login`, `accounts`, `all`, `holdings`, `activities`, `disconnect`, `documents`, `refresh`, `trade/impact`, `trade/place` | `VITE_SNAPTRADE_CLIENT_ID` + `VITE_SNAPTRADE_CONSUMER_KEY` | Free sandbox / paid production | Yes |
| **Plaid** | ✅ Fully wired | `/api/plaid/link-token`, `exchange`, `accounts`, `transactions`, `item-status`, `item` (DELETE), `webhook` | `PLAID_CLIENT_ID`, `PLAID_SECRET`, `PLAID_ENV` | Paid production | Yes |
| **Anthropic** | ✅ Fully wired | `/api/advisor` (POST), `/api/advisor/count` (POST) — hardened compliance prompt (`lib/compliance/advisor-prompt.mjs`) + post-generation filter (`advisor-filter.mjs`) that rewrites personalized-advice output to the compliant redirect | `ANTHROPIC_KEY` | Pay-as-you-go (~$0.01/msg) | Yes |
| **Stooq** | ✅ Fully wired | `/api/metals/spot` (proxies Stooq CSV) | None (free, no key) | Free | Yes |
| **Finnhub** | ✅ Fully wired | `/api/finnhub/earnings`, `dividends`, `profile2`, `metric`, `quote`, `news`; **`/api/market/quote`** (chart live line); **Sharia screening fundamentals via `lib/sharia.mjs` → `/api/screen`** | `FINNHUB_KEY` / `VITE_FINNHUB_KEY` | Free tier (60 req/min — **no `/stock/candle`**; the price chart uses Polygon for OHLC) | Yes |
| **Zoya** | 🔌 Seam ready, not keyed | `lib/sharia.mjs` → `/api/screen` (overrides Finnhub when `ZOYA_API_KEY` set; adds non-permissible-income test + direct verdict; falls back to Finnhub on error) | `ZOYA_API_KEY`, `ZOYA_API_BASE` (opt) | Free (per user — not yet provisioned) | No (provider seam only) |
| **Polygon** | ✅ Fully wired | `/api/polygon/bars` (backtester) **+ `/api/market/candles`** (holdings price chart — auth-gated, IMPERSONAL, validated); both share `getPolygonBars()` (24h `polygon_cache` + `fetchWithRetry` backoff + stale-on-failure) | `POLYGON_KEY` | Free tier (5 req/min, 2yr history) | Yes (backtester + price chart) |
| **Alpaca** | ⚙️ Partially | `/api/alpaca/order`, `orders`, `positions` — **paper API only** | `ALPACA_KEY_ID`, `ALPACA_SECRET` | Free paper trading | Backend yes, UI gated |
| **Supabase** | ✅ Fully wired | DB + Auth for entire app | `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` | Paid | Yes |
| **Resend** | ⚙️ Partially | `lib/alerts.mjs` — anomaly alert emails only (no user-facing transactional email) | `RESEND_API_KEY` | Free (3000/mo) | Yes (alerts only) |
| **Sentry** | ✅ Fully wired | Frontend (`@sentry/react`) + backend (`@sentry/node`) v10.52 | `VITE_SENTRY_DSN`, `SENTRY_DSN` | Free tier (5k events/mo) | Yes |
| **Web Push (VAPID)** | ✅ Fully wired | `/api/notifications/subscribe`, `vapid-public-key`, `test` | `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` | `web-push` library | Yes |

**Not present**: No Alpaca production keys (paper only). No outbound email to users (only owner alert emails via Resend).

---

## ═══ SECTION 3 — FEATURE INVENTORY ═══

### Overview Tab
| Feature | Status |
|---------|--------|
| Total portfolio value (Snap + Plaid combined) | ✅ Built + Deployed |
| Net worth history area chart | ✅ Built + Deployed |
| Asset allocation donut (halal/review/haram/cash) | ✅ Built + Deployed |
| Zakat Due tile (live gold/silver nisab via Stooq) | ✅ Built + Deployed |
| Dividend purification callout (pending owed → Zakat tab CTA) | ✅ Built + Deployed |
| Sadaqah donated total | ✅ Built + Deployed |
| YTD metrics (contributions, dividends, fees, returns) | ✅ Built + Deployed |
| Account cards with PLAID / SNAPTRADE badge | ✅ Built + Deployed |
| Values masking ("privacy mode") | ✅ Built + Deployed |
| Demo mode (fixture data when no accounts connected) | ✅ Built + Deployed |

### Finances Tab (Plaid)
| Feature | Status |
|---------|--------|
| Plaid bank account list with balances | ✅ Built + Deployed |
| Transaction list (cursor sync) | ✅ Built + Deployed |
| Spending by category (Plaid categories) | ✅ Built + Deployed |
| Budget management (monthly caps per category) | ✅ Built + Deployed |
| Bills Calendar (`BillsCalendar.jsx`) | ✅ Built + Deployed |
| "Awaiting data" empty state before first sync | ✅ Built + Deployed |

### Portfolio Tab
| Sub-tab | Status |
|---------|--------|
| Holdings (live Finnhub prices, Sharia status, unrealized P&L) | ✅ Built + Deployed |
| Per-holding price chart (candlestick + volume, 7 timeframes 1D–5Y, Polygon-fed, IMPERSONAL) | ✅ Built + Deployed · `PriceChart.jsx`; neutral SMA/EMA/volume toggles, **no signal/target UI** |
| Per-holding account-servicing overlay (your executed trades + "Your average cost" line) | ✅ Built + Deployed · privacy-gated, display-only, no judgments (`holdingsOverlay.js`) |
| Per-holding news accordion (3 headlines, expandable inline) | ✅ Built + Deployed |
| Per-holding earnings accordion (next/last earnings dates + EPS) | ✅ Built + Deployed |
| Activity (SnapTrade transaction history) | ✅ Built + Deployed |
| Rebalance (target asset class weights, drift suggestions) | ✅ Built + Deployed · halal-mode keys off the live screen (shared `sh_`) |
| Tax (unrealized gains/losses breakdown) | ✅ Built + Deployed |
| Backtest (Polygon OHLC, strategy runner) | ✅ Built + Deployed |
| Screener (AAOIFI 7-framework Sharia screener) | ✅ Built + Deployed · now backed by `lib/sharia.mjs` → `/api/screen` (single source of truth for `sh_`); provider seam ready for Zoya |
| ETFs & Funds catalog | ✅ Built + Deployed |
| Documents (SnapTrade account statements) | ✅ Built + Deployed |

### Goals Tab
| Sub-tab | Status |
|---------|--------|
| Goals (savings goals, progress bars, projected completion, Islamic templates) | ✅ Built + Deployed |
| Goals Overview widget on Goals tab header | ✅ Built + Deployed |
| Zakat & Sadaqah (full Zakat calc with methodology toggles) | ✅ Built + Deployed |
| Dividend Purification (AAOIFI — per-dividend purification log, Mark Purified / Purify All, inline impurity % override) | ✅ Built + Deployed |
| Retirement / FIRE calculator | ✅ Built + Deployed |

### AI Advisor Tab
| Feature | Status |
|---------|--------|
| Claude Sonnet 4 chat with portfolio context injected | ✅ Built + Deployed |
| Server-side Sharia guardrails system prompt | ✅ Built + Deployed · now the **hardened compliance prompt** (`lib/compliance/advisor-prompt.mjs`): explain-don't-recommend, refuses personalized advice |
| Compliance output filter (rewrites personalized-advice output to the compliant redirect; flags audited) | ✅ Built + Deployed · `lib/compliance/advisor-filter.mjs` — see `docs/COMPLIANCE.md` |
| Rate limited (60/hr) | ✅ Built + Deployed |
| Token count preflight (`/api/advisor/count`) | ✅ Built + Deployed |

### Settings Tab
| Sub-section | Status |
|------------|--------|
| Connect accounts (SnapTrade OAuth + Plaid Link with MFA gate) | ✅ Built + Deployed |
| Connection health dashboard (`ConnectionHealth.jsx`) | ✅ Built + Deployed |
| Account profile + email change + first/last name editor | ✅ Built + Deployed |
| Security (TOTP MFA enroll/disable, active sessions, revoke) | ✅ Built + Deployed |
| Push notifications (subscribe/unsubscribe) | ✅ Built + Deployed |
| Manual assets (gold, silver, cash, real estate, liabilities) | ✅ Built + Deployed |
| Documents (SnapTrade statements) | ✅ Built + Deployed |
| Privacy & Data (export + account delete) | ✅ Built + Deployed |
| About page | ✅ Built + Deployed |
| Admin panel (users, audit log, stats, suspend) — root-only | ✅ Built + Deployed |

### Trade Tab (folded into Portfolio)
| Feature | Status |
|---------|--------|
| Order Ticket UI (buy/sell form, SnapTrade + Alpaca toggle) | ⚙️ Built but **double-gated** — wrapped in both `<ComingSoon>` AND `{false && ...}` |
| Alpaca paper trading backend (`/api/alpaca/order`) | ✅ Built + Deployed (backend only) |
| SnapTrade live trading backend (`/api/snaptrade/trade/*`) | ✅ Built + Deployed (backend only) |
| Sharia precheck blocklist (HARAM_TICKERS) | ✅ Built + Deployed (backend only) |

---

## ═══ SECTION 4 — SECURITY POSTURE ═══

### Data Storage
| Data | Where | Notes |
|------|-------|-------|
| Plaid `access_token` | `plaid_tokens` table (server-only) | No RLS client policy by design — only accessible via service-role key. Never reaches browser. |
| SnapTrade `userSecret` | `user_snaptrade.snaptrade_user_secret_enc` + `_nonce` | **AES-256-GCM encrypted at application layer** (mig 016 added enc columns; mig 017 dropped plaintext column). Key from `APP_ENCRYPTION_KEY` env. |
| Plaid transactions | `plaid_transactions` | RLS SELECT own. Written server-side only. |
| User preferences, watchlist, Zakat settings | `user_state` + localStorage | Supabase-backed with localStorage mirror |
| Per-user API keys (Finnhub, Polygon) | `user_keys` | RLS CRUD own. `encrypted = true` — AES-256-GCM encrypted at application layer |
| Push endpoints | `push_subscriptions` | RLS SELECT own |
| Auth sessions | `auth.sessions` (Supabase internal) | Accessed only via service-role RPCs |
| IP blocks | `security_events` | **DB-backed** — persists across cold starts and Vercel instances |

### Encryption
`lib/crypto.mjs` provides `encryptField(plaintext)` / `decryptField(enc, nonce)` using Node `crypto.createCipheriv('aes-256-gcm', ...)`. Key is `Buffer.from(APP_ENCRYPTION_KEY, 'hex')` — 32-byte hex string from env. Migration `016_encrypt_secrets.sql` added `_enc`/`_nonce` columns; migration `017_drop_plaintext_secrets.sql` dropped plaintext columns.

### Rate Limiting
- **Mechanism**: Supabase DB-backed atomic `increment_rate_limit` RPC (survives cold starts, shared across Vercel instances)
- **Anonymous fallback**: In-memory Map keyed by IP
- **Limits** (per-hour): snaptrade.login: 30, anthropic: 60, plaid: 120, plaid.sync: 10, auth: 20, export: 20, feedback: 10, default: 6000, anon: 300
- **Root bypass**: Admin users skip all rate limits

### Audit Logging
- **What's logged**: `auth.sign_in`, `auth.sign_out`, `auth.signin_failed`, `auth.mfa_enrolled/unenrolled`, `auth.password_changed`, `auth.new_device`, `broker.connect_initiated`, `broker.disconnect`, `plaid.link_completed`, `plaid.item_deleted`, `alpaca.order_placed`, `alpaca.order_blocked`, `goal.created/updated/deleted`, `settings.api_keys_saved`, `account.export`, `account.deleted`, `budget.*`, `cron.sync`, `cron.cleanup`
- **Retention**: No explicit TTL column or cleanup cron targeting audit_log specifically
- **Access**: Users can SELECT their own rows. Admin reads all via service-role

### RLS Coverage
Migration `015_rls_audit_and_select_policies.sql` performed a pre-launch audit and confirmed **no user-scoped table is missing a SELECT policy**. New tables added since: `security_events` (intentionally server-only, no client policy) and `purification_ratios` (public SELECT, no user data).

### MFA / Session Revocation
- TOTP MFA fully implemented and available to all users
- AAL2 enforced server-side for Plaid link operations specifically
- MFA is **not mandatory** globally — only for bank linking
- Full session revocation (individual + "all others") via SQL RPCs

### Anomaly Detection (4 detectors in `lib/anomaly.mjs`)
| Detector | Trigger | Action | Persistence |
|----------|---------|--------|-------------|
| Brute force | 5 auth failures from same IP in 60s | 24h IP block + Resend alert | ✅ **DB-backed** (`security_events` table — survives cold starts) |
| SnapTrade 5xx spike | 10 upstream 5xx in 5 min | Resend alert | ⚠️ In-memory |
| Cron staleness | >25h since last cron.sync or cleanup | Resend alert | DB-backed (reads audit_log) |
| New device sign-in | (IP, UA hash) not in last 20 logins | Resend alert | DB-backed (reads audit_log) |

### CSP Headers (vercel.json)
Present and reasonably strict. Uses `'unsafe-inline'` for `script-src` (Plaid Link requires it) and `style-src` — not nonce-based. `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`. HSTS with preload (2-year max-age).

### Secrets Scan
No hardcoded secrets found in any source file. All keys read from `process.env`. **RESOLVED (2026-06-16, `f600d9d`; F6 closed 2026-07-15):** the legacy `.snaptrade-users.json` flat file is no longer read or written by any production code — `getSnapForRequest` (`lib/handlers.mjs`) is Supabase-only. The file is not part of the runtime path; the only remaining reference is the owner-only diagnostic `scripts/verify-refresh.mjs`, now env-var-first (`SNAPTRADE_PRIMARY_SECRET`) with the flat file as a deprecated local fallback.

---

## ═══ SECTION 5 — DEPLOYED vs BUILT-BUT-NOT-DEPLOYED ═══

### Live in Production
- All 6 nav tabs with all sub-views
- All API routes in `handlers.mjs`
- 6 Vercel cron jobs
- Service worker / PWA install (full install surface for Chrome + iOS as of 2026-08-15)
- Sentry error tracking (frontend + backend)
- All 22 Supabase migrations (020–022 applied in prod via MCP this session)

### Built but NOT Live in UI
- **Order Ticket**: UI code exists in `MizanApp.jsx` but is wrapped in `{false && ...}` (hardcoded dead code). Clicking the tab shows a `<ComingSoon>` tile. Two separate gates to remove before it renders.
- **Alpaca paper trading UI** and **SnapTrade live trading UI**: Both backend API routes fully deployed; no UI path reaches them.

### Demo Mode
A believable ~$435k demo persona (rescaled 2026-07-15 from ~$55M, `107763e`) lives in hardcoded `DEMO_ACCOUNTS`, `DEMO_BANK_ACCOUNTS`, `DEMO_TRANSACTIONS`, `DEMO_ACTIVITIES`, `DEMO_MANUAL_ASSETS`, `DEMO_SADAQAH`, `DEMO_SHARIA`, `DEMO_PURIFICATION_ITEMS` inside `MizanApp.jsx`. **As of 2026-06-25 demo is OPT-IN, not the default** — `demoMode` initializes from `localStorage.mizan_demo==="1"` only; a new/connection-less user sees their real **$0 + Welcome/Connect** state, never the demo's ~$41M as their net worth. The DEMO toggle (shown while `!hasRealData || demoMode`) flips `mizan_demo` and `fetchSnapHoldings` swaps in/out `DEMO_ACCOUNTS` via the `[demoMode]` effect. (Previously demo defaulted ON for any user without real data, which routed the demo book into the net-worth headline — that was the "$0 → $41M" bug.)

### Backend Endpoints with No Active Frontend Caller
- `/api/alpaca/orders` (GET), `/api/alpaca/positions` (GET) — exist in handlers, no component fetches them
- `/api/debug/sentry-test` — admin-only debug route
- `/api/admin/stats`, `/api/admin/users/:id/suspend`, `/api/admin/users/:id/broker` — callable from AdminPanel (root-only)

---

## ═══ SECTION 6 — FEATURE GAP CHECK ═══

### 1. Dividend Purification Automation
**Status**: ✅ **BUILT + DEPLOYED** (completed 2026-06-16)

`purification_ratios` table seeded with AAOIFI impurity % for 8 tickers (SPUS 1.70%, HLAL 2.80%, UMMA 2.20%, SPSK 0.50%, SPRE 3.80%, SPTE 1.50%, AMAGX 1.20%, AMANX 2.40%). `/api/purification/calculate` endpoint computes `dividend × impurity_pct / 100` per holding. `PurificationPanel` component in Goals → Zakat & Sadaqah shows per-dividend table with Mark Purified / Purify All actions, inline impurity % override, and scholar disclaimer. Overview tile shows CTA with pending purification total. Cron dividend-check triggers push notification when unpurified dividends accumulate.

---

### 2. Islamic Calendar / Hijri Integration
**Status**: ❌ Not present — Zakat calc exists but no calendar

What exists: Full Zakat calculator with nisab gating, live gold/silver prices, methodology toggles. Hawl tracking is conceptual only — no Hijri date, no per-asset hawl start date, no countdown.

What's missing: Hijri date library, per-asset hawl start date storage, Ramadan detection, giving-mode UI, Zakat due date reminder push notification.

Reusable: ZakatSadaqah component, goals table (could store hawl dates), push notifications infrastructure.

**Effort: M**

---

### 3. Goal-Based Investing (Hajj fund, Mahr savings, etc.)
**Status**: ✅ **BUILT + DEPLOYED** (completed 2026-06-16)

Islamic goal templates (Hajj, Mahr, Home down payment, Emergency fund, Education, Business) available as one-tap pre-fills. Goals Overview widget on the Goals tab header shows progress summary.

---

### 4. Per-Holding News & Earnings
**Status**: ✅ **BUILT + DEPLOYED** (completed 2026-06-16)

Expandable accordion row on each holding in the Holdings table. News tab shows 3 latest Finnhub headlines with source + age. Earnings tab shows next/last earnings dates and EPS estimate vs actual. Results cached 30 min client-side.

---

### 5. Manual Monthly Budget Tracker
**Status**: ✅ Substantially built — Muslim categories are the gap

What exists: `Budgeting.jsx` (425 lines), `/api/budgets` CRUD, `budgets` table, per-category monthly caps with progress bars, actual spend pulled from Plaid transaction categories.

What's missing: Muslim-household category presets (Sadaqah, Masjid donations, Islamic school fees, Halal food). Currently uses Plaid's generic category labels.

Reusable: Everything already exists.

**Effort: S**

---

### 6. Shareable Snapshot Links
**Status**: ❌ Not present

No read-only view, no token-based share routes, no anonymization logic.

Reusable: `user_state`, SnapTrade account/holdings structure.

**Effort: M**

---

### 7. Browser Extension
**Status**: ❌ Not present

No extension manifest, no content script, nothing.

Reusable: Sharia screener logic in MizanApp.jsx (AAOIFI categories), Finnhub profile endpoint.

**Effort: L**

---

### 8. Risk-Adjusted Return Metrics (Sharpe, drawdown, volatility vs SPUS)
**Status**: ❌ Not present

Activity tab shows total return and YTD P&L. Backtest shows strategy return vs buy-and-hold. No Sharpe ratio, max drawdown, volatility, or benchmark comparison.

Reusable: SnapTrade activities data, Polygon OHLC bars endpoint, backtester data pipeline.

**Effort: M**

---

### 9. Anonymous Peer Comparison
**Status**: ❌ Not present

No aggregation, no anonymized peer benchmark, no user cohort data.

Reusable: Would need opt-in consent + aggregate query views on `user_state`/balances.

**Effort: L**

---

## ═══ SECTION 7 — DESIGN SYSTEM ═══

### Typography
| Role | Font | Usage |
|------|------|-------|
| Display / Stats | Fraunces (optical-size variable serif) | All hero numbers, stat values, large headings (fontSize ≥ 16 with `tabular-nums`) |
| Body | IBM Plex Sans | All body text, labels, form inputs, descriptions (fontSize 11–15) |
| Mono | IBM Plex Mono | Tickers, chips, pills, code-style labels, buttons |

Loaded via Google Fonts with `display=swap`. `FU` constant = Fraunces, `FP` = IBM Plex Sans, `FM` = IBM Plex Mono.

### Color Palette (dark theme — `data-theme="dark"`)
| Token | Value | Semantic Role |
|-------|-------|---------------|
| `--mz-bg` | `#0d1311` | Page background (forest-green ink) |
| `--mz-surface` | `#111a17` | Elevated surface |
| `--mz-card` | `#15201c` | Card / tile background |
| `--mz-border` | `#2a3a33` | Default border |
| `--mz-borderHi` | `#3a4f45` | Hovered border |
| `--mz-text` | `#b7b3a6` | Body text |
| `--mz-textHi` | `#e9e4d6` | High-emphasis text / paper |
| `T.blue` | `#c9a24b` | Gold — primary accent, buttons, links |
| `T.blueDim` | `#9a7a35` | Gold dim — gradient end for buttons |
| `T.gold` | `#cf9e54` | Amber — warnings, Zakat amounts |
| `T.gain` | `#6fae8e` | Jade — halal / healthy / gain |
| `T.loss` | `#c46a52` | Rust — haram / error / loss |
| `T.slate` | `#7b94a6` | Unscreened holdings |
| `T.violet` | `#9d86b8` | Crypto / alternative assets |

Light theme (`data-theme="light"`) uses warm paper `#f5f2eb` background with matching surface/border adjustments.

### Bento Layout
- `BentoTile` component: 2px top accent bar, 1px tinted left border, hover lift (`translateY(-1px)`), click lift (`translateY(-2px) scale(1.003)`), `box-shadow` from `--sh-md/lg/sm`
- `BentoRow`: CSS grid with fake-gap border technique (1px gap on `--mz-border` background)
- Ambient glows: `body::before` (gold radial, top-right) + `body::after` (jade radial, bottom-left)

### Components with local tokens updated
All satellite components updated to match main design system: Goals.jsx, Budgeting.jsx, ConnectionHealth.jsx, ComingSoon.jsx, BillsCalendar.jsx, BugReportButton.jsx, CommandPalette.jsx, Login.jsx, LegalLayout.jsx.

---

## ═══ SECTION 8 — HONEST ASSESSMENT ═══

### Top 3 Most Production-Ready

**1. Security architecture**
RLS confirmed gap-free across all 17 user-scoped tables. TOTP MFA, session revocation, persistent DB-backed rate limiting, **DB-backed IP blocks** (survives cold starts), four anomaly detectors, audit log covering ~20 action types, Sentry on both sides, CSP headers with HSTS preload. SnapTrade `userSecret` and user API keys now AES-256-GCM encrypted at application layer. Plaid `access_token` server-only.

**2. Plaid integration**
Full cursor-based transaction sync with `MAX_PAGES=200` hard cap, webhook support, MFA (AAL2) enforcement before bank linking, error taxonomy with user-safe messages (12 error codes mapped to human copy), `access_token` architecture correct (never reaches browser). The `syncPlaidItem` function handles added/modified/removed deltas properly.

**3. Islamic finance feature set**
Live Zakat nisab via Stooq (gold/silver spot), AAOIFI dividend purification with per-ticker impurity ratios, full Sharia screener with business activity + financial ratios, halal ETF catalog, Sadaqah log, goal templates (Hajj, Mahr, etc.), FIRE calculator. Comprehensive for an Islamic personal finance app.

---

### Top 3 Weakest / Most Fragile Areas

**1. MizanApp.jsx is 9,201 lines**
A single file contains every view, every data structure (including all demo fixtures), all state management, all chart logic, all Zakat calculation, and all Sharia screening. It has hardcoded `HOLDINGS`, `ACCOUNTS`, `SADAQAH`, `ETF_LIST`, `BROKERS`, `DEMO_ACCOUNTS`, `DEMO_ACTIVITIES`, `DEMO_TRANSACTIONS` arrays. This file is practically untestable and is a serious maintenance liability.

**2. ~~SnapTrade 5xx spike detector is still in-memory~~ — RESOLVED (corrected 2026-07-05)**
This was stale: `trackSnapTradeError` (`lib/anomaly.mjs:110-132`) is **DB-backed** via `security_events`, same as the brute-force/IP-block detector — verified by direct code inspection in the 2026-07-05 security audit. No cold-start reset risk. (CLAUDE.md §10.5 corrected to match.)

**3. No test suite**
Zero unit tests, zero integration tests, zero E2E tests. Every change is verified manually. The 9,201-line MizanApp.jsx makes retroactive test-writing very difficult. Adding any meaningful test coverage requires component extraction first.

---

### Things Found Broken or Half-Wired

0. **[RESOLVED 2026-06-25] Crons never ran for the app's life.** `CRON_SECRET` was never set in Vercel (a typo'd `CRON_SECRE` had been created). The fail-closed `cronUnauthorized()` (`!CRON_SECRET || bearer mismatch`) therefore 401'd every `/api/cron/*` invocation — empty `cron_jobs`, zero `cron.*` audit rows, recurring `cron.stale` high alert. **Fix: set `CRON_SECRET` in Vercel Production** (Vercel only auto-attaches the cron `Authorization: Bearer` header when that exact var exists). Two code bugs surfaced once crons finally ran and are also fixed: (a) bot-signals 500'd on `.catch is not a function` — three Supabase filter builders chained `.catch()` (which builders don't have); (b) `cron.sync` logged success via `info()` but never wrote an audit row, so the staleness detector + admin "last sync" always read Infinity → now writes `audit({action:"cron.sync"})`. **Watch-outs:** Vercel **Redeploy** reuses the original deployment's env snapshot — a *fresh git build* is required to bind a newly-added env var; and the env-name typo class (`CRON_SECRE`) is silent because nothing reads it.

1. **Order Ticket is double-gated.** The tab renders `<ComingSoon>` for all users. The real UI code is also wrapped in `{false && sub==="order" && ...}` (hardcoded dead code). To re-enable, both the `<ComingSoon>` render AND the `{false && ...}` wrappers must be removed. The backend (`/api/alpaca/order`, `/api/snaptrade/trade/*`) is fully deployed and ready.

2. **~~Legacy `.snaptrade-users.json` flat file still active.~~ RESOLVED (F6, `f600d9d` 2026-06-16 removed the prod path; F6 closed 2026-07-15).** No production code reads or writes this file — `getSnapForRequest` (`lib/handlers.mjs`) is Supabase-only (`grep readFileSync\|writeFileSync\|snaptrade-users` over `lib/` + `api/` = 0 hits). The prior "file-read code path remains active" claim was stale. The only remaining reference is the owner-only diagnostic `scripts/verify-refresh.mjs`, now env-var-first with a deprecated flat-file fallback.

3. **Purification `purification_ratios` table needs ongoing maintenance.** Seeded with 8 tickers at launch. New halal ETFs entering the portfolio won't have a ratio and will fall back to 0% (no purification shown). Needs either a data-provider feed or a manual admin UI to add ratios.

4. **`mizan_purification_log` and `mizan_purification_overrides` live in `user_state`/localStorage only.** Unlike Sadaqah entries (which write to `mizan_sadaqah` in user_state via Supabase sync), purification state is only localStorage-mirrored. A new browser/device loses all purification history.
